%% template.tex — Beacon research-paper template
%% Extracted and generalized from egohygiene/renderflow.
%%
%% Usage:
%%   pandoc input.md --template template.tex -o output.pdf
%%
%% Required metadata:
%%   title: "My Paper"
%%   author: "Author Name"
%%
%% Optional metadata:
%%   date: "2026"
%%   abstract: "A short summary of the paper."
%%   toc: true

\documentclass[$if(fontsize)$$fontsize$$else$11pt$endif$,$if(papersize)$$papersize$$else$a4paper$endif$]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{parskip}

\usepackage[
  $if(geometry)$$geometry$$else$
  top=2.5cm, bottom=2.5cm,
  left=3cm, right=3cm
  $endif$
]{geometry}

\usepackage{graphicx}
\usepackage{xcolor}

\usepackage[
  colorlinks=true,
  linkcolor=black,
  citecolor=black,
  urlcolor=blue
]{hyperref}

\usepackage{titlesec}
\titleformat{\section}{\large\bfseries}{\thesection}{1em}{}[\titlerule]
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection}{1em}{}
\titleformat{\subsubsection}{\normalsize\itshape}{\thesubsubsection}{1em}{}

\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\itshape $if(title)$$title$$endif$}
\fancyhead[R]{\small\itshape $if(author)$$author$$endif$}
\fancyfoot[C]{\small\thepage}
\renewcommand{\headrulewidth}{0.4pt}

\usepackage{enumitem}
\setlist{noitemsep, topsep=4pt}

\usepackage{booktabs}
\usepackage{longtable}

\usepackage{fancyvrb}
\usepackage{listings}
\lstset{
  basicstyle=\small\ttfamily,
  breaklines=true,
  frame=single,
  framerule=0.4pt,
  xleftmargin=0.5em,
  xrightmargin=0.5em
}

\usepackage{amsmath}
\usepackage{amssymb}

$for(header-includes)$
$header-includes$
$endfor$

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

\begin{document}

$if(title)$
\begin{center}
  {\LARGE\bfseries $title$}\\[0.8em]
  $if(author)${\large $author$}\\[0.4em]$endif$
  $if(date)${\normalsize $date$}$endif$
\end{center}
\vspace{1em}
$endif$

$if(abstract)$
\begin{abstract}
  \noindent $abstract$
\end{abstract}
\vspace{1em}
$endif$

$if(toc)$
\tableofcontents
\newpage
$endif$

$body$

\end{document}
