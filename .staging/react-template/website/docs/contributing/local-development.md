# Local development

1. Enable Corepack.
2. Install with `pnpm install --frozen-lockfile`.
3. Start the apps with `pnpm dev` or run them individually with `pnpm --filter ... dev`.
4. Validate changes with `pnpm test`, `pnpm build`, and `pnpm check`.
