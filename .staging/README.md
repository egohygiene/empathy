# Staging is frozen

Do not add material to this directory unless the same change updates the
deterministic file ledger and classification rules.

Read [`docs/migration/STAGING.md`](../docs/migration/STAGING.md) before moving,
activating, or removing anything. Staged files are inert intake evidence, not
active repository source. No removal is allowed without destination evidence,
owner validation, preserved provenance, and a separate recorded approval.
