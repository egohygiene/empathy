# Repository architecture

Applications depend on shared packages. Shared packages depend on foundational packages and tokens.

## Direction

- applications → shared product packages
- shared product packages → foundational packages
- foundational packages → tokens and utilities
