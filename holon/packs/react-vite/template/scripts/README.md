# Scripts

- `scripts/checks/*` keep the workspace honest.
- `scripts/generators/*` create small, explicit workspace scaffolds.
- `scripts/release/*` validate release intent without publishing private workspaces.
