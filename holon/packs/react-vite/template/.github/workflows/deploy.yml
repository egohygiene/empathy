name: Deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-until-configured:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: printf '%s\n' "Deployment is intentionally disabled until hosting credentials and environment configuration are provided."
