# Identity integration history

Empathy's incubated Identity v0 integration has been promoted to the versioned
v1 consumer contract. The current operational guide is
[Identity v1 consumer integration](IDENTITY_V1.md). The original extraction
revision remains recorded in `.config/identity/consumer-lock.json`; it is not a
runtime dependency or a second implementation.
