# Safe Secretlint integration fixture.
#
# These values are intentionally descriptive placeholders and are not valid
# credentials.

APPLICATION_ENVIRONMENT=development
API_TOKEN=replace-with-your-api-token
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=example-user
DATABASE_PASSWORD=replace-with-your-database-password

