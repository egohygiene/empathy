# Secret Management Fixture

Store credentials in the repository's approved secret-management service.

Copy the environment template and replace its descriptive placeholders only in
an ignored local environment file. Never commit real API keys, private keys,
session tokens, passwords, or authenticated connection strings.
