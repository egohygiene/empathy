# Ansible inventories

This directory contains portable inventory fixtures used to validate Egolint's
Ansible policy. Real environment addresses, credentials, vault material, and
organization-specific hostnames must not be committed here.

Use the `example/` inventory as a syntax-safe reference and add focused fixtures
only when a rule requires coverage.
