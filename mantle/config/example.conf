# Copyright 2026 Ego Hygiene
# SPDX-License-Identifier: MIT
# Mantle configuration schema version. This field is required.
schema_version=1

# Built-ins: minimal, standard, full, workbench, ci, share-safe.
profile=standard

# Optional typed overrides. Environment overrides take precedence.
# aliases.safe=true
# aliases.network=false
# aliases.system=false
# aliases.legacy=false
# aliases.safety=false
# history=true
# updates.suppress_checks=false
# experimental=false
# presentation=private
