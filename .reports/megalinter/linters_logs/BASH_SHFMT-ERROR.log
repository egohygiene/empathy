Results of shfmt linter (version 3.13.1)
See documentation on https://megalinter.io/10.0.0/descriptors/bash_shfmt/
-----------------------------------------------

❌ [ERROR] for workspace /github/workspace
Linter raw log:
diff .devcontainer/scripts/initialize.sh.orig .devcontainer/scripts/initialize.sh
--- .devcontainer/scripts/initialize.sh.orig
+++ .devcontainer/scripts/initialize.sh
@@ -96,7 +96,7 @@
 #   Writes the formatted message to stdout.
 ##
 print_status() {
-    printf "%s %s\n" "$STATUS_PREFIX" "$*"
+  printf "%s %s\n" "$STATUS_PREFIX" "$*"
 }
 
 ##
@@ -109,9 +109,9 @@
 #   Writes the formatted message to stderr when enabled.
 ##
 print_debug() {
-    if ((VERBOSE == 1)); then
-        printf "%s %s\n" "$DEBUG_PREFIX" "$*" >&2
-    fi
+  if ((VERBOSE == 1)); then
+    printf "%s %s\n" "$DEBUG_PREFIX" "$*" >&2
+  fi
 }
 
 ##
@@ -124,7 +124,7 @@
 #   Writes the formatted message to stderr.
 ##
 print_warning() {
-    printf "%s %s\n" "$WARNING_PREFIX" "$*" >&2
+  printf "%s %s\n" "$WARNING_PREFIX" "$*" >&2
 }
 
 ##
@@ -137,9 +137,9 @@
 #   Does not return.
 ##
 die() {
-    ERROR_REPORTED=1
-    printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
-    exit 1
+  ERROR_REPORTED=1
+  printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
+  exit 1
 }
 
 ##
@@ -152,10 +152,10 @@
 #   Does not return.
 ##
 usage_error() {
-    ERROR_REPORTED=1
-    printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
-    printf "%s Try --help for usage information.\n" "$ERROR_PREFIX" >&2
-    exit 2
+  ERROR_REPORTED=1
+  printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
+  printf "%s Try --help for usage information.\n" "$ERROR_PREFIX" >&2
+  exit 2
 }
 
 ##
@@ -168,14 +168,14 @@
 #   0 when enabled; otherwise 1.
 ##
 is_enabled() {
-    case "${1:-0}" in
-        1 | true | TRUE | yes | YES | on | ON)
-            return 0
-            ;;
-        *)
-            return 1
-            ;;
-    esac
+  case "${1:-0}" in
+  1 | true | TRUE | yes | YES | on | ON)
+    return 0
+    ;;
+  *)
+    return 1
+    ;;
+  esac
 }
 
 ##
@@ -185,11 +185,11 @@
 #   TEMPORARY_FILE
 ##
 cleanup() {
-    if [[ -n $TEMPORARY_FILE && -f $TEMPORARY_FILE ]]; then
-        rm -f "$TEMPORARY_FILE"
-    fi
-
-    TEMPORARY_FILE=""
+  if [[ -n $TEMPORARY_FILE && -f $TEMPORARY_FILE ]]; then
+    rm -f "$TEMPORARY_FILE"
+  fi
+
+  TEMPORARY_FILE=""
 }
 
 ##
@@ -204,20 +204,20 @@
 #   The original command exit code.
 ##
 on_error() {
-    local exit_code="${1:?Exit code is required}"
-    local line_number="${2:?Line number is required}"
-    local command="${3:-unknown command}"
-
-    trap - ERR
-    ERROR_REPORTED=1
-
-    printf "%s Command failed with exit code %d at line %d: %s\n" \
-        "$ERROR_PREFIX" \
-        "$exit_code" \
-        "$line_number" \
-        "$command" >&2
-
-    return "$exit_code"
+  local exit_code="${1:?Exit code is required}"
+  local line_number="${2:?Line number is required}"
+  local command="${3:-unknown command}"
+
+  trap - ERR
+  ERROR_REPORTED=1
+
+  printf "%s Command failed with exit code %d at line %d: %s\n" \
+    "$ERROR_PREFIX" \
+    "$exit_code" \
+    "$line_number" \
+    "$command" >&2
+
+  return "$exit_code"
 }
 
 ##
@@ -230,16 +230,16 @@
 #   Does not return.
 ##
 on_exit() {
-    local exit_code="${1:?Exit code is required}"
-
-    trap - EXIT
-    cleanup
-
-    if ((exit_code != 0 && ERROR_REPORTED == 0)); then
-        print_warning "Initialization stopped with exit code $exit_code."
-    fi
-
-    exit "$exit_code"
+  local exit_code="${1:?Exit code is required}"
+
+  trap - EXIT
+  cleanup
+
+  if ((exit_code != 0 && ERROR_REPORTED == 0)); then
+    print_warning "Initialization stopped with exit code $exit_code."
+  fi
+
+  exit "$exit_code"
 }
 
 ##
@@ -253,12 +253,12 @@
 #   Does not return.
 ##
 on_signal() {
-    local signal_name="${1:?Signal name is required}"
-    local exit_code="${2:?Exit code is required}"
-
-    ERROR_REPORTED=1
-    print_warning "Received $signal_name; stopping initialization."
-    exit "$exit_code"
+  local signal_name="${1:?Signal name is required}"
+  local exit_code="${2:?Exit code is required}"
+
+  ERROR_REPORTED=1
+  print_warning "Received $signal_name; stopping initialization."
+  exit "$exit_code"
 }
 
 ##
@@ -265,10 +265,10 @@
 # Installs error, exit, and signal handlers.
 ##
 install_traps() {
-    trap 'on_error "$?" "$LINENO" "$BASH_COMMAND"' ERR
-    trap 'on_exit "$?"' EXIT
-    trap 'on_signal "SIGINT" 130' INT
-    trap 'on_signal "SIGTERM" 143' TERM
+  trap 'on_error "$?" "$LINENO" "$BASH_COMMAND"' ERR
+  trap 'on_exit "$?"' EXIT
+  trap 'on_signal "SIGINT" 130' INT
+  trap 'on_signal "SIGTERM" 143' TERM
 }
 
 # -----------------------------------------------------------------------------
@@ -279,16 +279,16 @@
 # Prints command usage to stdout.
 ##
 print_usage() {
-    printf "Usage: %s [OPTIONS]\n" "${0##*/}"
-    printf "\n"
-    printf "Initialize the host-side, repository-agnostic Dev Container baseline.\n"
-    printf "\n"
-    printf "Options:\n"
-    printf "  --devcontainer-id ID  Record and display the Dev Container identifier.\n"
-    printf "  --dry-run             Report actions without modifying files or running hooks.\n"
-    printf "  --force               Ignore a matching initialization marker and run again.\n"
-    printf "  --verbose             Print additional diagnostic information.\n"
-    printf "  --help                Show this help text.\n"
+  printf "Usage: %s [OPTIONS]\n" "${0##*/}"
+  printf "\n"
+  printf "Initialize the host-side, repository-agnostic Dev Container baseline.\n"
+  printf "\n"
+  printf "Options:\n"
+  printf "  --devcontainer-id ID  Record and display the Dev Container identifier.\n"
+  printf "  --dry-run             Report actions without modifying files or running hooks.\n"
+  printf "  --force               Ignore a matching initialization marker and run again.\n"
+  printf "  --verbose             Print additional diagnostic information.\n"
+  printf "  --help                Show this help text.\n"
 }
 
 ##
@@ -298,47 +298,47 @@
 #   $@ - Script arguments.
 ##
 parse_arguments() {
-    while (($# > 0)); do
-        case "$1" in
-            --devcontainer-id)
-                (($# >= 2)) || usage_error "Option --devcontainer-id requires a value."
-                DEVCONTAINER_ID="$2"
-                shift
-                ;;
-            --devcontainer-id=*)
-                DEVCONTAINER_ID="${1#*=}"
-                ;;
-            --dry-run)
-                DRY_RUN=1
-                ;;
-            --force)
-                FORCE_INITIALIZATION=1
-                ;;
-            --verbose)
-                VERBOSE=1
-                ;;
-            --help)
-                print_usage
-                exit 0
-                ;;
-            --)
-                shift
-                break
-                ;;
-            -*)
-                usage_error "Unknown option: $1"
-                ;;
-            *)
-                usage_error "Unexpected positional argument: $1"
-                ;;
-        esac
-
-        shift
-    done
-
-    if (($# > 0)); then
-        usage_error "Unexpected positional argument: $1"
-    fi
+  while (($# > 0)); do
+    case "$1" in
+    --devcontainer-id)
+      (($# >= 2)) || usage_error "Option --devcontainer-id requires a value."
+      DEVCONTAINER_ID="$2"
+      shift
+      ;;
+    --devcontainer-id=*)
+      DEVCONTAINER_ID="${1#*=}"
+      ;;
+    --dry-run)
+      DRY_RUN=1
+      ;;
+    --force)
+      FORCE_INITIALIZATION=1
+      ;;
+    --verbose)
+      VERBOSE=1
+      ;;
+    --help)
+      print_usage
+      exit 0
+      ;;
+    --)
+      shift
+      break
+      ;;
+    -*)
+      usage_error "Unknown option: $1"
+      ;;
+    *)
+      usage_error "Unexpected positional argument: $1"
+      ;;
+    esac
+
+    shift
+  done
+
+  if (($# > 0)); then
+    usage_error "Unexpected positional argument: $1"
+  fi
 }
 
 ##
@@ -348,17 +348,17 @@
 #   DEVCONTAINER_ID
 ##
 normalize_devcontainer_id() {
-    if [[ $DEVCONTAINER_ID == "\${devcontainerId}" ]]; then
-        print_warning "The devcontainerId substitution was not resolved by this implementation."
-        DEVCONTAINER_ID=""
-        return 0
-    fi
-
-    case "$DEVCONTAINER_ID" in
-        *$'\n'* | *$'\r'*)
-            usage_error "The Dev Container ID must not contain line breaks."
-            ;;
-    esac
+  if [[ $DEVCONTAINER_ID == "\${devcontainerId}" ]]; then
+    print_warning "The devcontainerId substitution was not resolved by this implementation."
+    DEVCONTAINER_ID=""
+    return 0
+  fi
+
+  case "$DEVCONTAINER_ID" in
+  *$'\n'* | *$'\r'*)
+    usage_error "The Dev Container ID must not contain line breaks."
+    ;;
+  esac
 }
 
 # -----------------------------------------------------------------------------
@@ -375,12 +375,12 @@
 #   Writes the resolved path to stdout.
 ##
 resolve_directory() {
-    local directory="${1:?Directory path is required}"
-
-    (
-        cd -- "$directory"
-        pwd -P
-    )
+  local directory="${1:?Directory path is required}"
+
+  (
+    cd -- "$directory"
+    pwd -P
+  )
 }
 
 ##
@@ -387,18 +387,18 @@
 # Resolves the script directory, repository root, and marker path.
 ##
 resolve_paths() {
-    SCRIPT_DIRECTORY="$(resolve_directory "$(dirname "${BASH_SOURCE[0]}")")"
-
-    if [[ -n ${DEVCONTAINER_REPO_ROOT:-} ]]; then
-        REPO_ROOT="$(resolve_directory "$DEVCONTAINER_REPO_ROOT")"
-    else
-        REPO_ROOT="$(resolve_directory "$SCRIPT_DIRECTORY/../..")"
-    fi
-
-    INITIALIZATION_MARKER_FILE="${REPO_ROOT}/${DEVCONTAINER_CACHE_DIRECTORY}/${INITIALIZATION_MARKER_NAME}"
-
-    [[ -d $REPO_ROOT/$DEVCONTAINER_DIRECTORY ]] ||
-        print_warning "Expected Dev Container directory not found: $REPO_ROOT/$DEVCONTAINER_DIRECTORY"
+  SCRIPT_DIRECTORY="$(resolve_directory "$(dirname "${BASH_SOURCE[0]}")")"
+
+  if [[ -n ${DEVCONTAINER_REPO_ROOT:-} ]]; then
+    REPO_ROOT="$(resolve_directory "$DEVCONTAINER_REPO_ROOT")"
+  else
+    REPO_ROOT="$(resolve_directory "$SCRIPT_DIRECTORY/../..")"
+  fi
+
+  INITIALIZATION_MARKER_FILE="${REPO_ROOT}/${DEVCONTAINER_CACHE_DIRECTORY}/${INITIALIZATION_MARKER_NAME}"
+
+  [[ -d $REPO_ROOT/$DEVCONTAINER_DIRECTORY ]] ||
+    print_warning "Expected Dev Container directory not found: $REPO_ROOT/$DEVCONTAINER_DIRECTORY"
 }
 
 ##
@@ -408,14 +408,14 @@
 #   $1 - Directory to add.
 ##
 prepend_path_once() {
-    local directory="${1:?Directory path is required}"
-
-    case ":${PATH:-}:" in
-        *":$directory:"*) ;;
-        *) PATH="$directory${PATH:+:$PATH}" ;;
-    esac
-
-    export PATH
+  local directory="${1:?Directory path is required}"
+
+  case ":${PATH:-}:" in
+  *":$directory:"*) ;;
+  *) PATH="$directory${PATH:+:$PATH}" ;;
+  esac
+
+  export PATH
 }
 
 ##
@@ -422,23 +422,23 @@
 # Configures the environment inherited by repository-specific hooks.
 ##
 configure_environment() {
-    : "${HOME:?HOME must be set for Dev Container initialization}"
-
-    export XDG_CACHE_HOME="${XDG_CACHE_HOME:-$HOME/.cache}"
-    export XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
-    export XDG_DATA_HOME="${XDG_DATA_HOME:-$HOME/.local/share}"
-    export XDG_STATE_HOME="${XDG_STATE_HOME:-$HOME/.local/state}"
-
-    export DEVCONTAINER_ID
-    export DEVCONTAINER_LIFECYCLE="initialize"
-    export DEVCONTAINER_REPO_ROOT="$REPO_ROOT"
-    export DEVCONTAINER_SCRIPT_DIRECTORY="$SCRIPT_DIRECTORY"
-
-    prepend_path_once "$HOME/.local/bin"
-    prepend_path_once "$HOME/bin"
-
-    print_debug "Host: $(uname -s 2> /dev/null || printf "unknown")/$(uname -m 2> /dev/null || printf "unknown")"
-    print_debug "PATH: $PATH"
+  : "${HOME:?HOME must be set for Dev Container initialization}"
+
+  export XDG_CACHE_HOME="${XDG_CACHE_HOME:-$HOME/.cache}"
+  export XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
+  export XDG_DATA_HOME="${XDG_DATA_HOME:-$HOME/.local/share}"
+  export XDG_STATE_HOME="${XDG_STATE_HOME:-$HOME/.local/state}"
+
+  export DEVCONTAINER_ID
+  export DEVCONTAINER_LIFECYCLE="initialize"
+  export DEVCONTAINER_REPO_ROOT="$REPO_ROOT"
+  export DEVCONTAINER_SCRIPT_DIRECTORY="$SCRIPT_DIRECTORY"
+
+  prepend_path_once "$HOME/.local/bin"
+  prepend_path_once "$HOME/bin"
+
+  print_debug "Host: $(uname -s 2>/dev/null || printf "unknown")/$(uname -m 2>/dev/null || printf "unknown")"
+  print_debug "PATH: $PATH"
 }
 
 # -----------------------------------------------------------------------------
@@ -453,25 +453,25 @@
 #   $2 - Optional directory mode. Defaults to 0775.
 ##
 ensure_directory() {
-    local directory="${1:?Directory path is required}"
-    local mode="${2:-0775}"
-
-    if [[ -d $directory ]]; then
-        print_debug "Directory already exists: $directory"
-        return 0
-    fi
-
-    if ((DRY_RUN == 1)); then
-        print_status "Would create directory: $directory"
-        return 0
-    fi
-
-    print_status "Creating directory: $directory"
-    mkdir -p "$directory"
-
-    if ! chmod "$mode" "$directory" 2> /dev/null; then
-        print_warning "Could not set mode $mode on directory: $directory"
-    fi
+  local directory="${1:?Directory path is required}"
+  local mode="${2:-0775}"
+
+  if [[ -d $directory ]]; then
+    print_debug "Directory already exists: $directory"
+    return 0
+  fi
+
+  if ((DRY_RUN == 1)); then
+    print_status "Would create directory: $directory"
+    return 0
+  fi
+
+  print_status "Creating directory: $directory"
+  mkdir -p "$directory"
+
+  if ! chmod "$mode" "$directory" 2>/dev/null; then
+    print_warning "Could not set mode $mode on directory: $directory"
+  fi
 }
 
 ##
@@ -478,10 +478,10 @@
 # Creates harmless repository-local cache and temporary directories.
 ##
 create_required_directories() {
-    ensure_directory "$REPO_ROOT/$DEVCONTAINER_CACHE_DIRECTORY"
-    ensure_directory "$REPO_ROOT/$DEVCONTAINER_TEMP_DIRECTORY"
-    ensure_directory "$REPO_ROOT/.cache"
-    ensure_directory "$REPO_ROOT/.tmp"
+  ensure_directory "$REPO_ROOT/$DEVCONTAINER_CACHE_DIRECTORY"
+  ensure_directory "$REPO_ROOT/$DEVCONTAINER_TEMP_DIRECTORY"
+  ensure_directory "$REPO_ROOT/.cache"
+  ensure_directory "$REPO_ROOT/.tmp"
 }
 
 ##
@@ -488,31 +488,31 @@
 # Optionally creates .env from .env.example with private permissions.
 ##
 create_environment_file_if_requested() {
-    local destination="$REPO_ROOT/.env"
-    local source="$REPO_ROOT/.env.example"
-
-    is_enabled "${DEVCONTAINER_INIT_CREATE_ENV:-0}" || return 0
-
-    if [[ ! -f $source ]]; then
-        print_debug "Environment template not found: $source"
-        return 0
-    fi
-
-    if [[ -e $destination || -L $destination ]]; then
-        print_debug "Environment file already exists: $destination"
-        return 0
-    fi
-
-    if ((DRY_RUN == 1)); then
-        print_status "Would create .env from .env.example"
-        return 0
-    fi
-
-    print_status "Creating .env from .env.example"
-    (
-        umask 0077
-        cp "$source" "$destination"
-    )
+  local destination="$REPO_ROOT/.env"
+  local source="$REPO_ROOT/.env.example"
+
+  is_enabled "${DEVCONTAINER_INIT_CREATE_ENV:-0}" || return 0
+
+  if [[ ! -f $source ]]; then
+    print_debug "Environment template not found: $source"
+    return 0
+  fi
+
+  if [[ -e $destination || -L $destination ]]; then
+    print_debug "Environment file already exists: $destination"
+    return 0
+  fi
+
+  if ((DRY_RUN == 1)); then
+    print_status "Would create .env from .env.example"
+    return 0
+  fi
+
+  print_status "Creating .env from .env.example"
+  (
+    umask 0077
+    cp "$source" "$destination"
+  )
 }
 
 ##
@@ -522,17 +522,17 @@
 #   0 when a matching marker exists; otherwise 1.
 ##
 is_already_initialized() {
-    [[ -f $INITIALIZATION_MARKER_FILE ]] || return 1
-
-    grep -Fqx "schema_version=$INITIALIZATION_SCHEMA_VERSION" \
-        "$INITIALIZATION_MARKER_FILE" || return 1
-
-    if [[ -n $DEVCONTAINER_ID ]]; then
-        grep -Fqx "devcontainer_id=$DEVCONTAINER_ID" \
-            "$INITIALIZATION_MARKER_FILE" || return 1
-    fi
-
-    return 0
+  [[ -f $INITIALIZATION_MARKER_FILE ]] || return 1
+
+  grep -Fqx "schema_version=$INITIALIZATION_SCHEMA_VERSION" \
+    "$INITIALIZATION_MARKER_FILE" || return 1
+
+  if [[ -n $DEVCONTAINER_ID ]]; then
+    grep -Fqx "devcontainer_id=$DEVCONTAINER_ID" \
+      "$INITIALIZATION_MARKER_FILE" || return 1
+  fi
+
+  return 0
 }
 
 ##
@@ -539,28 +539,28 @@
 # Atomically writes the initialization completion marker.
 ##
 write_initialization_marker() {
-    local initialized_at
-
-    if ((DRY_RUN == 1)); then
-        print_status "Would write initialization marker: $INITIALIZATION_MARKER_FILE"
-        return 0
-    fi
-
-    initialized_at="$(date -u "+%Y-%m-%dT%H:%M:%SZ")"
-    TEMPORARY_FILE="$(mktemp "${INITIALIZATION_MARKER_FILE}.tmp.XXXXXX")"
-
-    {
-        printf "schema_version=%s\n" "$INITIALIZATION_SCHEMA_VERSION"
-        printf "initialized_at=%s\n" "$initialized_at"
-        printf "devcontainer_id=%s\n" "$DEVCONTAINER_ID"
-        printf "repository_root=%s\n" "$REPO_ROOT"
-    } > "$TEMPORARY_FILE"
-
-    chmod 0600 "$TEMPORARY_FILE"
-    mv "$TEMPORARY_FILE" "$INITIALIZATION_MARKER_FILE"
-    TEMPORARY_FILE=""
-
-    print_status "Wrote initialization marker: $INITIALIZATION_MARKER_FILE"
+  local initialized_at
+
+  if ((DRY_RUN == 1)); then
+    print_status "Would write initialization marker: $INITIALIZATION_MARKER_FILE"
+    return 0
+  fi
+
+  initialized_at="$(date -u "+%Y-%m-%dT%H:%M:%SZ")"
+  TEMPORARY_FILE="$(mktemp "${INITIALIZATION_MARKER_FILE}.tmp.XXXXXX")"
+
+  {
+    printf "schema_version=%s\n" "$INITIALIZATION_SCHEMA_VERSION"
+    printf "initialized_at=%s\n" "$initialized_at"
+    printf "devcontainer_id=%s\n" "$DEVCONTAINER_ID"
+    printf "repository_root=%s\n" "$REPO_ROOT"
+  } >"$TEMPORARY_FILE"
+
+  chmod 0600 "$TEMPORARY_FILE"
+  mv "$TEMPORARY_FILE" "$INITIALIZATION_MARKER_FILE"
+  TEMPORARY_FILE=""
+
+  print_status "Wrote initialization marker: $INITIALIZATION_MARKER_FILE"
 }
 
 # -----------------------------------------------------------------------------
@@ -574,26 +574,26 @@
 #   $1 - Hook path.
 ##
 run_hook() {
-    local hook="${1:?Hook path is required}"
-    local relative_hook="${hook#"$REPO_ROOT"/}"
-
-    if ((DRY_RUN == 1)); then
-        print_status "Would run hook: $relative_hook"
-        return 0
-    fi
-
-    print_status "Running hook: $relative_hook"
-
-    if bash "$hook"; then
-        return 0
-    fi
-
-    if is_enabled "${DEVCONTAINER_INIT_CONTINUE_ON_HOOK_FAILURE:-0}"; then
-        print_warning "Hook failed; continuing: $relative_hook"
-        return 0
-    fi
-
-    die "Hook failed: $relative_hook"
+  local hook="${1:?Hook path is required}"
+  local relative_hook="${hook#"$REPO_ROOT"/}"
+
+  if ((DRY_RUN == 1)); then
+    print_status "Would run hook: $relative_hook"
+    return 0
+  fi
+
+  print_status "Running hook: $relative_hook"
+
+  if bash "$hook"; then
+    return 0
+  fi
+
+  if is_enabled "${DEVCONTAINER_INIT_CONTINUE_ON_HOOK_FAILURE:-0}"; then
+    print_warning "Hook failed; continuing: $relative_hook"
+    return 0
+  fi
+
+  die "Hook failed: $relative_hook"
 }
 
 ##
@@ -600,41 +600,41 @@
 # Discovers and executes local hooks in deterministic filename order.
 ##
 run_extension_hooks() {
-    local hooks_directory="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/initialize.d"
-    local local_hook="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/initialize.local.sh"
-    local hook
-    local nullglob_was_enabled=0
-    local previous_lc_all="${LC_ALL-}"
-    local -a hooks=()
-
-    if [[ -f $local_hook ]]; then
-        run_hook "$local_hook"
-    fi
-
-    [[ -d $hooks_directory ]] || return 0
-
-    if shopt -q nullglob; then
-        nullglob_was_enabled=1
-    fi
-
-    LC_ALL=C
-    shopt -s nullglob
-    hooks=("$hooks_directory"/*.sh)
-
-    if ((nullglob_was_enabled == 0)); then
-        shopt -u nullglob
-    fi
-
-    if [[ -n $previous_lc_all ]]; then
-        LC_ALL="$previous_lc_all"
-    else
-        unset LC_ALL
-    fi
-
-    for hook in "${hooks[@]}"; do
-        [[ -f $hook ]] || continue
-        run_hook "$hook"
-    done
+  local hooks_directory="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/initialize.d"
+  local local_hook="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/initialize.local.sh"
+  local hook
+  local nullglob_was_enabled=0
+  local previous_lc_all="${LC_ALL-}"
+  local -a hooks=()
+
+  if [[ -f $local_hook ]]; then
+    run_hook "$local_hook"
+  fi
+
+  [[ -d $hooks_directory ]] || return 0
+
+  if shopt -q nullglob; then
+    nullglob_was_enabled=1
+  fi
+
+  LC_ALL=C
+  shopt -s nullglob
+  hooks=("$hooks_directory"/*.sh)
+
+  if ((nullglob_was_enabled == 0)); then
+    shopt -u nullglob
+  fi
+
+  if [[ -n $previous_lc_all ]]; then
+    LC_ALL="$previous_lc_all"
+  else
+    unset LC_ALL
+  fi
+
+  for hook in "${hooks[@]}"; do
+    [[ -f $hook ]] || continue
+    run_hook "$hook"
+  done
 }
 
 ##
@@ -647,13 +647,13 @@
 #   Writes the joined labels to stdout.
 ##
 join_labels() {
-    local label
-    local separator=""
-
-    for label in "$@"; do
-        printf "%s%s" "$separator" "$label"
-        separator=", "
-    done
+  local label
+  local separator=""
+
+  for label in "$@"; do
+    printf "%s%s" "$separator" "$label"
+    separator=", "
+  done
 }
 
 ##
@@ -660,28 +660,28 @@
 # Reports recognized project manifests without installing dependencies.
 ##
 report_detected_tooling() {
-    local -a detected=()
-
-    [[ -f $REPO_ROOT/package.json ]] && detected+=("Node.js")
-    [[ -f $REPO_ROOT/pyproject.toml || -f $REPO_ROOT/requirements.txt || -f $REPO_ROOT/setup.py ]] &&
-        detected+=("Python")
-    [[ -f $REPO_ROOT/Cargo.toml ]] && detected+=("Rust")
-    [[ -f $REPO_ROOT/go.mod ]] && detected+=("Go")
-    [[ -f $REPO_ROOT/pom.xml || -f $REPO_ROOT/build.gradle || -f $REPO_ROOT/build.gradle.kts ]] &&
-        detected+=("Java/JVM")
-    [[ -f $REPO_ROOT/Gemfile ]] && detected+=("Ruby")
-    [[ -f $REPO_ROOT/composer.json ]] && detected+=("PHP")
-    [[ -f $REPO_ROOT/compose.yaml || -f $REPO_ROOT/compose.yml || -f $REPO_ROOT/docker-compose.yaml || -f $REPO_ROOT/docker-compose.yml ]] &&
-        detected+=("Docker Compose")
-    [[ -f $REPO_ROOT/Taskfile.yml || -f $REPO_ROOT/Taskfile.yaml ]] && detected+=("Task")
-    [[ -f $REPO_ROOT/Makefile ]] && detected+=("Make")
-
-    if ((${#detected[@]} == 0)); then
-        print_debug "No common project manifests detected."
-        return 0
-    fi
-
-    print_status "Detected project tooling: $(join_labels "${detected[@]}")"
+  local -a detected=()
+
+  [[ -f $REPO_ROOT/package.json ]] && detected+=("Node.js")
+  [[ -f $REPO_ROOT/pyproject.toml || -f $REPO_ROOT/requirements.txt || -f $REPO_ROOT/setup.py ]] &&
+    detected+=("Python")
+  [[ -f $REPO_ROOT/Cargo.toml ]] && detected+=("Rust")
+  [[ -f $REPO_ROOT/go.mod ]] && detected+=("Go")
+  [[ -f $REPO_ROOT/pom.xml || -f $REPO_ROOT/build.gradle || -f $REPO_ROOT/build.gradle.kts ]] &&
+    detected+=("Java/JVM")
+  [[ -f $REPO_ROOT/Gemfile ]] && detected+=("Ruby")
+  [[ -f $REPO_ROOT/composer.json ]] && detected+=("PHP")
+  [[ -f $REPO_ROOT/compose.yaml || -f $REPO_ROOT/compose.yml || -f $REPO_ROOT/docker-compose.yaml || -f $REPO_ROOT/docker-compose.yml ]] &&
+    detected+=("Docker Compose")
+  [[ -f $REPO_ROOT/Taskfile.yml || -f $REPO_ROOT/Taskfile.yaml ]] && detected+=("Task")
+  [[ -f $REPO_ROOT/Makefile ]] && detected+=("Make")
+
+  if ((${#detected[@]} == 0)); then
+    print_debug "No common project manifests detected."
+    return 0
+  fi
+
+  print_status "Detected project tooling: $(join_labels "${detected[@]}")"
 }
 
 # -----------------------------------------------------------------------------
@@ -695,48 +695,48 @@
 #   $@ - Script arguments.
 ##
 main() {
-    parse_arguments "$@"
-    normalize_devcontainer_id
-    install_traps
-
-    if is_enabled "${DEVCONTAINER_INIT_VERBOSE:-0}"; then
-        VERBOSE=1
-    fi
-
-    if is_enabled "${DEVCONTAINER_INIT_FORCE:-0}"; then
-        FORCE_INITIALIZATION=1
-    fi
-
-    if is_enabled "${DEVCONTAINER_INIT_TRACE:-0}"; then
-        set -o xtrace
-    fi
-
-    resolve_paths
-    cd -- "$REPO_ROOT"
-    configure_environment
-
-    print_status "Initializing development container host state."
-    print_status "Repository root: $REPO_ROOT"
-
-    if [[ -n $DEVCONTAINER_ID ]]; then
-        print_status "Dev Container ID: $DEVCONTAINER_ID"
-    else
-        print_status "Dev Container ID: unavailable"
-    fi
-
-    create_required_directories
-    create_environment_file_if_requested
-    report_detected_tooling
-
-    if ((FORCE_INITIALIZATION == 0)) && is_already_initialized; then
-        print_status "Development container host state is already initialized."
-        return 0
-    fi
-
-    run_extension_hooks
-    write_initialization_marker
-
-    print_status "Development container host initialization complete."
+  parse_arguments "$@"
+  normalize_devcontainer_id
+  install_traps
+
+  if is_enabled "${DEVCONTAINER_INIT_VERBOSE:-0}"; then
+    VERBOSE=1
+  fi
+
+  if is_enabled "${DEVCONTAINER_INIT_FORCE:-0}"; then
+    FORCE_INITIALIZATION=1
+  fi
+
+  if is_enabled "${DEVCONTAINER_INIT_TRACE:-0}"; then
+    set -o xtrace
+  fi
+
+  resolve_paths
+  cd -- "$REPO_ROOT"
+  configure_environment
+
+  print_status "Initializing development container host state."
+  print_status "Repository root: $REPO_ROOT"
+
+  if [[ -n $DEVCONTAINER_ID ]]; then
+    print_status "Dev Container ID: $DEVCONTAINER_ID"
+  else
+    print_status "Dev Container ID: unavailable"
+  fi
+
+  create_required_directories
+  create_environment_file_if_requested
+  report_detected_tooling
+
+  if ((FORCE_INITIALIZATION == 0)) && is_already_initialized; then
+    print_status "Development container host state is already initialized."
+    return 0
+  fi
+
+  run_extension_hooks
+  write_initialization_marker
+
+  print_status "Development container host initialization complete."
 }
 
 main "$@"
diff .devcontainer/scripts/on-create.sh.orig .devcontainer/scripts/on-create.sh
--- .devcontainer/scripts/on-create.sh.orig
+++ .devcontainer/scripts/on-create.sh
@@ -100,7 +100,7 @@
 #   Writes the formatted message to stdout.
 ##
 print_status() {
-    printf "%s %s\n" "$STATUS_PREFIX" "$*"
+  printf "%s %s\n" "$STATUS_PREFIX" "$*"
 }
 
 ##
@@ -113,9 +113,9 @@
 #   Writes the formatted message to stderr when enabled.
 ##
 print_debug() {
-    if ((VERBOSE == 1)); then
-        printf "%s %s\n" "$DEBUG_PREFIX" "$*" >&2
-    fi
+  if ((VERBOSE == 1)); then
+    printf "%s %s\n" "$DEBUG_PREFIX" "$*" >&2
+  fi
 }
 
 ##
@@ -128,7 +128,7 @@
 #   Writes the formatted message to stderr.
 ##
 print_warning() {
-    printf "%s %s\n" "$WARNING_PREFIX" "$*" >&2
+  printf "%s %s\n" "$WARNING_PREFIX" "$*" >&2
 }
 
 ##
@@ -141,9 +141,9 @@
 #   Does not return.
 ##
 die() {
-    ERROR_REPORTED=1
-    printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
-    exit 1
+  ERROR_REPORTED=1
+  printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
+  exit 1
 }
 
 ##
@@ -156,10 +156,10 @@
 #   Does not return.
 ##
 usage_error() {
-    ERROR_REPORTED=1
-    printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
-    printf "%s Try --help for usage information.\n" "$ERROR_PREFIX" >&2
-    exit 2
+  ERROR_REPORTED=1
+  printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
+  printf "%s Try --help for usage information.\n" "$ERROR_PREFIX" >&2
+  exit 2
 }
 
 ##
@@ -172,14 +172,14 @@
 #   0 when enabled; otherwise 1.
 ##
 is_enabled() {
-    case "${1:-0}" in
-        1 | true | TRUE | yes | YES | on | ON)
-            return 0
-            ;;
-        *)
-            return 1
-            ;;
-    esac
+  case "${1:-0}" in
+  1 | true | TRUE | yes | YES | on | ON)
+    return 0
+    ;;
+  *)
+    return 1
+    ;;
+  esac
 }
 
 ##
@@ -189,11 +189,11 @@
 #   TEMPORARY_FILE
 ##
 cleanup() {
-    if [[ -n $TEMPORARY_FILE && -f $TEMPORARY_FILE ]]; then
-        rm -f "$TEMPORARY_FILE"
-    fi
-
-    TEMPORARY_FILE=""
+  if [[ -n $TEMPORARY_FILE && -f $TEMPORARY_FILE ]]; then
+    rm -f "$TEMPORARY_FILE"
+  fi
+
+  TEMPORARY_FILE=""
 }
 
 ##
@@ -208,20 +208,20 @@
 #   The original command exit code.
 ##
 on_error() {
-    local exit_code="${1:?Exit code is required}"
-    local line_number="${2:?Line number is required}"
-    local command="${3:-unknown command}"
-
-    trap - ERR
-    ERROR_REPORTED=1
-
-    printf "%s Command failed with exit code %d at line %d: %s\n" \
-        "$ERROR_PREFIX" \
-        "$exit_code" \
-        "$line_number" \
-        "$command" >&2
-
-    return "$exit_code"
+  local exit_code="${1:?Exit code is required}"
+  local line_number="${2:?Line number is required}"
+  local command="${3:-unknown command}"
+
+  trap - ERR
+  ERROR_REPORTED=1
+
+  printf "%s Command failed with exit code %d at line %d: %s\n" \
+    "$ERROR_PREFIX" \
+    "$exit_code" \
+    "$line_number" \
+    "$command" >&2
+
+  return "$exit_code"
 }
 
 ##
@@ -234,16 +234,16 @@
 #   Does not return.
 ##
 on_exit() {
-    local exit_code="${1:?Exit code is required}"
-
-    trap - EXIT
-    cleanup
-
-    if ((exit_code != 0 && ERROR_REPORTED == 0)); then
-        print_warning "On-create lifecycle stopped with exit code $exit_code."
-    fi
-
-    exit "$exit_code"
+  local exit_code="${1:?Exit code is required}"
+
+  trap - EXIT
+  cleanup
+
+  if ((exit_code != 0 && ERROR_REPORTED == 0)); then
+    print_warning "On-create lifecycle stopped with exit code $exit_code."
+  fi
+
+  exit "$exit_code"
 }
 
 ##
@@ -257,12 +257,12 @@
 #   Does not return.
 ##
 on_signal() {
-    local signal_name="${1:?Signal name is required}"
-    local exit_code="${2:?Exit code is required}"
-
-    ERROR_REPORTED=1
-    print_warning "Received $signal_name; stopping the on-create lifecycle."
-    exit "$exit_code"
+  local signal_name="${1:?Signal name is required}"
+  local exit_code="${2:?Exit code is required}"
+
+  ERROR_REPORTED=1
+  print_warning "Received $signal_name; stopping the on-create lifecycle."
+  exit "$exit_code"
 }
 
 ##
@@ -269,10 +269,10 @@
 # Installs error, exit, and signal handlers.
 ##
 install_traps() {
-    trap 'on_error "$?" "$LINENO" "$BASH_COMMAND"' ERR
-    trap 'on_exit "$?"' EXIT
-    trap 'on_signal "SIGINT" 130' INT
-    trap 'on_signal "SIGTERM" 143' TERM
+  trap 'on_error "$?" "$LINENO" "$BASH_COMMAND"' ERR
+  trap 'on_exit "$?"' EXIT
+  trap 'on_signal "SIGINT" 130' INT
+  trap 'on_signal "SIGTERM" 143' TERM
 }
 
 # -----------------------------------------------------------------------------
@@ -283,17 +283,17 @@
 # Prints command usage to stdout.
 ##
 print_usage() {
-    printf "Usage: %s [OPTIONS]\n" "${0##*/}"
-    printf "\n"
-    printf "Initialize the container-side, repository-agnostic Dev Container baseline.\n"
-    printf "\n"
-    printf "Options:\n"
-    printf "  --devcontainer-id ID  Record and display the Dev Container identifier.\n"
-    printf "  --container-id ID     Override runtime container identity detection.\n"
-    printf "  --dry-run             Report actions without modifying files or running hooks.\n"
-    printf "  --force               Ignore a matching on-create marker and run again.\n"
-    printf "  --verbose             Print additional diagnostic information.\n"
-    printf "  --help                Show this help text.\n"
+  printf "Usage: %s [OPTIONS]\n" "${0##*/}"
+  printf "\n"
+  printf "Initialize the container-side, repository-agnostic Dev Container baseline.\n"
+  printf "\n"
+  printf "Options:\n"
+  printf "  --devcontainer-id ID  Record and display the Dev Container identifier.\n"
+  printf "  --container-id ID     Override runtime container identity detection.\n"
+  printf "  --dry-run             Report actions without modifying files or running hooks.\n"
+  printf "  --force               Ignore a matching on-create marker and run again.\n"
+  printf "  --verbose             Print additional diagnostic information.\n"
+  printf "  --help                Show this help text.\n"
 }
 
 ##
@@ -303,55 +303,55 @@
 #   $@ - Script arguments.
 ##
 parse_arguments() {
-    while (($# > 0)); do
-        case "$1" in
-            --devcontainer-id)
-                (($# >= 2)) || usage_error "Option --devcontainer-id requires a value."
-                DEVCONTAINER_ID="$2"
-                shift
-                ;;
-            --devcontainer-id=*)
-                DEVCONTAINER_ID="${1#*=}"
-                ;;
-            --container-id)
-                (($# >= 2)) || usage_error "Option --container-id requires a value."
-                RUNTIME_CONTAINER_ID="$2"
-                shift
-                ;;
-            --container-id=*)
-                RUNTIME_CONTAINER_ID="${1#*=}"
-                ;;
-            --dry-run)
-                DRY_RUN=1
-                ;;
-            --force)
-                FORCE_ON_CREATE=1
-                ;;
-            --verbose)
-                VERBOSE=1
-                ;;
-            --help)
-                print_usage
-                exit 0
-                ;;
-            --)
-                shift
-                break
-                ;;
-            -*)
-                usage_error "Unknown option: $1"
-                ;;
-            *)
-                usage_error "Unexpected positional argument: $1"
-                ;;
-        esac
-
-        shift
-    done
-
-    if (($# > 0)); then
-        usage_error "Unexpected positional argument: $1"
-    fi
+  while (($# > 0)); do
+    case "$1" in
+    --devcontainer-id)
+      (($# >= 2)) || usage_error "Option --devcontainer-id requires a value."
+      DEVCONTAINER_ID="$2"
+      shift
+      ;;
+    --devcontainer-id=*)
+      DEVCONTAINER_ID="${1#*=}"
+      ;;
+    --container-id)
+      (($# >= 2)) || usage_error "Option --container-id requires a value."
+      RUNTIME_CONTAINER_ID="$2"
+      shift
+      ;;
+    --container-id=*)
+      RUNTIME_CONTAINER_ID="${1#*=}"
+      ;;
+    --dry-run)
+      DRY_RUN=1
+      ;;
+    --force)
+      FORCE_ON_CREATE=1
+      ;;
+    --verbose)
+      VERBOSE=1
+      ;;
+    --help)
+      print_usage
+      exit 0
+      ;;
+    --)
+      shift
+      break
+      ;;
+    -*)
+      usage_error "Unknown option: $1"
+      ;;
+    *)
+      usage_error "Unexpected positional argument: $1"
+      ;;
+    esac
+
+    shift
+  done
+
+  if (($# > 0)); then
+    usage_error "Unexpected positional argument: $1"
+  fi
 }
 
 ##
@@ -362,14 +362,14 @@
 #   $2 - Identifier value.
 ##
 validate_identifier() {
-    local label="${1:?Identifier label is required}"
-    local value="${2:-}"
-
-    case "$value" in
-        *$'\n'* | *$'\r'*)
-            usage_error "$label must not contain line breaks."
-            ;;
-    esac
+  local label="${1:?Identifier label is required}"
+  local value="${2:-}"
+
+  case "$value" in
+  *$'\n'* | *$'\r'*)
+    usage_error "$label must not contain line breaks."
+    ;;
+  esac
 }
 
 ##
@@ -376,12 +376,12 @@
 # Validates and normalizes the optional Dev Container identifier.
 ##
 normalize_devcontainer_id() {
-    if [[ $DEVCONTAINER_ID == "\${devcontainerId}" ]]; then
-        print_warning "The devcontainerId substitution was not resolved by this implementation."
-        DEVCONTAINER_ID=""
-    fi
-
-    validate_identifier "The Dev Container ID" "$DEVCONTAINER_ID"
+  if [[ $DEVCONTAINER_ID == "\${devcontainerId}" ]]; then
+    print_warning "The devcontainerId substitution was not resolved by this implementation."
+    DEVCONTAINER_ID=""
+  fi
+
+  validate_identifier "The Dev Container ID" "$DEVCONTAINER_ID"
 }
 
 # -----------------------------------------------------------------------------
@@ -398,12 +398,12 @@
 #   Writes the resolved path to stdout.
 ##
 resolve_directory() {
-    local directory="${1:?Directory path is required}"
-
-    (
-        cd -- "$directory"
-        pwd -P
-    )
+  local directory="${1:?Directory path is required}"
+
+  (
+    cd -- "$directory"
+    pwd -P
+  )
 }
 
 ##
@@ -410,16 +410,16 @@
 # Resolves the script directory and repository root.
 ##
 resolve_paths() {
-    SCRIPT_DIRECTORY="$(resolve_directory "$(dirname "${BASH_SOURCE[0]}")")"
-
-    if [[ -n ${DEVCONTAINER_REPO_ROOT:-} ]]; then
-        REPO_ROOT="$(resolve_directory "$DEVCONTAINER_REPO_ROOT")"
-    else
-        REPO_ROOT="$(resolve_directory "$SCRIPT_DIRECTORY/../..")"
-    fi
-
-    [[ -d $REPO_ROOT/$DEVCONTAINER_DIRECTORY ]] ||
-        print_warning "Expected Dev Container directory not found: $REPO_ROOT/$DEVCONTAINER_DIRECTORY"
+  SCRIPT_DIRECTORY="$(resolve_directory "$(dirname "${BASH_SOURCE[0]}")")"
+
+  if [[ -n ${DEVCONTAINER_REPO_ROOT:-} ]]; then
+    REPO_ROOT="$(resolve_directory "$DEVCONTAINER_REPO_ROOT")"
+  else
+    REPO_ROOT="$(resolve_directory "$SCRIPT_DIRECTORY/../..")"
+  fi
+
+  [[ -d $REPO_ROOT/$DEVCONTAINER_DIRECTORY ]] ||
+    print_warning "Expected Dev Container directory not found: $REPO_ROOT/$DEVCONTAINER_DIRECTORY"
 }
 
 ##
@@ -426,24 +426,24 @@
 # Resolves the runtime container identifier when one was not supplied.
 ##
 resolve_runtime_container_id() {
-    if [[ -n $RUNTIME_CONTAINER_ID ]]; then
-        validate_identifier "The runtime container ID" "$RUNTIME_CONTAINER_ID"
-        return 0
-    fi
-
-    if [[ -r /etc/hostname ]]; then
-        IFS= read -r RUNTIME_CONTAINER_ID < /etc/hostname || true
-    fi
-
-    if [[ -z $RUNTIME_CONTAINER_ID ]] && command -v hostname > /dev/null 2>&1; then
-        RUNTIME_CONTAINER_ID="$(hostname 2> /dev/null || true)"
-    fi
-
-    if [[ -z $RUNTIME_CONTAINER_ID && -n ${HOSTNAME:-} ]]; then
-        RUNTIME_CONTAINER_ID="$HOSTNAME"
-    fi
-
+  if [[ -n $RUNTIME_CONTAINER_ID ]]; then
     validate_identifier "The runtime container ID" "$RUNTIME_CONTAINER_ID"
+    return 0
+  fi
+
+  if [[ -r /etc/hostname ]]; then
+    IFS= read -r RUNTIME_CONTAINER_ID </etc/hostname || true
+  fi
+
+  if [[ -z $RUNTIME_CONTAINER_ID ]] && command -v hostname >/dev/null 2>&1; then
+    RUNTIME_CONTAINER_ID="$(hostname 2>/dev/null || true)"
+  fi
+
+  if [[ -z $RUNTIME_CONTAINER_ID && -n ${HOSTNAME:-} ]]; then
+    RUNTIME_CONTAINER_ID="$HOSTNAME"
+  fi
+
+  validate_identifier "The runtime container ID" "$RUNTIME_CONTAINER_ID"
 }
 
 ##
@@ -453,14 +453,14 @@
 #   $1 - Directory to add.
 ##
 prepend_path_once() {
-    local directory="${1:?Directory path is required}"
-
-    case ":${PATH:-}:" in
-        *":$directory:"*) ;;
-        *) PATH="$directory${PATH:+:$PATH}" ;;
-    esac
-
-    export PATH
+  local directory="${1:?Directory path is required}"
+
+  case ":${PATH:-}:" in
+  *":$directory:"*) ;;
+  *) PATH="$directory${PATH:+:$PATH}" ;;
+  esac
+
+  export PATH
 }
 
 ##
@@ -467,27 +467,27 @@
 # Configures the environment inherited by repository-specific hooks.
 ##
 configure_environment() {
-    : "${HOME:?HOME must be set for the Dev Container on-create lifecycle}"
-
-    export XDG_CACHE_HOME="${XDG_CACHE_HOME:-$HOME/.cache}"
-    export XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
-    export XDG_DATA_HOME="${XDG_DATA_HOME:-$HOME/.local/share}"
-    export XDG_STATE_HOME="${XDG_STATE_HOME:-$HOME/.local/state}"
-
-    ON_CREATE_STATE_DIRECTORY="$XDG_STATE_HOME/$ON_CREATE_STATE_DIRECTORY_NAME"
-    ON_CREATE_MARKER_FILE="$ON_CREATE_STATE_DIRECTORY/$ON_CREATE_MARKER_NAME"
-
-    export DEVCONTAINER_ID
-    export DEVCONTAINER_RUNTIME_ID="$RUNTIME_CONTAINER_ID"
-    export DEVCONTAINER_LIFECYCLE="on-create"
-    export DEVCONTAINER_REPO_ROOT="$REPO_ROOT"
-    export DEVCONTAINER_SCRIPT_DIRECTORY="$SCRIPT_DIRECTORY"
-
-    prepend_path_once "$HOME/.local/bin"
-    prepend_path_once "$HOME/bin"
-
-    print_debug "Platform: $(uname -s 2> /dev/null || printf "unknown")/$(uname -m 2> /dev/null || printf "unknown")"
-    print_debug "PATH: $PATH"
+  : "${HOME:?HOME must be set for the Dev Container on-create lifecycle}"
+
+  export XDG_CACHE_HOME="${XDG_CACHE_HOME:-$HOME/.cache}"
+  export XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
+  export XDG_DATA_HOME="${XDG_DATA_HOME:-$HOME/.local/share}"
+  export XDG_STATE_HOME="${XDG_STATE_HOME:-$HOME/.local/state}"
+
+  ON_CREATE_STATE_DIRECTORY="$XDG_STATE_HOME/$ON_CREATE_STATE_DIRECTORY_NAME"
+  ON_CREATE_MARKER_FILE="$ON_CREATE_STATE_DIRECTORY/$ON_CREATE_MARKER_NAME"
+
+  export DEVCONTAINER_ID
+  export DEVCONTAINER_RUNTIME_ID="$RUNTIME_CONTAINER_ID"
+  export DEVCONTAINER_LIFECYCLE="on-create"
+  export DEVCONTAINER_REPO_ROOT="$REPO_ROOT"
+  export DEVCONTAINER_SCRIPT_DIRECTORY="$SCRIPT_DIRECTORY"
+
+  prepend_path_once "$HOME/.local/bin"
+  prepend_path_once "$HOME/bin"
+
+  print_debug "Platform: $(uname -s 2>/dev/null || printf "unknown")/$(uname -m 2>/dev/null || printf "unknown")"
+  print_debug "PATH: $PATH"
 }
 
 # -----------------------------------------------------------------------------
@@ -498,17 +498,17 @@
 # Validates basic workspace and home-directory expectations.
 ##
 validate_environment() {
-    [[ -d $REPO_ROOT ]] || die "Repository root does not exist: $REPO_ROOT"
-    [[ -r $REPO_ROOT ]] || die "Repository root is not readable: $REPO_ROOT"
-    [[ -d $HOME ]] || die "Home directory does not exist: $HOME"
-
-    if [[ ! -w $REPO_ROOT ]]; then
-        print_warning "Repository root is not writable: $REPO_ROOT"
-    fi
-
-    if [[ ! -w $HOME ]]; then
-        print_warning "Home directory is not writable: $HOME"
-    fi
+  [[ -d $REPO_ROOT ]] || die "Repository root does not exist: $REPO_ROOT"
+  [[ -r $REPO_ROOT ]] || die "Repository root is not readable: $REPO_ROOT"
+  [[ -d $HOME ]] || die "Home directory does not exist: $HOME"
+
+  if [[ ! -w $REPO_ROOT ]]; then
+    print_warning "Repository root is not writable: $REPO_ROOT"
+  fi
+
+  if [[ ! -w $HOME ]]; then
+    print_warning "Home directory is not writable: $HOME"
+  fi
 }
 
 ##
@@ -519,25 +519,25 @@
 #   $2 - Optional directory mode. Defaults to 0755.
 ##
 ensure_directory() {
-    local directory="${1:?Directory path is required}"
-    local mode="${2:-0755}"
-
-    if [[ -d $directory ]]; then
-        print_debug "Directory already exists: $directory"
-        return 0
-    fi
-
-    if ((DRY_RUN == 1)); then
-        print_status "Would create directory: $directory"
-        return 0
-    fi
-
-    print_status "Creating directory: $directory"
-    mkdir -p "$directory"
-
-    if ! chmod "$mode" "$directory" 2> /dev/null; then
-        print_warning "Could not set mode $mode on directory: $directory"
-    fi
+  local directory="${1:?Directory path is required}"
+  local mode="${2:-0755}"
+
+  if [[ -d $directory ]]; then
+    print_debug "Directory already exists: $directory"
+    return 0
+  fi
+
+  if ((DRY_RUN == 1)); then
+    print_status "Would create directory: $directory"
+    return 0
+  fi
+
+  print_status "Creating directory: $directory"
+  mkdir -p "$directory"
+
+  if ! chmod "$mode" "$directory" 2>/dev/null; then
+    print_warning "Could not set mode $mode on directory: $directory"
+  fi
 }
 
 ##
@@ -544,14 +544,14 @@
 # Creates baseline user-local and repository-local directories.
 ##
 create_required_directories() {
-    ensure_directory "$HOME/.local/bin"
-    ensure_directory "$XDG_CACHE_HOME"
-    ensure_directory "$XDG_CONFIG_HOME"
-    ensure_directory "$XDG_DATA_HOME"
-    ensure_directory "$XDG_STATE_HOME"
-    ensure_directory "$ON_CREATE_STATE_DIRECTORY" 0700
-    ensure_directory "$REPO_ROOT/.cache" 0775
-    ensure_directory "$REPO_ROOT/.tmp" 0775
+  ensure_directory "$HOME/.local/bin"
+  ensure_directory "$XDG_CACHE_HOME"
+  ensure_directory "$XDG_CONFIG_HOME"
+  ensure_directory "$XDG_DATA_HOME"
+  ensure_directory "$XDG_STATE_HOME"
+  ensure_directory "$ON_CREATE_STATE_DIRECTORY" 0700
+  ensure_directory "$REPO_ROOT/.cache" 0775
+  ensure_directory "$REPO_ROOT/.tmp" 0775
 }
 
 ##
@@ -558,29 +558,29 @@
 # Reports the effective user and container identifiers.
 ##
 report_runtime_identity() {
-    local user_name
-    local user_id
-    local group_name
-    local group_id
-
-    user_name="$(id -un 2> /dev/null || printf "unknown")"
-    user_id="$(id -u 2> /dev/null || printf "unknown")"
-    group_name="$(id -gn 2> /dev/null || printf "unknown")"
-    group_id="$(id -g 2> /dev/null || printf "unknown")"
-
-    print_status "Container user: $user_name (uid=$user_id, gid=$group_id, group=$group_name)"
-
-    if [[ -n $DEVCONTAINER_ID ]]; then
-        print_status "Dev Container ID: $DEVCONTAINER_ID"
-    else
-        print_status "Dev Container ID: unavailable"
-    fi
-
-    if [[ -n $RUNTIME_CONTAINER_ID ]]; then
-        print_status "Runtime container ID: $RUNTIME_CONTAINER_ID"
-    else
-        print_status "Runtime container ID: unavailable"
-    fi
+  local user_name
+  local user_id
+  local group_name
+  local group_id
+
+  user_name="$(id -un 2>/dev/null || printf "unknown")"
+  user_id="$(id -u 2>/dev/null || printf "unknown")"
+  group_name="$(id -gn 2>/dev/null || printf "unknown")"
+  group_id="$(id -g 2>/dev/null || printf "unknown")"
+
+  print_status "Container user: $user_name (uid=$user_id, gid=$group_id, group=$group_name)"
+
+  if [[ -n $DEVCONTAINER_ID ]]; then
+    print_status "Dev Container ID: $DEVCONTAINER_ID"
+  else
+    print_status "Dev Container ID: unavailable"
+  fi
+
+  if [[ -n $RUNTIME_CONTAINER_ID ]]; then
+    print_status "Runtime container ID: $RUNTIME_CONTAINER_ID"
+  else
+    print_status "Runtime container ID: unavailable"
+  fi
 }
 
 ##
@@ -590,22 +590,22 @@
 #   0 when a matching marker exists; otherwise 1.
 ##
 is_already_created() {
-    [[ -f $ON_CREATE_MARKER_FILE ]] || return 1
-
-    grep -Fqx "schema_version=$ON_CREATE_SCHEMA_VERSION" \
-        "$ON_CREATE_MARKER_FILE" || return 1
-
-    if [[ -n $DEVCONTAINER_ID ]]; then
-        grep -Fqx "devcontainer_id=$DEVCONTAINER_ID" \
-            "$ON_CREATE_MARKER_FILE" || return 1
-    fi
-
-    if [[ -n $RUNTIME_CONTAINER_ID ]]; then
-        grep -Fqx "runtime_container_id=$RUNTIME_CONTAINER_ID" \
-            "$ON_CREATE_MARKER_FILE" || return 1
-    fi
-
-    return 0
+  [[ -f $ON_CREATE_MARKER_FILE ]] || return 1
+
+  grep -Fqx "schema_version=$ON_CREATE_SCHEMA_VERSION" \
+    "$ON_CREATE_MARKER_FILE" || return 1
+
+  if [[ -n $DEVCONTAINER_ID ]]; then
+    grep -Fqx "devcontainer_id=$DEVCONTAINER_ID" \
+      "$ON_CREATE_MARKER_FILE" || return 1
+  fi
+
+  if [[ -n $RUNTIME_CONTAINER_ID ]]; then
+    grep -Fqx "runtime_container_id=$RUNTIME_CONTAINER_ID" \
+      "$ON_CREATE_MARKER_FILE" || return 1
+  fi
+
+  return 0
 }
 
 ##
@@ -612,29 +612,29 @@
 # Atomically writes the on-create completion marker.
 ##
 write_on_create_marker() {
-    local completed_at
-
-    if ((DRY_RUN == 1)); then
-        print_status "Would write on-create marker: $ON_CREATE_MARKER_FILE"
-        return 0
-    fi
-
-    completed_at="$(date -u "+%Y-%m-%dT%H:%M:%SZ")"
-    TEMPORARY_FILE="$(mktemp "${ON_CREATE_MARKER_FILE}.tmp.XXXXXX")"
-
-    {
-        printf "schema_version=%s\n" "$ON_CREATE_SCHEMA_VERSION"
-        printf "completed_at=%s\n" "$completed_at"
-        printf "devcontainer_id=%s\n" "$DEVCONTAINER_ID"
-        printf "runtime_container_id=%s\n" "$RUNTIME_CONTAINER_ID"
-        printf "repository_root=%s\n" "$REPO_ROOT"
-    } > "$TEMPORARY_FILE"
-
-    chmod 0600 "$TEMPORARY_FILE"
-    mv "$TEMPORARY_FILE" "$ON_CREATE_MARKER_FILE"
-    TEMPORARY_FILE=""
-
-    print_status "Wrote on-create marker: $ON_CREATE_MARKER_FILE"
+  local completed_at
+
+  if ((DRY_RUN == 1)); then
+    print_status "Would write on-create marker: $ON_CREATE_MARKER_FILE"
+    return 0
+  fi
+
+  completed_at="$(date -u "+%Y-%m-%dT%H:%M:%SZ")"
+  TEMPORARY_FILE="$(mktemp "${ON_CREATE_MARKER_FILE}.tmp.XXXXXX")"
+
+  {
+    printf "schema_version=%s\n" "$ON_CREATE_SCHEMA_VERSION"
+    printf "completed_at=%s\n" "$completed_at"
+    printf "devcontainer_id=%s\n" "$DEVCONTAINER_ID"
+    printf "runtime_container_id=%s\n" "$RUNTIME_CONTAINER_ID"
+    printf "repository_root=%s\n" "$REPO_ROOT"
+  } >"$TEMPORARY_FILE"
+
+  chmod 0600 "$TEMPORARY_FILE"
+  mv "$TEMPORARY_FILE" "$ON_CREATE_MARKER_FILE"
+  TEMPORARY_FILE=""
+
+  print_status "Wrote on-create marker: $ON_CREATE_MARKER_FILE"
 }
 
 # -----------------------------------------------------------------------------
@@ -648,26 +648,26 @@
 #   $1 - Hook path.
 ##
 run_hook() {
-    local hook="${1:?Hook path is required}"
-    local relative_hook="${hook#"$REPO_ROOT"/}"
-
-    if ((DRY_RUN == 1)); then
-        print_status "Would run hook: $relative_hook"
-        return 0
-    fi
-
-    print_status "Running hook: $relative_hook"
-
-    if bash "$hook"; then
-        return 0
-    fi
-
-    if is_enabled "${DEVCONTAINER_ON_CREATE_CONTINUE_ON_HOOK_FAILURE:-0}"; then
-        print_warning "Hook failed; continuing: $relative_hook"
-        return 0
-    fi
-
-    die "Hook failed: $relative_hook"
+  local hook="${1:?Hook path is required}"
+  local relative_hook="${hook#"$REPO_ROOT"/}"
+
+  if ((DRY_RUN == 1)); then
+    print_status "Would run hook: $relative_hook"
+    return 0
+  fi
+
+  print_status "Running hook: $relative_hook"
+
+  if bash "$hook"; then
+    return 0
+  fi
+
+  if is_enabled "${DEVCONTAINER_ON_CREATE_CONTINUE_ON_HOOK_FAILURE:-0}"; then
+    print_warning "Hook failed; continuing: $relative_hook"
+    return 0
+  fi
+
+  die "Hook failed: $relative_hook"
 }
 
 ##
@@ -674,41 +674,41 @@
 # Discovers and executes on-create hooks in deterministic filename order.
 ##
 run_extension_hooks() {
-    local hooks_directory="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/on-create.d"
-    local local_hook="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/on-create.local.sh"
-    local hook
-    local nullglob_was_enabled=0
-    local previous_lc_all="${LC_ALL-}"
-    local -a hooks=()
-
-    if [[ -f $local_hook ]]; then
-        run_hook "$local_hook"
-    fi
-
-    [[ -d $hooks_directory ]] || return 0
-
-    if shopt -q nullglob; then
-        nullglob_was_enabled=1
-    fi
-
-    LC_ALL=C
-    shopt -s nullglob
-    hooks=("$hooks_directory"/*.sh)
-
-    if ((nullglob_was_enabled == 0)); then
-        shopt -u nullglob
-    fi
-
-    if [[ -n $previous_lc_all ]]; then
-        LC_ALL="$previous_lc_all"
-    else
-        unset LC_ALL
-    fi
-
-    for hook in "${hooks[@]}"; do
-        [[ -f $hook ]] || continue
-        run_hook "$hook"
-    done
+  local hooks_directory="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/on-create.d"
+  local local_hook="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/on-create.local.sh"
+  local hook
+  local nullglob_was_enabled=0
+  local previous_lc_all="${LC_ALL-}"
+  local -a hooks=()
+
+  if [[ -f $local_hook ]]; then
+    run_hook "$local_hook"
+  fi
+
+  [[ -d $hooks_directory ]] || return 0
+
+  if shopt -q nullglob; then
+    nullglob_was_enabled=1
+  fi
+
+  LC_ALL=C
+  shopt -s nullglob
+  hooks=("$hooks_directory"/*.sh)
+
+  if ((nullglob_was_enabled == 0)); then
+    shopt -u nullglob
+  fi
+
+  if [[ -n $previous_lc_all ]]; then
+    LC_ALL="$previous_lc_all"
+  else
+    unset LC_ALL
+  fi
+
+  for hook in "${hooks[@]}"; do
+    [[ -f $hook ]] || continue
+    run_hook "$hook"
+  done
 }
 
 ##
@@ -721,13 +721,13 @@
 #   Writes the joined labels to stdout.
 ##
 join_labels() {
-    local label
-    local separator=""
-
-    for label in "$@"; do
-        printf "%s%s" "$separator" "$label"
-        separator=", "
-    done
+  local label
+  local separator=""
+
+  for label in "$@"; do
+    printf "%s%s" "$separator" "$label"
+    separator=", "
+  done
 }
 
 ##
@@ -734,28 +734,28 @@
 # Reports recognized project manifests without installing dependencies.
 ##
 report_detected_tooling() {
-    local -a detected=()
-
-    [[ -f $REPO_ROOT/package.json ]] && detected+=("Node.js")
-    [[ -f $REPO_ROOT/pyproject.toml || -f $REPO_ROOT/requirements.txt || -f $REPO_ROOT/setup.py ]] &&
-        detected+=("Python")
-    [[ -f $REPO_ROOT/Cargo.toml ]] && detected+=("Rust")
-    [[ -f $REPO_ROOT/go.mod ]] && detected+=("Go")
-    [[ -f $REPO_ROOT/pom.xml || -f $REPO_ROOT/build.gradle || -f $REPO_ROOT/build.gradle.kts ]] &&
-        detected+=("Java/JVM")
-    [[ -f $REPO_ROOT/Gemfile ]] && detected+=("Ruby")
-    [[ -f $REPO_ROOT/composer.json ]] && detected+=("PHP")
-    [[ -f $REPO_ROOT/compose.yaml || -f $REPO_ROOT/compose.yml || -f $REPO_ROOT/docker-compose.yaml || -f $REPO_ROOT/docker-compose.yml ]] &&
-        detected+=("Docker Compose")
-    [[ -f $REPO_ROOT/Taskfile.yml || -f $REPO_ROOT/Taskfile.yaml ]] && detected+=("Task")
-    [[ -f $REPO_ROOT/Makefile ]] && detected+=("Make")
-
-    if ((${#detected[@]} == 0)); then
-        print_debug "No common project manifests detected."
-        return 0
-    fi
-
-    print_status "Detected project tooling: $(join_labels "${detected[@]}")"
+  local -a detected=()
+
+  [[ -f $REPO_ROOT/package.json ]] && detected+=("Node.js")
+  [[ -f $REPO_ROOT/pyproject.toml || -f $REPO_ROOT/requirements.txt || -f $REPO_ROOT/setup.py ]] &&
+    detected+=("Python")
+  [[ -f $REPO_ROOT/Cargo.toml ]] && detected+=("Rust")
+  [[ -f $REPO_ROOT/go.mod ]] && detected+=("Go")
+  [[ -f $REPO_ROOT/pom.xml || -f $REPO_ROOT/build.gradle || -f $REPO_ROOT/build.gradle.kts ]] &&
+    detected+=("Java/JVM")
+  [[ -f $REPO_ROOT/Gemfile ]] && detected+=("Ruby")
+  [[ -f $REPO_ROOT/composer.json ]] && detected+=("PHP")
+  [[ -f $REPO_ROOT/compose.yaml || -f $REPO_ROOT/compose.yml || -f $REPO_ROOT/docker-compose.yaml || -f $REPO_ROOT/docker-compose.yml ]] &&
+    detected+=("Docker Compose")
+  [[ -f $REPO_ROOT/Taskfile.yml || -f $REPO_ROOT/Taskfile.yaml ]] && detected+=("Task")
+  [[ -f $REPO_ROOT/Makefile ]] && detected+=("Make")
+
+  if ((${#detected[@]} == 0)); then
+    print_debug "No common project manifests detected."
+    return 0
+  fi
+
+  print_status "Detected project tooling: $(join_labels "${detected[@]}")"
 }
 
 # -----------------------------------------------------------------------------
@@ -769,44 +769,44 @@
 #   $@ - Script arguments.
 ##
 main() {
-    parse_arguments "$@"
-    normalize_devcontainer_id
-    install_traps
-
-    if is_enabled "${DEVCONTAINER_ON_CREATE_VERBOSE:-0}"; then
-        VERBOSE=1
-    fi
-
-    if is_enabled "${DEVCONTAINER_ON_CREATE_FORCE:-0}"; then
-        FORCE_ON_CREATE=1
-    fi
-
-    if is_enabled "${DEVCONTAINER_ON_CREATE_TRACE:-0}"; then
-        set -o xtrace
-    fi
-
-    resolve_paths
-    resolve_runtime_container_id
-    cd -- "$REPO_ROOT"
-    configure_environment
-
-    print_status "Preparing newly created development container."
-    print_status "Repository root: $REPO_ROOT"
-
-    validate_environment
-    report_runtime_identity
-    create_required_directories
-    report_detected_tooling
-
-    if ((FORCE_ON_CREATE == 0)) && is_already_created; then
-        print_status "Development container on-create state is already complete."
-        return 0
-    fi
-
-    run_extension_hooks
-    write_on_create_marker
-
-    print_status "Development container on-create lifecycle complete."
+  parse_arguments "$@"
+  normalize_devcontainer_id
+  install_traps
+
+  if is_enabled "${DEVCONTAINER_ON_CREATE_VERBOSE:-0}"; then
+    VERBOSE=1
+  fi
+
+  if is_enabled "${DEVCONTAINER_ON_CREATE_FORCE:-0}"; then
+    FORCE_ON_CREATE=1
+  fi
+
+  if is_enabled "${DEVCONTAINER_ON_CREATE_TRACE:-0}"; then
+    set -o xtrace
+  fi
+
+  resolve_paths
+  resolve_runtime_container_id
+  cd -- "$REPO_ROOT"
+  configure_environment
+
+  print_status "Preparing newly created development container."
+  print_status "Repository root: $REPO_ROOT"
+
+  validate_environment
+  report_runtime_identity
+  create_required_directories
+  report_detected_tooling
+
+  if ((FORCE_ON_CREATE == 0)) && is_already_created; then
+    print_status "Development container on-create state is already complete."
+    return 0
+  fi
+
+  run_extension_hooks
+  write_on_create_marker
+
+  print_status "Development container on-create lifecycle complete."
 }
 
 main "$@"
diff .devcontainer/scripts/post-attach.sh.orig .devcontainer/scripts/post-attach.sh
--- .devcontainer/scripts/post-attach.sh.orig
+++ .devcontainer/scripts/post-attach.sh
@@ -98,7 +98,7 @@
 #   Writes the formatted message to stdout.
 ##
 print_status() {
-    printf "%s %s\n" "$STATUS_PREFIX" "$*"
+  printf "%s %s\n" "$STATUS_PREFIX" "$*"
 }
 
 ##
@@ -111,9 +111,9 @@
 #   Writes the formatted message to stderr when enabled.
 ##
 print_debug() {
-    if ((VERBOSE == 1)); then
-        printf "%s %s\n" "$DEBUG_PREFIX" "$*" >&2
-    fi
+  if ((VERBOSE == 1)); then
+    printf "%s %s\n" "$DEBUG_PREFIX" "$*" >&2
+  fi
 }
 
 ##
@@ -126,7 +126,7 @@
 #   Writes the formatted message to stderr.
 ##
 print_warning() {
-    printf "%s %s\n" "$WARNING_PREFIX" "$*" >&2
+  printf "%s %s\n" "$WARNING_PREFIX" "$*" >&2
 }
 
 ##
@@ -139,9 +139,9 @@
 #   Does not return.
 ##
 die() {
-    ERROR_REPORTED=1
-    printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
-    exit 1
+  ERROR_REPORTED=1
+  printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
+  exit 1
 }
 
 ##
@@ -154,10 +154,10 @@
 #   Does not return.
 ##
 usage_error() {
-    ERROR_REPORTED=1
-    printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
-    printf "%s Try --help for usage information.\n" "$ERROR_PREFIX" >&2
-    exit 2
+  ERROR_REPORTED=1
+  printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
+  printf "%s Try --help for usage information.\n" "$ERROR_PREFIX" >&2
+  exit 2
 }
 
 ##
@@ -170,14 +170,14 @@
 #   0 when enabled; otherwise 1.
 ##
 is_enabled() {
-    case "${1:-0}" in
-        1 | true | TRUE | yes | YES | on | ON)
-            return 0
-            ;;
-        *)
-            return 1
-            ;;
-    esac
+  case "${1:-0}" in
+  1 | true | TRUE | yes | YES | on | ON)
+    return 0
+    ;;
+  *)
+    return 1
+    ;;
+  esac
 }
 
 ##
@@ -192,20 +192,20 @@
 #   The original command exit code.
 ##
 on_error() {
-    local exit_code="${1:?Exit code is required}"
-    local line_number="${2:?Line number is required}"
-    local command="${3:-unknown command}"
-
-    trap - ERR
-    ERROR_REPORTED=1
-
-    printf "%s Command failed with exit code %d at line %d: %s\n" \
-        "$ERROR_PREFIX" \
-        "$exit_code" \
-        "$line_number" \
-        "$command" >&2
-
-    return "$exit_code"
+  local exit_code="${1:?Exit code is required}"
+  local line_number="${2:?Line number is required}"
+  local command="${3:-unknown command}"
+
+  trap - ERR
+  ERROR_REPORTED=1
+
+  printf "%s Command failed with exit code %d at line %d: %s\n" \
+    "$ERROR_PREFIX" \
+    "$exit_code" \
+    "$line_number" \
+    "$command" >&2
+
+  return "$exit_code"
 }
 
 ##
@@ -218,15 +218,15 @@
 #   Does not return.
 ##
 on_exit() {
-    local exit_code="${1:?Exit code is required}"
-
-    trap - EXIT
-
-    if ((exit_code != 0 && ERROR_REPORTED == 0)); then
-        print_warning "Post-attach lifecycle stopped with exit code $exit_code."
-    fi
-
-    exit "$exit_code"
+  local exit_code="${1:?Exit code is required}"
+
+  trap - EXIT
+
+  if ((exit_code != 0 && ERROR_REPORTED == 0)); then
+    print_warning "Post-attach lifecycle stopped with exit code $exit_code."
+  fi
+
+  exit "$exit_code"
 }
 
 ##
@@ -240,12 +240,12 @@
 #   Does not return.
 ##
 on_signal() {
-    local signal_name="${1:?Signal name is required}"
-    local exit_code="${2:?Exit code is required}"
-
-    ERROR_REPORTED=1
-    print_warning "Received $signal_name; stopping the post-attach lifecycle."
-    exit "$exit_code"
+  local signal_name="${1:?Signal name is required}"
+  local exit_code="${2:?Exit code is required}"
+
+  ERROR_REPORTED=1
+  print_warning "Received $signal_name; stopping the post-attach lifecycle."
+  exit "$exit_code"
 }
 
 ##
@@ -252,10 +252,10 @@
 # Installs error, exit, and signal handlers.
 ##
 install_traps() {
-    trap 'on_error "$?" "$LINENO" "$BASH_COMMAND"' ERR
-    trap 'on_exit "$?"' EXIT
-    trap 'on_signal "SIGINT" 130' INT
-    trap 'on_signal "SIGTERM" 143' TERM
+  trap 'on_error "$?" "$LINENO" "$BASH_COMMAND"' ERR
+  trap 'on_exit "$?"' EXIT
+  trap 'on_signal "SIGINT" 130' INT
+  trap 'on_signal "SIGTERM" 143' TERM
 }
 
 # -----------------------------------------------------------------------------
@@ -266,18 +266,18 @@
 # Prints command usage to stdout.
 ##
 print_usage() {
-    printf "Usage: %s [OPTIONS]\n" "${0##*/}"
-    printf "\n"
-    printf "Run repository-agnostic Dev Container post-attach orchestration.\n"
-    printf "\n"
-    printf "Options:\n"
-    printf "  --devcontainer-id ID  Display the Dev Container identifier.\n"
-    printf "  --container-id ID     Override runtime container identity detection.\n"
-    printf "  --git-status          Display the concise Git working-tree status.\n"
-    printf "  --no-git-status       Skip the concise Git working-tree status.\n"
-    printf "  --dry-run             Report hooks without executing them.\n"
-    printf "  --verbose             Print additional diagnostic information.\n"
-    printf "  --help                Show this help text.\n"
+  printf "Usage: %s [OPTIONS]\n" "${0##*/}"
+  printf "\n"
+  printf "Run repository-agnostic Dev Container post-attach orchestration.\n"
+  printf "\n"
+  printf "Options:\n"
+  printf "  --devcontainer-id ID  Display the Dev Container identifier.\n"
+  printf "  --container-id ID     Override runtime container identity detection.\n"
+  printf "  --git-status          Display the concise Git working-tree status.\n"
+  printf "  --no-git-status       Skip the concise Git working-tree status.\n"
+  printf "  --dry-run             Report hooks without executing them.\n"
+  printf "  --verbose             Print additional diagnostic information.\n"
+  printf "  --help                Show this help text.\n"
 }
 
 ##
@@ -287,60 +287,60 @@
 #   $@ - Script arguments.
 ##
 parse_arguments() {
-    while (($# > 0)); do
-        case "$1" in
-            --devcontainer-id)
-                (($# >= 2)) || usage_error "Option --devcontainer-id requires a value."
-                DEVCONTAINER_ID="$2"
-                shift
-                ;;
-            --devcontainer-id=*)
-                DEVCONTAINER_ID="${1#*=}"
-                ;;
-            --container-id)
-                (($# >= 2)) || usage_error "Option --container-id requires a value."
-                RUNTIME_CONTAINER_ID="$2"
-                shift
-                ;;
-            --container-id=*)
-                RUNTIME_CONTAINER_ID="${1#*=}"
-                ;;
-            --git-status)
-                SHOW_GIT_STATUS=1
-                GIT_STATUS_OPTION_SET=1
-                ;;
-            --no-git-status)
-                SHOW_GIT_STATUS=0
-                GIT_STATUS_OPTION_SET=1
-                ;;
-            --dry-run)
-                DRY_RUN=1
-                ;;
-            --verbose)
-                VERBOSE=1
-                ;;
-            --help)
-                print_usage
-                exit 0
-                ;;
-            --)
-                shift
-                break
-                ;;
-            -*)
-                usage_error "Unknown option: $1"
-                ;;
-            *)
-                usage_error "Unexpected positional argument: $1"
-                ;;
-        esac
-
-        shift
-    done
-
-    if (($# > 0)); then
-        usage_error "Unexpected positional argument: $1"
-    fi
+  while (($# > 0)); do
+    case "$1" in
+    --devcontainer-id)
+      (($# >= 2)) || usage_error "Option --devcontainer-id requires a value."
+      DEVCONTAINER_ID="$2"
+      shift
+      ;;
+    --devcontainer-id=*)
+      DEVCONTAINER_ID="${1#*=}"
+      ;;
+    --container-id)
+      (($# >= 2)) || usage_error "Option --container-id requires a value."
+      RUNTIME_CONTAINER_ID="$2"
+      shift
+      ;;
+    --container-id=*)
+      RUNTIME_CONTAINER_ID="${1#*=}"
+      ;;
+    --git-status)
+      SHOW_GIT_STATUS=1
+      GIT_STATUS_OPTION_SET=1
+      ;;
+    --no-git-status)
+      SHOW_GIT_STATUS=0
+      GIT_STATUS_OPTION_SET=1
+      ;;
+    --dry-run)
+      DRY_RUN=1
+      ;;
+    --verbose)
+      VERBOSE=1
+      ;;
+    --help)
+      print_usage
+      exit 0
+      ;;
+    --)
+      shift
+      break
+      ;;
+    -*)
+      usage_error "Unknown option: $1"
+      ;;
+    *)
+      usage_error "Unexpected positional argument: $1"
+      ;;
+    esac
+
+    shift
+  done
+
+  if (($# > 0)); then
+    usage_error "Unexpected positional argument: $1"
+  fi
 }
 
 ##
@@ -347,14 +347,14 @@
 # Applies environment-controlled defaults not explicitly selected by the CLI.
 ##
 configure_options() {
-    if ((GIT_STATUS_OPTION_SET == 0)) &&
-        ! is_enabled "${DEVCONTAINER_POST_ATTACH_SHOW_GIT_STATUS:-1}"; then
-        SHOW_GIT_STATUS=0
-    fi
-
-    if is_enabled "${DEVCONTAINER_POST_ATTACH_VERBOSE:-0}"; then
-        VERBOSE=1
-    fi
+  if ((GIT_STATUS_OPTION_SET == 0)) &&
+    ! is_enabled "${DEVCONTAINER_POST_ATTACH_SHOW_GIT_STATUS:-1}"; then
+    SHOW_GIT_STATUS=0
+  fi
+
+  if is_enabled "${DEVCONTAINER_POST_ATTACH_VERBOSE:-0}"; then
+    VERBOSE=1
+  fi
 }
 
 ##
@@ -365,14 +365,14 @@
 #   $2 - Identifier value.
 ##
 validate_identifier() {
-    local label="${1:?Identifier label is required}"
-    local value="${2:-}"
-
-    case "$value" in
-        *$'\n'* | *$'\r'*)
-            usage_error "$label must not contain line breaks."
-            ;;
-    esac
+  local label="${1:?Identifier label is required}"
+  local value="${2:-}"
+
+  case "$value" in
+  *$'\n'* | *$'\r'*)
+    usage_error "$label must not contain line breaks."
+    ;;
+  esac
 }
 
 ##
@@ -379,12 +379,12 @@
 # Validates and normalizes the optional Dev Container identifier.
 ##
 normalize_devcontainer_id() {
-    if [[ $DEVCONTAINER_ID == "\${devcontainerId}" ]]; then
-        print_warning "The devcontainerId substitution was not resolved by this implementation."
-        DEVCONTAINER_ID=""
-    fi
-
-    validate_identifier "The Dev Container ID" "$DEVCONTAINER_ID"
+  if [[ $DEVCONTAINER_ID == "\${devcontainerId}" ]]; then
+    print_warning "The devcontainerId substitution was not resolved by this implementation."
+    DEVCONTAINER_ID=""
+  fi
+
+  validate_identifier "The Dev Container ID" "$DEVCONTAINER_ID"
 }
 
 # -----------------------------------------------------------------------------
@@ -401,12 +401,12 @@
 #   Writes the resolved path to stdout.
 ##
 resolve_directory() {
-    local directory="${1:?Directory path is required}"
-
-    (
-        cd -- "$directory"
-        pwd -P
-    )
+  local directory="${1:?Directory path is required}"
+
+  (
+    cd -- "$directory"
+    pwd -P
+  )
 }
 
 ##
@@ -413,16 +413,16 @@
 # Resolves the script directory and repository root.
 ##
 resolve_paths() {
-    SCRIPT_DIRECTORY="$(resolve_directory "$(dirname "${BASH_SOURCE[0]}")")"
-
-    if [[ -n ${DEVCONTAINER_REPO_ROOT:-} ]]; then
-        REPO_ROOT="$(resolve_directory "$DEVCONTAINER_REPO_ROOT")"
-    else
-        REPO_ROOT="$(resolve_directory "$SCRIPT_DIRECTORY/../..")"
-    fi
-
-    [[ -d $REPO_ROOT/$DEVCONTAINER_DIRECTORY ]] ||
-        print_warning "Expected Dev Container directory not found: $REPO_ROOT/$DEVCONTAINER_DIRECTORY"
+  SCRIPT_DIRECTORY="$(resolve_directory "$(dirname "${BASH_SOURCE[0]}")")"
+
+  if [[ -n ${DEVCONTAINER_REPO_ROOT:-} ]]; then
+    REPO_ROOT="$(resolve_directory "$DEVCONTAINER_REPO_ROOT")"
+  else
+    REPO_ROOT="$(resolve_directory "$SCRIPT_DIRECTORY/../..")"
+  fi
+
+  [[ -d $REPO_ROOT/$DEVCONTAINER_DIRECTORY ]] ||
+    print_warning "Expected Dev Container directory not found: $REPO_ROOT/$DEVCONTAINER_DIRECTORY"
 }
 
 ##
@@ -429,24 +429,24 @@
 # Resolves the runtime container identifier when one was not supplied.
 ##
 resolve_runtime_container_id() {
-    if [[ -n $RUNTIME_CONTAINER_ID ]]; then
-        validate_identifier "The runtime container ID" "$RUNTIME_CONTAINER_ID"
-        return 0
-    fi
-
-    if [[ -r /etc/hostname ]]; then
-        IFS= read -r RUNTIME_CONTAINER_ID < /etc/hostname || true
-    fi
-
-    if [[ -z $RUNTIME_CONTAINER_ID ]] && command -v hostname > /dev/null 2>&1; then
-        RUNTIME_CONTAINER_ID="$(hostname 2> /dev/null || true)"
-    fi
-
-    if [[ -z $RUNTIME_CONTAINER_ID && -n ${HOSTNAME:-} ]]; then
-        RUNTIME_CONTAINER_ID="$HOSTNAME"
-    fi
-
+  if [[ -n $RUNTIME_CONTAINER_ID ]]; then
     validate_identifier "The runtime container ID" "$RUNTIME_CONTAINER_ID"
+    return 0
+  fi
+
+  if [[ -r /etc/hostname ]]; then
+    IFS= read -r RUNTIME_CONTAINER_ID </etc/hostname || true
+  fi
+
+  if [[ -z $RUNTIME_CONTAINER_ID ]] && command -v hostname >/dev/null 2>&1; then
+    RUNTIME_CONTAINER_ID="$(hostname 2>/dev/null || true)"
+  fi
+
+  if [[ -z $RUNTIME_CONTAINER_ID && -n ${HOSTNAME:-} ]]; then
+    RUNTIME_CONTAINER_ID="$HOSTNAME"
+  fi
+
+  validate_identifier "The runtime container ID" "$RUNTIME_CONTAINER_ID"
 }
 
 ##
@@ -456,14 +456,14 @@
 #   $1 - Directory to add.
 ##
 prepend_path_once() {
-    local directory="${1:?Directory path is required}"
-
-    case ":${PATH:-}:" in
-        *":$directory:"*) ;;
-        *) PATH="$directory${PATH:+:$PATH}" ;;
-    esac
-
-    export PATH
+  local directory="${1:?Directory path is required}"
+
+  case ":${PATH:-}:" in
+  *":$directory:"*) ;;
+  *) PATH="$directory${PATH:+:$PATH}" ;;
+  esac
+
+  export PATH
 }
 
 ##
@@ -470,24 +470,24 @@
 # Configures the environment inherited by repository-specific hooks.
 ##
 configure_environment() {
-    : "${HOME:?HOME must be set for the Dev Container post-attach lifecycle}"
-
-    export XDG_CACHE_HOME="${XDG_CACHE_HOME:-$HOME/.cache}"
-    export XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
-    export XDG_DATA_HOME="${XDG_DATA_HOME:-$HOME/.local/share}"
-    export XDG_STATE_HOME="${XDG_STATE_HOME:-$HOME/.local/state}"
-
-    export DEVCONTAINER_ID
-    export DEVCONTAINER_RUNTIME_ID="$RUNTIME_CONTAINER_ID"
-    export DEVCONTAINER_LIFECYCLE="post-attach"
-    export DEVCONTAINER_REPO_ROOT="$REPO_ROOT"
-    export DEVCONTAINER_SCRIPT_DIRECTORY="$SCRIPT_DIRECTORY"
-
-    prepend_path_once "$HOME/.local/bin"
-    prepend_path_once "$HOME/bin"
-
-    print_debug "Platform: $(uname -s 2> /dev/null || printf "unknown")/$(uname -m 2> /dev/null || printf "unknown")"
-    print_debug "PATH: $PATH"
+  : "${HOME:?HOME must be set for the Dev Container post-attach lifecycle}"
+
+  export XDG_CACHE_HOME="${XDG_CACHE_HOME:-$HOME/.cache}"
+  export XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
+  export XDG_DATA_HOME="${XDG_DATA_HOME:-$HOME/.local/share}"
+  export XDG_STATE_HOME="${XDG_STATE_HOME:-$HOME/.local/state}"
+
+  export DEVCONTAINER_ID
+  export DEVCONTAINER_RUNTIME_ID="$RUNTIME_CONTAINER_ID"
+  export DEVCONTAINER_LIFECYCLE="post-attach"
+  export DEVCONTAINER_REPO_ROOT="$REPO_ROOT"
+  export DEVCONTAINER_SCRIPT_DIRECTORY="$SCRIPT_DIRECTORY"
+
+  prepend_path_once "$HOME/.local/bin"
+  prepend_path_once "$HOME/bin"
+
+  print_debug "Platform: $(uname -s 2>/dev/null || printf "unknown")/$(uname -m 2>/dev/null || printf "unknown")"
+  print_debug "PATH: $PATH"
 }
 
 # -----------------------------------------------------------------------------
@@ -498,9 +498,9 @@
 # Validates basic workspace and home-directory expectations.
 ##
 validate_environment() {
-    [[ -d $REPO_ROOT ]] || die "Repository root does not exist: $REPO_ROOT"
-    [[ -r $REPO_ROOT ]] || die "Repository root is not readable: $REPO_ROOT"
-    [[ -d $HOME ]] || die "Home directory does not exist: $HOME"
+  [[ -d $REPO_ROOT ]] || die "Repository root does not exist: $REPO_ROOT"
+  [[ -r $REPO_ROOT ]] || die "Repository root is not readable: $REPO_ROOT"
+  [[ -d $HOME ]] || die "Home directory does not exist: $HOME"
 }
 
 ##
@@ -507,25 +507,25 @@
 # Reports the effective user and container identifiers.
 ##
 report_runtime_identity() {
-    local user_name
-    local user_id
-
-    user_name="$(id -un 2> /dev/null || printf "unknown")"
-    user_id="$(id -u 2> /dev/null || printf "unknown")"
-
-    print_status "Attached as $user_name (uid=$user_id)."
-
-    if [[ -n $DEVCONTAINER_ID ]]; then
-        print_status "Dev Container ID: $DEVCONTAINER_ID"
-    else
-        print_status "Dev Container ID: unavailable"
-    fi
-
-    if [[ -n $RUNTIME_CONTAINER_ID ]]; then
-        print_debug "Runtime container ID: $RUNTIME_CONTAINER_ID"
-    else
-        print_debug "Runtime container ID: unavailable"
-    fi
+  local user_name
+  local user_id
+
+  user_name="$(id -un 2>/dev/null || printf "unknown")"
+  user_id="$(id -u 2>/dev/null || printf "unknown")"
+
+  print_status "Attached as $user_name (uid=$user_id)."
+
+  if [[ -n $DEVCONTAINER_ID ]]; then
+    print_status "Dev Container ID: $DEVCONTAINER_ID"
+  else
+    print_status "Dev Container ID: unavailable"
+  fi
+
+  if [[ -n $RUNTIME_CONTAINER_ID ]]; then
+    print_debug "Runtime container ID: $RUNTIME_CONTAINER_ID"
+  else
+    print_debug "Runtime container ID: unavailable"
+  fi
 }
 
 ##
@@ -532,43 +532,43 @@
 # Displays the current branch and concise Git working-tree status.
 ##
 report_git_status() {
-    local branch=""
-    local status_output=""
-    local status_line
-
-    ((SHOW_GIT_STATUS == 1)) || return 0
-
-    if ! command -v git > /dev/null 2>&1; then
-        print_warning "Git is unavailable; skipping repository status."
-        return 0
-    fi
-
-    if [[ $(git rev-parse --is-inside-work-tree 2> /dev/null || true) != "true" ]]; then
-        print_debug "Repository root is not inside a Git working tree."
-        return 0
-    fi
-
-    branch="$(git symbolic-ref --quiet --short HEAD 2> /dev/null || true)"
-    if [[ -z $branch ]]; then
-        branch="detached at $(git rev-parse --short HEAD 2> /dev/null || printf "unknown")"
-    fi
-
-    print_status "Git branch: $branch"
-
-    if ! status_output="$(GIT_OPTIONAL_LOCKS=0 git status --short 2> /dev/null)"; then
-        print_warning "Unable to read the Git working-tree status."
-        return 0
-    fi
-
-    if [[ -z $status_output ]]; then
-        print_status "Git status: clean"
-        return 0
-    fi
-
-    print_status "Git status:"
-    while IFS= read -r status_line; do
-        printf "%s   %s\n" "$STATUS_PREFIX" "$status_line"
-    done <<< "$status_output"
+  local branch=""
+  local status_output=""
+  local status_line
+
+  ((SHOW_GIT_STATUS == 1)) || return 0
+
+  if ! command -v git >/dev/null 2>&1; then
+    print_warning "Git is unavailable; skipping repository status."
+    return 0
+  fi
+
+  if [[ $(git rev-parse --is-inside-work-tree 2>/dev/null || true) != "true" ]]; then
+    print_debug "Repository root is not inside a Git working tree."
+    return 0
+  fi
+
+  branch="$(git symbolic-ref --quiet --short HEAD 2>/dev/null || true)"
+  if [[ -z $branch ]]; then
+    branch="detached at $(git rev-parse --short HEAD 2>/dev/null || printf "unknown")"
+  fi
+
+  print_status "Git branch: $branch"
+
+  if ! status_output="$(GIT_OPTIONAL_LOCKS=0 git status --short 2>/dev/null)"; then
+    print_warning "Unable to read the Git working-tree status."
+    return 0
+  fi
+
+  if [[ -z $status_output ]]; then
+    print_status "Git status: clean"
+    return 0
+  fi
+
+  print_status "Git status:"
+  while IFS= read -r status_line; do
+    printf "%s   %s\n" "$STATUS_PREFIX" "$status_line"
+  done <<<"$status_output"
 }
 
 # -----------------------------------------------------------------------------
@@ -582,26 +582,26 @@
 #   $1 - Hook path.
 ##
 run_hook() {
-    local hook="${1:?Hook path is required}"
-    local relative_hook="${hook#"$REPO_ROOT"/}"
-
-    if ((DRY_RUN == 1)); then
-        print_status "Would run hook: $relative_hook"
-        return 0
-    fi
-
-    print_status "Running hook: $relative_hook"
-
-    if bash "$hook"; then
-        return 0
-    fi
-
-    if is_enabled "${DEVCONTAINER_POST_ATTACH_CONTINUE_ON_HOOK_FAILURE:-0}"; then
-        print_warning "Hook failed; continuing: $relative_hook"
-        return 0
-    fi
-
-    die "Hook failed: $relative_hook"
+  local hook="${1:?Hook path is required}"
+  local relative_hook="${hook#"$REPO_ROOT"/}"
+
+  if ((DRY_RUN == 1)); then
+    print_status "Would run hook: $relative_hook"
+    return 0
+  fi
+
+  print_status "Running hook: $relative_hook"
+
+  if bash "$hook"; then
+    return 0
+  fi
+
+  if is_enabled "${DEVCONTAINER_POST_ATTACH_CONTINUE_ON_HOOK_FAILURE:-0}"; then
+    print_warning "Hook failed; continuing: $relative_hook"
+    return 0
+  fi
+
+  die "Hook failed: $relative_hook"
 }
 
 ##
@@ -608,41 +608,41 @@
 # Discovers and executes post-attach hooks in deterministic filename order.
 ##
 run_extension_hooks() {
-    local hooks_directory="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/post-attach.d"
-    local local_hook="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/post-attach.local.sh"
-    local hook
-    local nullglob_was_enabled=0
-    local previous_lc_all="${LC_ALL-}"
-    local -a hooks=()
-
-    if [[ -f $local_hook ]]; then
-        run_hook "$local_hook"
-    fi
-
-    [[ -d $hooks_directory ]] || return 0
-
-    if shopt -q nullglob; then
-        nullglob_was_enabled=1
-    fi
-
-    LC_ALL=C
-    shopt -s nullglob
-    hooks=("$hooks_directory"/*.sh)
-
-    if ((nullglob_was_enabled == 0)); then
-        shopt -u nullglob
-    fi
-
-    if [[ -n $previous_lc_all ]]; then
-        LC_ALL="$previous_lc_all"
-    else
-        unset LC_ALL
-    fi
-
-    for hook in "${hooks[@]}"; do
-        [[ -f $hook ]] || continue
-        run_hook "$hook"
-    done
+  local hooks_directory="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/post-attach.d"
+  local local_hook="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/post-attach.local.sh"
+  local hook
+  local nullglob_was_enabled=0
+  local previous_lc_all="${LC_ALL-}"
+  local -a hooks=()
+
+  if [[ -f $local_hook ]]; then
+    run_hook "$local_hook"
+  fi
+
+  [[ -d $hooks_directory ]] || return 0
+
+  if shopt -q nullglob; then
+    nullglob_was_enabled=1
+  fi
+
+  LC_ALL=C
+  shopt -s nullglob
+  hooks=("$hooks_directory"/*.sh)
+
+  if ((nullglob_was_enabled == 0)); then
+    shopt -u nullglob
+  fi
+
+  if [[ -n $previous_lc_all ]]; then
+    LC_ALL="$previous_lc_all"
+  else
+    unset LC_ALL
+  fi
+
+  for hook in "${hooks[@]}"; do
+    [[ -f $hook ]] || continue
+    run_hook "$hook"
+  done
 }
 
 # -----------------------------------------------------------------------------
@@ -656,32 +656,32 @@
 #   $@ - Script arguments.
 ##
 main() {
-    parse_arguments "$@"
-    configure_options
-    normalize_devcontainer_id
-    install_traps
-
-    if is_enabled "${DEVCONTAINER_POST_ATTACH_TRACE:-0}"; then
-        set -o xtrace
-    fi
-
-    resolve_paths
-    resolve_runtime_container_id
-    cd -- "$REPO_ROOT"
-    configure_environment
-
-    print_status "Preparing the attached development session."
-
-    validate_environment
-    report_runtime_identity
-    report_git_status
-    run_extension_hooks
-
-    if ((DRY_RUN == 1)); then
-        print_status "Post-attach dry run complete."
-    else
-        print_status "Development session attachment complete."
-    fi
+  parse_arguments "$@"
+  configure_options
+  normalize_devcontainer_id
+  install_traps
+
+  if is_enabled "${DEVCONTAINER_POST_ATTACH_TRACE:-0}"; then
+    set -o xtrace
+  fi
+
+  resolve_paths
+  resolve_runtime_container_id
+  cd -- "$REPO_ROOT"
+  configure_environment
+
+  print_status "Preparing the attached development session."
+
+  validate_environment
+  report_runtime_identity
+  report_git_status
+  run_extension_hooks
+
+  if ((DRY_RUN == 1)); then
+    print_status "Post-attach dry run complete."
+  else
+    print_status "Development session attachment complete."
+  fi
 }
 
 main "$@"
diff .devcontainer/scripts/post-create.sh.orig .devcontainer/scripts/post-create.sh
--- .devcontainer/scripts/post-create.sh.orig
+++ .devcontainer/scripts/post-create.sh
@@ -106,7 +106,7 @@
 #   Writes the formatted message to stdout.
 ##
 print_status() {
-    printf "%s %s\n" "$STATUS_PREFIX" "$*"
+  printf "%s %s\n" "$STATUS_PREFIX" "$*"
 }
 
 ##
@@ -119,9 +119,9 @@
 #   Writes the formatted message to stderr when enabled.
 ##
 print_debug() {
-    if ((VERBOSE == 1)); then
-        printf "%s %s\n" "$DEBUG_PREFIX" "$*" >&2
-    fi
+  if ((VERBOSE == 1)); then
+    printf "%s %s\n" "$DEBUG_PREFIX" "$*" >&2
+  fi
 }
 
 ##
@@ -134,7 +134,7 @@
 #   Writes the formatted message to stderr.
 ##
 print_warning() {
-    printf "%s %s\n" "$WARNING_PREFIX" "$*" >&2
+  printf "%s %s\n" "$WARNING_PREFIX" "$*" >&2
 }
 
 ##
@@ -147,9 +147,9 @@
 #   Does not return.
 ##
 die() {
-    ERROR_REPORTED=1
-    printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
-    exit 1
+  ERROR_REPORTED=1
+  printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
+  exit 1
 }
 
 ##
@@ -162,10 +162,10 @@
 #   Does not return.
 ##
 usage_error() {
-    ERROR_REPORTED=1
-    printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
-    printf "%s Try --help for usage information.\n" "$ERROR_PREFIX" >&2
-    exit 2
+  ERROR_REPORTED=1
+  printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
+  printf "%s Try --help for usage information.\n" "$ERROR_PREFIX" >&2
+  exit 2
 }
 
 ##
@@ -178,14 +178,14 @@
 #   0 when enabled; otherwise 1.
 ##
 is_enabled() {
-    case "${1:-0}" in
-        1 | true | TRUE | yes | YES | on | ON)
-            return 0
-            ;;
-        *)
-            return 1
-            ;;
-    esac
+  case "${1:-0}" in
+  1 | true | TRUE | yes | YES | on | ON)
+    return 0
+    ;;
+  *)
+    return 1
+    ;;
+  esac
 }
 
 ##
@@ -195,11 +195,11 @@
 #   TEMPORARY_FILE
 ##
 cleanup() {
-    if [[ -n $TEMPORARY_FILE && -f $TEMPORARY_FILE ]]; then
-        rm -f "$TEMPORARY_FILE"
-    fi
-
-    TEMPORARY_FILE=""
+  if [[ -n $TEMPORARY_FILE && -f $TEMPORARY_FILE ]]; then
+    rm -f "$TEMPORARY_FILE"
+  fi
+
+  TEMPORARY_FILE=""
 }
 
 ##
@@ -214,20 +214,20 @@
 #   The original command exit code.
 ##
 on_error() {
-    local exit_code="${1:?Exit code is required}"
-    local line_number="${2:?Line number is required}"
-    local command="${3:-unknown command}"
-
-    trap - ERR
-    ERROR_REPORTED=1
-
-    printf "%s Command failed with exit code %d at line %d: %s\n" \
-        "$ERROR_PREFIX" \
-        "$exit_code" \
-        "$line_number" \
-        "$command" >&2
-
-    return "$exit_code"
+  local exit_code="${1:?Exit code is required}"
+  local line_number="${2:?Line number is required}"
+  local command="${3:-unknown command}"
+
+  trap - ERR
+  ERROR_REPORTED=1
+
+  printf "%s Command failed with exit code %d at line %d: %s\n" \
+    "$ERROR_PREFIX" \
+    "$exit_code" \
+    "$line_number" \
+    "$command" >&2
+
+  return "$exit_code"
 }
 
 ##
@@ -240,16 +240,16 @@
 #   Does not return.
 ##
 on_exit() {
-    local exit_code="${1:?Exit code is required}"
-
-    trap - EXIT
-    cleanup
-
-    if ((exit_code != 0 && ERROR_REPORTED == 0)); then
-        print_warning "Post-create lifecycle stopped with exit code $exit_code."
-    fi
-
-    exit "$exit_code"
+  local exit_code="${1:?Exit code is required}"
+
+  trap - EXIT
+  cleanup
+
+  if ((exit_code != 0 && ERROR_REPORTED == 0)); then
+    print_warning "Post-create lifecycle stopped with exit code $exit_code."
+  fi
+
+  exit "$exit_code"
 }
 
 ##
@@ -263,12 +263,12 @@
 #   Does not return.
 ##
 on_signal() {
-    local signal_name="${1:?Signal name is required}"
-    local exit_code="${2:?Exit code is required}"
-
-    ERROR_REPORTED=1
-    print_warning "Received $signal_name; stopping the post-create lifecycle."
-    exit "$exit_code"
+  local signal_name="${1:?Signal name is required}"
+  local exit_code="${2:?Exit code is required}"
+
+  ERROR_REPORTED=1
+  print_warning "Received $signal_name; stopping the post-create lifecycle."
+  exit "$exit_code"
 }
 
 ##
@@ -275,10 +275,10 @@
 # Installs error, exit, and signal handlers.
 ##
 install_traps() {
-    trap 'on_error "$?" "$LINENO" "$BASH_COMMAND"' ERR
-    trap 'on_exit "$?"' EXIT
-    trap 'on_signal "SIGINT" 130' INT
-    trap 'on_signal "SIGTERM" 143' TERM
+  trap 'on_error "$?" "$LINENO" "$BASH_COMMAND"' ERR
+  trap 'on_exit "$?"' EXIT
+  trap 'on_signal "SIGINT" 130' INT
+  trap 'on_signal "SIGTERM" 143' TERM
 }
 
 # -----------------------------------------------------------------------------
@@ -289,17 +289,17 @@
 # Prints command usage to stdout.
 ##
 print_usage() {
-    printf "Usage: %s [OPTIONS]\n" "${0##*/}"
-    printf "\n"
-    printf "Run repository-agnostic Dev Container post-create orchestration.\n"
-    printf "\n"
-    printf "Options:\n"
-    printf "  --devcontainer-id ID  Record and display the Dev Container identifier.\n"
-    printf "  --container-id ID     Override runtime container identity detection.\n"
-    printf "  --dry-run             Report actions without modifying files or running hooks.\n"
-    printf "  --force               Ignore a matching post-create marker and run again.\n"
-    printf "  --verbose             Print additional diagnostic information.\n"
-    printf "  --help                Show this help text.\n"
+  printf "Usage: %s [OPTIONS]\n" "${0##*/}"
+  printf "\n"
+  printf "Run repository-agnostic Dev Container post-create orchestration.\n"
+  printf "\n"
+  printf "Options:\n"
+  printf "  --devcontainer-id ID  Record and display the Dev Container identifier.\n"
+  printf "  --container-id ID     Override runtime container identity detection.\n"
+  printf "  --dry-run             Report actions without modifying files or running hooks.\n"
+  printf "  --force               Ignore a matching post-create marker and run again.\n"
+  printf "  --verbose             Print additional diagnostic information.\n"
+  printf "  --help                Show this help text.\n"
 }
 
 ##
@@ -309,55 +309,55 @@
 #   $@ - Script arguments.
 ##
 parse_arguments() {
-    while (($# > 0)); do
-        case "$1" in
-            --devcontainer-id)
-                (($# >= 2)) || usage_error "Option --devcontainer-id requires a value."
-                DEVCONTAINER_ID="$2"
-                shift
-                ;;
-            --devcontainer-id=*)
-                DEVCONTAINER_ID="${1#*=}"
-                ;;
-            --container-id)
-                (($# >= 2)) || usage_error "Option --container-id requires a value."
-                RUNTIME_CONTAINER_ID="$2"
-                shift
-                ;;
-            --container-id=*)
-                RUNTIME_CONTAINER_ID="${1#*=}"
-                ;;
-            --dry-run)
-                DRY_RUN=1
-                ;;
-            --force)
-                FORCE_POST_CREATE=1
-                ;;
-            --verbose)
-                VERBOSE=1
-                ;;
-            --help)
-                print_usage
-                exit 0
-                ;;
-            --)
-                shift
-                break
-                ;;
-            -*)
-                usage_error "Unknown option: $1"
-                ;;
-            *)
-                usage_error "Unexpected positional argument: $1"
-                ;;
-        esac
-
-        shift
-    done
-
-    if (($# > 0)); then
-        usage_error "Unexpected positional argument: $1"
-    fi
+  while (($# > 0)); do
+    case "$1" in
+    --devcontainer-id)
+      (($# >= 2)) || usage_error "Option --devcontainer-id requires a value."
+      DEVCONTAINER_ID="$2"
+      shift
+      ;;
+    --devcontainer-id=*)
+      DEVCONTAINER_ID="${1#*=}"
+      ;;
+    --container-id)
+      (($# >= 2)) || usage_error "Option --container-id requires a value."
+      RUNTIME_CONTAINER_ID="$2"
+      shift
+      ;;
+    --container-id=*)
+      RUNTIME_CONTAINER_ID="${1#*=}"
+      ;;
+    --dry-run)
+      DRY_RUN=1
+      ;;
+    --force)
+      FORCE_POST_CREATE=1
+      ;;
+    --verbose)
+      VERBOSE=1
+      ;;
+    --help)
+      print_usage
+      exit 0
+      ;;
+    --)
+      shift
+      break
+      ;;
+    -*)
+      usage_error "Unknown option: $1"
+      ;;
+    *)
+      usage_error "Unexpected positional argument: $1"
+      ;;
+    esac
+
+    shift
+  done
+
+  if (($# > 0)); then
+    usage_error "Unexpected positional argument: $1"
+  fi
 }
 
 ##
@@ -368,14 +368,14 @@
 #   $2 - Identifier value.
 ##
 validate_identifier() {
-    local label="${1:?Identifier label is required}"
-    local value="${2:-}"
-
-    case "$value" in
-        *$'\n'* | *$'\r'*)
-            usage_error "$label must not contain line breaks."
-            ;;
-    esac
+  local label="${1:?Identifier label is required}"
+  local value="${2:-}"
+
+  case "$value" in
+  *$'\n'* | *$'\r'*)
+    usage_error "$label must not contain line breaks."
+    ;;
+  esac
 }
 
 ##
@@ -382,12 +382,12 @@
 # Validates and normalizes the optional Dev Container identifier.
 ##
 normalize_devcontainer_id() {
-    if [[ $DEVCONTAINER_ID == "\${devcontainerId}" ]]; then
-        print_warning "The devcontainerId substitution was not resolved by this implementation."
-        DEVCONTAINER_ID=""
-    fi
-
-    validate_identifier "The Dev Container ID" "$DEVCONTAINER_ID"
+  if [[ $DEVCONTAINER_ID == "\${devcontainerId}" ]]; then
+    print_warning "The devcontainerId substitution was not resolved by this implementation."
+    DEVCONTAINER_ID=""
+  fi
+
+  validate_identifier "The Dev Container ID" "$DEVCONTAINER_ID"
 }
 
 # -----------------------------------------------------------------------------
@@ -404,12 +404,12 @@
 #   Writes the resolved path to stdout.
 ##
 resolve_directory() {
-    local directory="${1:?Directory path is required}"
-
-    (
-        cd -- "$directory"
-        pwd -P
-    )
+  local directory="${1:?Directory path is required}"
+
+  (
+    cd -- "$directory"
+    pwd -P
+  )
 }
 
 ##
@@ -416,16 +416,16 @@
 # Resolves the script directory and repository root.
 ##
 resolve_paths() {
-    SCRIPT_DIRECTORY="$(resolve_directory "$(dirname "${BASH_SOURCE[0]}")")"
-
-    if [[ -n ${DEVCONTAINER_REPO_ROOT:-} ]]; then
-        REPO_ROOT="$(resolve_directory "$DEVCONTAINER_REPO_ROOT")"
-    else
-        REPO_ROOT="$(resolve_directory "$SCRIPT_DIRECTORY/../..")"
-    fi
-
-    [[ -d $REPO_ROOT/$DEVCONTAINER_DIRECTORY ]] ||
-        print_warning "Expected Dev Container directory not found: $REPO_ROOT/$DEVCONTAINER_DIRECTORY"
+  SCRIPT_DIRECTORY="$(resolve_directory "$(dirname "${BASH_SOURCE[0]}")")"
+
+  if [[ -n ${DEVCONTAINER_REPO_ROOT:-} ]]; then
+    REPO_ROOT="$(resolve_directory "$DEVCONTAINER_REPO_ROOT")"
+  else
+    REPO_ROOT="$(resolve_directory "$SCRIPT_DIRECTORY/../..")"
+  fi
+
+  [[ -d $REPO_ROOT/$DEVCONTAINER_DIRECTORY ]] ||
+    print_warning "Expected Dev Container directory not found: $REPO_ROOT/$DEVCONTAINER_DIRECTORY"
 }
 
 ##
@@ -432,24 +432,24 @@
 # Resolves the runtime container identifier when one was not supplied.
 ##
 resolve_runtime_container_id() {
-    if [[ -n $RUNTIME_CONTAINER_ID ]]; then
-        validate_identifier "The runtime container ID" "$RUNTIME_CONTAINER_ID"
-        return 0
-    fi
-
-    if [[ -r /etc/hostname ]]; then
-        IFS= read -r RUNTIME_CONTAINER_ID < /etc/hostname || true
-    fi
-
-    if [[ -z $RUNTIME_CONTAINER_ID ]] && command -v hostname > /dev/null 2>&1; then
-        RUNTIME_CONTAINER_ID="$(hostname 2> /dev/null || true)"
-    fi
-
-    if [[ -z $RUNTIME_CONTAINER_ID && -n ${HOSTNAME:-} ]]; then
-        RUNTIME_CONTAINER_ID="$HOSTNAME"
-    fi
-
+  if [[ -n $RUNTIME_CONTAINER_ID ]]; then
     validate_identifier "The runtime container ID" "$RUNTIME_CONTAINER_ID"
+    return 0
+  fi
+
+  if [[ -r /etc/hostname ]]; then
+    IFS= read -r RUNTIME_CONTAINER_ID </etc/hostname || true
+  fi
+
+  if [[ -z $RUNTIME_CONTAINER_ID ]] && command -v hostname >/dev/null 2>&1; then
+    RUNTIME_CONTAINER_ID="$(hostname 2>/dev/null || true)"
+  fi
+
+  if [[ -z $RUNTIME_CONTAINER_ID && -n ${HOSTNAME:-} ]]; then
+    RUNTIME_CONTAINER_ID="$HOSTNAME"
+  fi
+
+  validate_identifier "The runtime container ID" "$RUNTIME_CONTAINER_ID"
 }
 
 ##
@@ -459,14 +459,14 @@
 #   $1 - Directory to add.
 ##
 prepend_path_once() {
-    local directory="${1:?Directory path is required}"
-
-    case ":${PATH:-}:" in
-        *":$directory:"*) ;;
-        *) PATH="$directory${PATH:+:$PATH}" ;;
-    esac
-
-    export PATH
+  local directory="${1:?Directory path is required}"
+
+  case ":${PATH:-}:" in
+  *":$directory:"*) ;;
+  *) PATH="$directory${PATH:+:$PATH}" ;;
+  esac
+
+  export PATH
 }
 
 ##
@@ -473,27 +473,27 @@
 # Configures the environment inherited by repository-specific hooks.
 ##
 configure_environment() {
-    : "${HOME:?HOME must be set for the Dev Container post-create lifecycle}"
-
-    export XDG_CACHE_HOME="${XDG_CACHE_HOME:-$HOME/.cache}"
-    export XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
-    export XDG_DATA_HOME="${XDG_DATA_HOME:-$HOME/.local/share}"
-    export XDG_STATE_HOME="${XDG_STATE_HOME:-$HOME/.local/state}"
-
-    POST_CREATE_STATE_DIRECTORY="$XDG_STATE_HOME/$POST_CREATE_STATE_DIRECTORY_NAME"
-    POST_CREATE_MARKER_FILE="$POST_CREATE_STATE_DIRECTORY/$POST_CREATE_MARKER_NAME"
-
-    export DEVCONTAINER_ID
-    export DEVCONTAINER_RUNTIME_ID="$RUNTIME_CONTAINER_ID"
-    export DEVCONTAINER_LIFECYCLE="post-create"
-    export DEVCONTAINER_REPO_ROOT="$REPO_ROOT"
-    export DEVCONTAINER_SCRIPT_DIRECTORY="$SCRIPT_DIRECTORY"
-
-    prepend_path_once "$HOME/.local/bin"
-    prepend_path_once "$HOME/bin"
-
-    print_debug "Platform: $(uname -s 2> /dev/null || printf "unknown")/$(uname -m 2> /dev/null || printf "unknown")"
-    print_debug "PATH: $PATH"
+  : "${HOME:?HOME must be set for the Dev Container post-create lifecycle}"
+
+  export XDG_CACHE_HOME="${XDG_CACHE_HOME:-$HOME/.cache}"
+  export XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
+  export XDG_DATA_HOME="${XDG_DATA_HOME:-$HOME/.local/share}"
+  export XDG_STATE_HOME="${XDG_STATE_HOME:-$HOME/.local/state}"
+
+  POST_CREATE_STATE_DIRECTORY="$XDG_STATE_HOME/$POST_CREATE_STATE_DIRECTORY_NAME"
+  POST_CREATE_MARKER_FILE="$POST_CREATE_STATE_DIRECTORY/$POST_CREATE_MARKER_NAME"
+
+  export DEVCONTAINER_ID
+  export DEVCONTAINER_RUNTIME_ID="$RUNTIME_CONTAINER_ID"
+  export DEVCONTAINER_LIFECYCLE="post-create"
+  export DEVCONTAINER_REPO_ROOT="$REPO_ROOT"
+  export DEVCONTAINER_SCRIPT_DIRECTORY="$SCRIPT_DIRECTORY"
+
+  prepend_path_once "$HOME/.local/bin"
+  prepend_path_once "$HOME/bin"
+
+  print_debug "Platform: $(uname -s 2>/dev/null || printf "unknown")/$(uname -m 2>/dev/null || printf "unknown")"
+  print_debug "PATH: $PATH"
 }
 
 # -----------------------------------------------------------------------------
@@ -504,17 +504,17 @@
 # Validates basic workspace and home-directory expectations.
 ##
 validate_environment() {
-    [[ -d $REPO_ROOT ]] || die "Repository root does not exist: $REPO_ROOT"
-    [[ -r $REPO_ROOT ]] || die "Repository root is not readable: $REPO_ROOT"
-    [[ -d $HOME ]] || die "Home directory does not exist: $HOME"
-
-    if [[ ! -w $REPO_ROOT ]]; then
-        print_warning "Repository root is not writable: $REPO_ROOT"
-    fi
-
-    if [[ ! -w $HOME ]]; then
-        print_warning "Home directory is not writable: $HOME"
-    fi
+  [[ -d $REPO_ROOT ]] || die "Repository root does not exist: $REPO_ROOT"
+  [[ -r $REPO_ROOT ]] || die "Repository root is not readable: $REPO_ROOT"
+  [[ -d $HOME ]] || die "Home directory does not exist: $HOME"
+
+  if [[ ! -w $REPO_ROOT ]]; then
+    print_warning "Repository root is not writable: $REPO_ROOT"
+  fi
+
+  if [[ ! -w $HOME ]]; then
+    print_warning "Home directory is not writable: $HOME"
+  fi
 }
 
 ##
@@ -525,25 +525,25 @@
 #   $2 - Optional directory mode. Defaults to 0755.
 ##
 ensure_directory() {
-    local directory="${1:?Directory path is required}"
-    local mode="${2:-0755}"
-
-    if [[ -d $directory ]]; then
-        print_debug "Directory already exists: $directory"
-        return 0
-    fi
-
-    if ((DRY_RUN == 1)); then
-        print_status "Would create directory: $directory"
-        return 0
-    fi
-
-    print_status "Creating directory: $directory"
-    mkdir -p "$directory"
-
-    if ! chmod "$mode" "$directory" 2> /dev/null; then
-        print_warning "Could not set mode $mode on directory: $directory"
-    fi
+  local directory="${1:?Directory path is required}"
+  local mode="${2:-0755}"
+
+  if [[ -d $directory ]]; then
+    print_debug "Directory already exists: $directory"
+    return 0
+  fi
+
+  if ((DRY_RUN == 1)); then
+    print_status "Would create directory: $directory"
+    return 0
+  fi
+
+  print_status "Creating directory: $directory"
+  mkdir -p "$directory"
+
+  if ! chmod "$mode" "$directory" 2>/dev/null; then
+    print_warning "Could not set mode $mode on directory: $directory"
+  fi
 }
 
 ##
@@ -550,14 +550,14 @@
 # Creates baseline user-local and repository-local directories.
 ##
 create_required_directories() {
-    ensure_directory "$HOME/.local/bin"
-    ensure_directory "$XDG_CACHE_HOME"
-    ensure_directory "$XDG_CONFIG_HOME"
-    ensure_directory "$XDG_DATA_HOME"
-    ensure_directory "$XDG_STATE_HOME"
-    ensure_directory "$POST_CREATE_STATE_DIRECTORY" 0700
-    ensure_directory "$REPO_ROOT/.cache" 0775
-    ensure_directory "$REPO_ROOT/.tmp" 0775
+  ensure_directory "$HOME/.local/bin"
+  ensure_directory "$XDG_CACHE_HOME"
+  ensure_directory "$XDG_CONFIG_HOME"
+  ensure_directory "$XDG_DATA_HOME"
+  ensure_directory "$XDG_STATE_HOME"
+  ensure_directory "$POST_CREATE_STATE_DIRECTORY" 0700
+  ensure_directory "$REPO_ROOT/.cache" 0775
+  ensure_directory "$REPO_ROOT/.tmp" 0775
 }
 
 ##
@@ -564,29 +564,29 @@
 # Reports the effective user and container identifiers.
 ##
 report_runtime_identity() {
-    local user_name
-    local user_id
-    local group_name
-    local group_id
-
-    user_name="$(id -un 2> /dev/null || printf "unknown")"
-    user_id="$(id -u 2> /dev/null || printf "unknown")"
-    group_name="$(id -gn 2> /dev/null || printf "unknown")"
-    group_id="$(id -g 2> /dev/null || printf "unknown")"
-
-    print_status "Container user: $user_name (uid=$user_id, gid=$group_id, group=$group_name)"
-
-    if [[ -n $DEVCONTAINER_ID ]]; then
-        print_status "Dev Container ID: $DEVCONTAINER_ID"
-    else
-        print_status "Dev Container ID: unavailable"
-    fi
-
-    if [[ -n $RUNTIME_CONTAINER_ID ]]; then
-        print_status "Runtime container ID: $RUNTIME_CONTAINER_ID"
-    else
-        print_status "Runtime container ID: unavailable"
-    fi
+  local user_name
+  local user_id
+  local group_name
+  local group_id
+
+  user_name="$(id -un 2>/dev/null || printf "unknown")"
+  user_id="$(id -u 2>/dev/null || printf "unknown")"
+  group_name="$(id -gn 2>/dev/null || printf "unknown")"
+  group_id="$(id -g 2>/dev/null || printf "unknown")"
+
+  print_status "Container user: $user_name (uid=$user_id, gid=$group_id, group=$group_name)"
+
+  if [[ -n $DEVCONTAINER_ID ]]; then
+    print_status "Dev Container ID: $DEVCONTAINER_ID"
+  else
+    print_status "Dev Container ID: unavailable"
+  fi
+
+  if [[ -n $RUNTIME_CONTAINER_ID ]]; then
+    print_status "Runtime container ID: $RUNTIME_CONTAINER_ID"
+  else
+    print_status "Runtime container ID: unavailable"
+  fi
 }
 
 ##
@@ -596,22 +596,22 @@
 #   0 when a matching marker exists; otherwise 1.
 ##
 is_already_completed() {
-    [[ -f $POST_CREATE_MARKER_FILE ]] || return 1
-
-    grep -Fqx "schema_version=$POST_CREATE_SCHEMA_VERSION" \
-        "$POST_CREATE_MARKER_FILE" || return 1
-
-    if [[ -n $DEVCONTAINER_ID ]]; then
-        grep -Fqx "devcontainer_id=$DEVCONTAINER_ID" \
-            "$POST_CREATE_MARKER_FILE" || return 1
-    fi
-
-    if [[ -n $RUNTIME_CONTAINER_ID ]]; then
-        grep -Fqx "runtime_container_id=$RUNTIME_CONTAINER_ID" \
-            "$POST_CREATE_MARKER_FILE" || return 1
-    fi
-
-    return 0
+  [[ -f $POST_CREATE_MARKER_FILE ]] || return 1
+
+  grep -Fqx "schema_version=$POST_CREATE_SCHEMA_VERSION" \
+    "$POST_CREATE_MARKER_FILE" || return 1
+
+  if [[ -n $DEVCONTAINER_ID ]]; then
+    grep -Fqx "devcontainer_id=$DEVCONTAINER_ID" \
+      "$POST_CREATE_MARKER_FILE" || return 1
+  fi
+
+  if [[ -n $RUNTIME_CONTAINER_ID ]]; then
+    grep -Fqx "runtime_container_id=$RUNTIME_CONTAINER_ID" \
+      "$POST_CREATE_MARKER_FILE" || return 1
+  fi
+
+  return 0
 }
 
 ##
@@ -618,29 +618,29 @@
 # Atomically writes the post-create completion marker.
 ##
 write_post_create_marker() {
-    local completed_at
-
-    if ((DRY_RUN == 1)); then
-        print_status "Would write post-create marker: $POST_CREATE_MARKER_FILE"
-        return 0
-    fi
-
-    completed_at="$(date -u "+%Y-%m-%dT%H:%M:%SZ")"
-    TEMPORARY_FILE="$(mktemp "${POST_CREATE_MARKER_FILE}.tmp.XXXXXX")"
-
-    {
-        printf "schema_version=%s\n" "$POST_CREATE_SCHEMA_VERSION"
-        printf "completed_at=%s\n" "$completed_at"
-        printf "devcontainer_id=%s\n" "$DEVCONTAINER_ID"
-        printf "runtime_container_id=%s\n" "$RUNTIME_CONTAINER_ID"
-        printf "repository_root=%s\n" "$REPO_ROOT"
-    } > "$TEMPORARY_FILE"
-
-    chmod 0600 "$TEMPORARY_FILE"
-    mv "$TEMPORARY_FILE" "$POST_CREATE_MARKER_FILE"
-    TEMPORARY_FILE=""
-
-    print_status "Wrote post-create marker: $POST_CREATE_MARKER_FILE"
+  local completed_at
+
+  if ((DRY_RUN == 1)); then
+    print_status "Would write post-create marker: $POST_CREATE_MARKER_FILE"
+    return 0
+  fi
+
+  completed_at="$(date -u "+%Y-%m-%dT%H:%M:%SZ")"
+  TEMPORARY_FILE="$(mktemp "${POST_CREATE_MARKER_FILE}.tmp.XXXXXX")"
+
+  {
+    printf "schema_version=%s\n" "$POST_CREATE_SCHEMA_VERSION"
+    printf "completed_at=%s\n" "$completed_at"
+    printf "devcontainer_id=%s\n" "$DEVCONTAINER_ID"
+    printf "runtime_container_id=%s\n" "$RUNTIME_CONTAINER_ID"
+    printf "repository_root=%s\n" "$REPO_ROOT"
+  } >"$TEMPORARY_FILE"
+
+  chmod 0600 "$TEMPORARY_FILE"
+  mv "$TEMPORARY_FILE" "$POST_CREATE_MARKER_FILE"
+  TEMPORARY_FILE=""
+
+  print_status "Wrote post-create marker: $POST_CREATE_MARKER_FILE"
 }
 
 # -----------------------------------------------------------------------------
@@ -654,26 +654,26 @@
 #   $1 - Hook path.
 ##
 run_hook() {
-    local hook="${1:?Hook path is required}"
-    local relative_hook="${hook#"$REPO_ROOT"/}"
-
-    if ((DRY_RUN == 1)); then
-        print_status "Would run hook: $relative_hook"
-        return 0
-    fi
-
-    print_status "Running hook: $relative_hook"
-
-    if bash "$hook"; then
-        return 0
-    fi
-
-    if is_enabled "${DEVCONTAINER_POST_CREATE_CONTINUE_ON_HOOK_FAILURE:-0}"; then
-        print_warning "Hook failed; continuing: $relative_hook"
-        return 0
-    fi
-
-    die "Hook failed: $relative_hook"
+  local hook="${1:?Hook path is required}"
+  local relative_hook="${hook#"$REPO_ROOT"/}"
+
+  if ((DRY_RUN == 1)); then
+    print_status "Would run hook: $relative_hook"
+    return 0
+  fi
+
+  print_status "Running hook: $relative_hook"
+
+  if bash "$hook"; then
+    return 0
+  fi
+
+  if is_enabled "${DEVCONTAINER_POST_CREATE_CONTINUE_ON_HOOK_FAILURE:-0}"; then
+    print_warning "Hook failed; continuing: $relative_hook"
+    return 0
+  fi
+
+  die "Hook failed: $relative_hook"
 }
 
 ##
@@ -680,41 +680,41 @@
 # Discovers and executes post-create hooks in deterministic filename order.
 ##
 run_extension_hooks() {
-    local hooks_directory="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/post-create.d"
-    local local_hook="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/post-create.local.sh"
-    local hook
-    local nullglob_was_enabled=0
-    local previous_lc_all="${LC_ALL-}"
-    local -a hooks=()
-
-    if [[ -f $local_hook ]]; then
-        run_hook "$local_hook"
-    fi
-
-    [[ -d $hooks_directory ]] || return 0
-
-    if shopt -q nullglob; then
-        nullglob_was_enabled=1
-    fi
-
-    LC_ALL=C
-    shopt -s nullglob
-    hooks=("$hooks_directory"/*.sh)
-
-    if ((nullglob_was_enabled == 0)); then
-        shopt -u nullglob
-    fi
-
-    if [[ -n $previous_lc_all ]]; then
-        LC_ALL="$previous_lc_all"
-    else
-        unset LC_ALL
-    fi
-
-    for hook in "${hooks[@]}"; do
-        [[ -f $hook ]] || continue
-        run_hook "$hook"
-    done
+  local hooks_directory="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/post-create.d"
+  local local_hook="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/post-create.local.sh"
+  local hook
+  local nullglob_was_enabled=0
+  local previous_lc_all="${LC_ALL-}"
+  local -a hooks=()
+
+  if [[ -f $local_hook ]]; then
+    run_hook "$local_hook"
+  fi
+
+  [[ -d $hooks_directory ]] || return 0
+
+  if shopt -q nullglob; then
+    nullglob_was_enabled=1
+  fi
+
+  LC_ALL=C
+  shopt -s nullglob
+  hooks=("$hooks_directory"/*.sh)
+
+  if ((nullglob_was_enabled == 0)); then
+    shopt -u nullglob
+  fi
+
+  if [[ -n $previous_lc_all ]]; then
+    LC_ALL="$previous_lc_all"
+  else
+    unset LC_ALL
+  fi
+
+  for hook in "${hooks[@]}"; do
+    [[ -f $hook ]] || continue
+    run_hook "$hook"
+  done
 }
 
 ##
@@ -727,13 +727,13 @@
 #   Writes the joined labels to stdout.
 ##
 join_labels() {
-    local label
-    local separator=""
-
-    for label in "$@"; do
-        printf "%s%s" "$separator" "$label"
-        separator=", "
-    done
+  local label
+  local separator=""
+
+  for label in "$@"; do
+    printf "%s%s" "$separator" "$label"
+    separator=", "
+  done
 }
 
 ##
@@ -740,28 +740,28 @@
 # Reports recognized project manifests without installing dependencies.
 ##
 report_detected_tooling() {
-    local -a detected=()
-
-    [[ -f $REPO_ROOT/package.json ]] && detected+=("Node.js")
-    [[ -f $REPO_ROOT/pyproject.toml || -f $REPO_ROOT/requirements.txt || -f $REPO_ROOT/setup.py ]] &&
-        detected+=("Python")
-    [[ -f $REPO_ROOT/Cargo.toml ]] && detected+=("Rust")
-    [[ -f $REPO_ROOT/go.mod ]] && detected+=("Go")
-    [[ -f $REPO_ROOT/pom.xml || -f $REPO_ROOT/build.gradle || -f $REPO_ROOT/build.gradle.kts ]] &&
-        detected+=("Java/JVM")
-    [[ -f $REPO_ROOT/Gemfile ]] && detected+=("Ruby")
-    [[ -f $REPO_ROOT/composer.json ]] && detected+=("PHP")
-    [[ -f $REPO_ROOT/compose.yaml || -f $REPO_ROOT/compose.yml || -f $REPO_ROOT/docker-compose.yaml || -f $REPO_ROOT/docker-compose.yml ]] &&
-        detected+=("Docker Compose")
-    [[ -f $REPO_ROOT/Taskfile.yml || -f $REPO_ROOT/Taskfile.yaml ]] && detected+=("Task")
-    [[ -f $REPO_ROOT/Makefile ]] && detected+=("Make")
-
-    if ((${#detected[@]} == 0)); then
-        print_debug "No common project manifests detected."
-        return 0
-    fi
-
-    print_status "Detected project tooling: $(join_labels "${detected[@]}")"
+  local -a detected=()
+
+  [[ -f $REPO_ROOT/package.json ]] && detected+=("Node.js")
+  [[ -f $REPO_ROOT/pyproject.toml || -f $REPO_ROOT/requirements.txt || -f $REPO_ROOT/setup.py ]] &&
+    detected+=("Python")
+  [[ -f $REPO_ROOT/Cargo.toml ]] && detected+=("Rust")
+  [[ -f $REPO_ROOT/go.mod ]] && detected+=("Go")
+  [[ -f $REPO_ROOT/pom.xml || -f $REPO_ROOT/build.gradle || -f $REPO_ROOT/build.gradle.kts ]] &&
+    detected+=("Java/JVM")
+  [[ -f $REPO_ROOT/Gemfile ]] && detected+=("Ruby")
+  [[ -f $REPO_ROOT/composer.json ]] && detected+=("PHP")
+  [[ -f $REPO_ROOT/compose.yaml || -f $REPO_ROOT/compose.yml || -f $REPO_ROOT/docker-compose.yaml || -f $REPO_ROOT/docker-compose.yml ]] &&
+    detected+=("Docker Compose")
+  [[ -f $REPO_ROOT/Taskfile.yml || -f $REPO_ROOT/Taskfile.yaml ]] && detected+=("Task")
+  [[ -f $REPO_ROOT/Makefile ]] && detected+=("Make")
+
+  if ((${#detected[@]} == 0)); then
+    print_debug "No common project manifests detected."
+    return 0
+  fi
+
+  print_status "Detected project tooling: $(join_labels "${detected[@]}")"
 }
 
 # -----------------------------------------------------------------------------
@@ -775,44 +775,44 @@
 #   $@ - Script arguments.
 ##
 main() {
-    parse_arguments "$@"
-    normalize_devcontainer_id
-    install_traps
-
-    if is_enabled "${DEVCONTAINER_POST_CREATE_VERBOSE:-0}"; then
-        VERBOSE=1
-    fi
-
-    if is_enabled "${DEVCONTAINER_POST_CREATE_FORCE:-0}"; then
-        FORCE_POST_CREATE=1
-    fi
-
-    if is_enabled "${DEVCONTAINER_POST_CREATE_TRACE:-0}"; then
-        set -o xtrace
-    fi
-
-    resolve_paths
-    resolve_runtime_container_id
-    cd -- "$REPO_ROOT"
-    configure_environment
-
-    print_status "Running post-create setup for the development container."
-    print_status "Repository root: $REPO_ROOT"
-
-    validate_environment
-    report_runtime_identity
-    create_required_directories
-    report_detected_tooling
-
-    if ((FORCE_POST_CREATE == 0)) && is_already_completed; then
-        print_status "Development container post-create state is already complete."
-        return 0
-    fi
-
-    run_extension_hooks
-    write_post_create_marker
-
-    print_status "Development container post-create lifecycle complete."
+  parse_arguments "$@"
+  normalize_devcontainer_id
+  install_traps
+
+  if is_enabled "${DEVCONTAINER_POST_CREATE_VERBOSE:-0}"; then
+    VERBOSE=1
+  fi
+
+  if is_enabled "${DEVCONTAINER_POST_CREATE_FORCE:-0}"; then
+    FORCE_POST_CREATE=1
+  fi
+
+  if is_enabled "${DEVCONTAINER_POST_CREATE_TRACE:-0}"; then
+    set -o xtrace
+  fi
+
+  resolve_paths
+  resolve_runtime_container_id
+  cd -- "$REPO_ROOT"
+  configure_environment
+
+  print_status "Running post-create setup for the development container."
+  print_status "Repository root: $REPO_ROOT"
+
+  validate_environment
+  report_runtime_identity
+  create_required_directories
+  report_detected_tooling
+
+  if ((FORCE_POST_CREATE == 0)) && is_already_completed; then
+    print_status "Development container post-create state is already complete."
+    return 0
+  fi
+
+  run_extension_hooks
+  write_post_create_marker
+
+  print_status "Development container post-create lifecycle complete."
 }
 
 main "$@"
diff .devcontainer/scripts/post-start.sh.orig .devcontainer/scripts/post-start.sh
--- .devcontainer/scripts/post-start.sh.orig
+++ .devcontainer/scripts/post-start.sh
@@ -103,7 +103,7 @@
 #   Writes the formatted message to stdout.
 ##
 print_status() {
-    printf "%s %s\n" "$STATUS_PREFIX" "$*"
+  printf "%s %s\n" "$STATUS_PREFIX" "$*"
 }
 
 ##
@@ -116,9 +116,9 @@
 #   Writes the formatted message to stderr when enabled.
 ##
 print_debug() {
-    if ((VERBOSE == 1)); then
-        printf "%s %s\n" "$DEBUG_PREFIX" "$*" >&2
-    fi
+  if ((VERBOSE == 1)); then
+    printf "%s %s\n" "$DEBUG_PREFIX" "$*" >&2
+  fi
 }
 
 ##
@@ -131,7 +131,7 @@
 #   Writes the formatted message to stderr.
 ##
 print_warning() {
-    printf "%s %s\n" "$WARNING_PREFIX" "$*" >&2
+  printf "%s %s\n" "$WARNING_PREFIX" "$*" >&2
 }
 
 ##
@@ -144,9 +144,9 @@
 #   Does not return.
 ##
 die() {
-    ERROR_REPORTED=1
-    printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
-    exit 1
+  ERROR_REPORTED=1
+  printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
+  exit 1
 }
 
 ##
@@ -159,10 +159,10 @@
 #   Does not return.
 ##
 usage_error() {
-    ERROR_REPORTED=1
-    printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
-    printf "%s Try --help for usage information.\n" "$ERROR_PREFIX" >&2
-    exit 2
+  ERROR_REPORTED=1
+  printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
+  printf "%s Try --help for usage information.\n" "$ERROR_PREFIX" >&2
+  exit 2
 }
 
 ##
@@ -175,14 +175,14 @@
 #   0 when enabled; otherwise 1.
 ##
 is_enabled() {
-    case "${1:-0}" in
-        1 | true | TRUE | yes | YES | on | ON)
-            return 0
-            ;;
-        *)
-            return 1
-            ;;
-    esac
+  case "${1:-0}" in
+  1 | true | TRUE | yes | YES | on | ON)
+    return 0
+    ;;
+  *)
+    return 1
+    ;;
+  esac
 }
 
 ##
@@ -192,11 +192,11 @@
 #   TEMPORARY_FILE
 ##
 cleanup() {
-    if [[ -n $TEMPORARY_FILE && -f $TEMPORARY_FILE ]]; then
-        rm -f "$TEMPORARY_FILE"
-    fi
-
-    TEMPORARY_FILE=""
+  if [[ -n $TEMPORARY_FILE && -f $TEMPORARY_FILE ]]; then
+    rm -f "$TEMPORARY_FILE"
+  fi
+
+  TEMPORARY_FILE=""
 }
 
 ##
@@ -211,20 +211,20 @@
 #   The original command exit code.
 ##
 on_error() {
-    local exit_code="${1:?Exit code is required}"
-    local line_number="${2:?Line number is required}"
-    local command="${3:-unknown command}"
-
-    trap - ERR
-    ERROR_REPORTED=1
-
-    printf "%s Command failed with exit code %d at line %d: %s\n" \
-        "$ERROR_PREFIX" \
-        "$exit_code" \
-        "$line_number" \
-        "$command" >&2
-
-    return "$exit_code"
+  local exit_code="${1:?Exit code is required}"
+  local line_number="${2:?Line number is required}"
+  local command="${3:-unknown command}"
+
+  trap - ERR
+  ERROR_REPORTED=1
+
+  printf "%s Command failed with exit code %d at line %d: %s\n" \
+    "$ERROR_PREFIX" \
+    "$exit_code" \
+    "$line_number" \
+    "$command" >&2
+
+  return "$exit_code"
 }
 
 ##
@@ -237,16 +237,16 @@
 #   Does not return.
 ##
 on_exit() {
-    local exit_code="${1:?Exit code is required}"
-
-    trap - EXIT
-    cleanup
-
-    if ((exit_code != 0 && ERROR_REPORTED == 0)); then
-        print_warning "Post-start lifecycle stopped with exit code $exit_code."
-    fi
-
-    exit "$exit_code"
+  local exit_code="${1:?Exit code is required}"
+
+  trap - EXIT
+  cleanup
+
+  if ((exit_code != 0 && ERROR_REPORTED == 0)); then
+    print_warning "Post-start lifecycle stopped with exit code $exit_code."
+  fi
+
+  exit "$exit_code"
 }
 
 ##
@@ -260,12 +260,12 @@
 #   Does not return.
 ##
 on_signal() {
-    local signal_name="${1:?Signal name is required}"
-    local exit_code="${2:?Exit code is required}"
-
-    ERROR_REPORTED=1
-    print_warning "Received $signal_name; stopping the post-start lifecycle."
-    exit "$exit_code"
+  local signal_name="${1:?Signal name is required}"
+  local exit_code="${2:?Exit code is required}"
+
+  ERROR_REPORTED=1
+  print_warning "Received $signal_name; stopping the post-start lifecycle."
+  exit "$exit_code"
 }
 
 ##
@@ -272,10 +272,10 @@
 # Installs error, exit, and signal handlers.
 ##
 install_traps() {
-    trap 'on_error "$?" "$LINENO" "$BASH_COMMAND"' ERR
-    trap 'on_exit "$?"' EXIT
-    trap 'on_signal "SIGINT" 130' INT
-    trap 'on_signal "SIGTERM" 143' TERM
+  trap 'on_error "$?" "$LINENO" "$BASH_COMMAND"' ERR
+  trap 'on_exit "$?"' EXIT
+  trap 'on_signal "SIGINT" 130' INT
+  trap 'on_signal "SIGTERM" 143' TERM
 }
 
 # -----------------------------------------------------------------------------
@@ -286,16 +286,16 @@
 # Prints command usage to stdout.
 ##
 print_usage() {
-    printf "Usage: %s [OPTIONS]\n" "${0##*/}"
-    printf "\n"
-    printf "Run repository-agnostic Dev Container post-start orchestration.\n"
-    printf "\n"
-    printf "Options:\n"
-    printf "  --devcontainer-id ID  Record and display the Dev Container identifier.\n"
-    printf "  --container-id ID     Override runtime container identity detection.\n"
-    printf "  --dry-run             Report actions without modifying files or running hooks.\n"
-    printf "  --verbose             Print additional diagnostic information.\n"
-    printf "  --help                Show this help text.\n"
+  printf "Usage: %s [OPTIONS]\n" "${0##*/}"
+  printf "\n"
+  printf "Run repository-agnostic Dev Container post-start orchestration.\n"
+  printf "\n"
+  printf "Options:\n"
+  printf "  --devcontainer-id ID  Record and display the Dev Container identifier.\n"
+  printf "  --container-id ID     Override runtime container identity detection.\n"
+  printf "  --dry-run             Report actions without modifying files or running hooks.\n"
+  printf "  --verbose             Print additional diagnostic information.\n"
+  printf "  --help                Show this help text.\n"
 }
 
 ##
@@ -305,52 +305,52 @@
 #   $@ - Script arguments.
 ##
 parse_arguments() {
-    while (($# > 0)); do
-        case "$1" in
-            --devcontainer-id)
-                (($# >= 2)) || usage_error "Option --devcontainer-id requires a value."
-                DEVCONTAINER_ID="$2"
-                shift
-                ;;
-            --devcontainer-id=*)
-                DEVCONTAINER_ID="${1#*=}"
-                ;;
-            --container-id)
-                (($# >= 2)) || usage_error "Option --container-id requires a value."
-                RUNTIME_CONTAINER_ID="$2"
-                shift
-                ;;
-            --container-id=*)
-                RUNTIME_CONTAINER_ID="${1#*=}"
-                ;;
-            --dry-run)
-                DRY_RUN=1
-                ;;
-            --verbose)
-                VERBOSE=1
-                ;;
-            --help)
-                print_usage
-                exit 0
-                ;;
-            --)
-                shift
-                break
-                ;;
-            -*)
-                usage_error "Unknown option: $1"
-                ;;
-            *)
-                usage_error "Unexpected positional argument: $1"
-                ;;
-        esac
-
-        shift
-    done
-
-    if (($# > 0)); then
-        usage_error "Unexpected positional argument: $1"
-    fi
+  while (($# > 0)); do
+    case "$1" in
+    --devcontainer-id)
+      (($# >= 2)) || usage_error "Option --devcontainer-id requires a value."
+      DEVCONTAINER_ID="$2"
+      shift
+      ;;
+    --devcontainer-id=*)
+      DEVCONTAINER_ID="${1#*=}"
+      ;;
+    --container-id)
+      (($# >= 2)) || usage_error "Option --container-id requires a value."
+      RUNTIME_CONTAINER_ID="$2"
+      shift
+      ;;
+    --container-id=*)
+      RUNTIME_CONTAINER_ID="${1#*=}"
+      ;;
+    --dry-run)
+      DRY_RUN=1
+      ;;
+    --verbose)
+      VERBOSE=1
+      ;;
+    --help)
+      print_usage
+      exit 0
+      ;;
+    --)
+      shift
+      break
+      ;;
+    -*)
+      usage_error "Unknown option: $1"
+      ;;
+    *)
+      usage_error "Unexpected positional argument: $1"
+      ;;
+    esac
+
+    shift
+  done
+
+  if (($# > 0)); then
+    usage_error "Unexpected positional argument: $1"
+  fi
 }
 
 ##
@@ -361,14 +361,14 @@
 #   $2 - Identifier value.
 ##
 validate_identifier() {
-    local label="${1:?Identifier label is required}"
-    local value="${2:-}"
-
-    case "$value" in
-        *$'\n'* | *$'\r'*)
-            usage_error "$label must not contain line breaks."
-            ;;
-    esac
+  local label="${1:?Identifier label is required}"
+  local value="${2:-}"
+
+  case "$value" in
+  *$'\n'* | *$'\r'*)
+    usage_error "$label must not contain line breaks."
+    ;;
+  esac
 }
 
 ##
@@ -375,12 +375,12 @@
 # Validates and normalizes the optional Dev Container identifier.
 ##
 normalize_devcontainer_id() {
-    if [[ $DEVCONTAINER_ID == "\${devcontainerId}" ]]; then
-        print_warning "The devcontainerId substitution was not resolved by this implementation."
-        DEVCONTAINER_ID=""
-    fi
-
-    validate_identifier "The Dev Container ID" "$DEVCONTAINER_ID"
+  if [[ $DEVCONTAINER_ID == "\${devcontainerId}" ]]; then
+    print_warning "The devcontainerId substitution was not resolved by this implementation."
+    DEVCONTAINER_ID=""
+  fi
+
+  validate_identifier "The Dev Container ID" "$DEVCONTAINER_ID"
 }
 
 # -----------------------------------------------------------------------------
@@ -397,12 +397,12 @@
 #   Writes the resolved path to stdout.
 ##
 resolve_directory() {
-    local directory="${1:?Directory path is required}"
-
-    (
-        cd -- "$directory"
-        pwd -P
-    )
+  local directory="${1:?Directory path is required}"
+
+  (
+    cd -- "$directory"
+    pwd -P
+  )
 }
 
 ##
@@ -409,16 +409,16 @@
 # Resolves the script directory and repository root.
 ##
 resolve_paths() {
-    SCRIPT_DIRECTORY="$(resolve_directory "$(dirname "${BASH_SOURCE[0]}")")"
-
-    if [[ -n ${DEVCONTAINER_REPO_ROOT:-} ]]; then
-        REPO_ROOT="$(resolve_directory "$DEVCONTAINER_REPO_ROOT")"
-    else
-        REPO_ROOT="$(resolve_directory "$SCRIPT_DIRECTORY/../..")"
-    fi
-
-    [[ -d $REPO_ROOT/$DEVCONTAINER_DIRECTORY ]] ||
-        print_warning "Expected Dev Container directory not found: $REPO_ROOT/$DEVCONTAINER_DIRECTORY"
+  SCRIPT_DIRECTORY="$(resolve_directory "$(dirname "${BASH_SOURCE[0]}")")"
+
+  if [[ -n ${DEVCONTAINER_REPO_ROOT:-} ]]; then
+    REPO_ROOT="$(resolve_directory "$DEVCONTAINER_REPO_ROOT")"
+  else
+    REPO_ROOT="$(resolve_directory "$SCRIPT_DIRECTORY/../..")"
+  fi
+
+  [[ -d $REPO_ROOT/$DEVCONTAINER_DIRECTORY ]] ||
+    print_warning "Expected Dev Container directory not found: $REPO_ROOT/$DEVCONTAINER_DIRECTORY"
 }
 
 ##
@@ -425,24 +425,24 @@
 # Resolves the runtime container identifier when one was not supplied.
 ##
 resolve_runtime_container_id() {
-    if [[ -n $RUNTIME_CONTAINER_ID ]]; then
-        validate_identifier "The runtime container ID" "$RUNTIME_CONTAINER_ID"
-        return 0
-    fi
-
-    if [[ -r /etc/hostname ]]; then
-        IFS= read -r RUNTIME_CONTAINER_ID < /etc/hostname || true
-    fi
-
-    if [[ -z $RUNTIME_CONTAINER_ID ]] && command -v hostname > /dev/null 2>&1; then
-        RUNTIME_CONTAINER_ID="$(hostname 2> /dev/null || true)"
-    fi
-
-    if [[ -z $RUNTIME_CONTAINER_ID && -n ${HOSTNAME:-} ]]; then
-        RUNTIME_CONTAINER_ID="$HOSTNAME"
-    fi
-
+  if [[ -n $RUNTIME_CONTAINER_ID ]]; then
     validate_identifier "The runtime container ID" "$RUNTIME_CONTAINER_ID"
+    return 0
+  fi
+
+  if [[ -r /etc/hostname ]]; then
+    IFS= read -r RUNTIME_CONTAINER_ID </etc/hostname || true
+  fi
+
+  if [[ -z $RUNTIME_CONTAINER_ID ]] && command -v hostname >/dev/null 2>&1; then
+    RUNTIME_CONTAINER_ID="$(hostname 2>/dev/null || true)"
+  fi
+
+  if [[ -z $RUNTIME_CONTAINER_ID && -n ${HOSTNAME:-} ]]; then
+    RUNTIME_CONTAINER_ID="$HOSTNAME"
+  fi
+
+  validate_identifier "The runtime container ID" "$RUNTIME_CONTAINER_ID"
 }
 
 ##
@@ -452,14 +452,14 @@
 #   $1 - Directory to add.
 ##
 prepend_path_once() {
-    local directory="${1:?Directory path is required}"
-
-    case ":${PATH:-}:" in
-        *":$directory:"*) ;;
-        *) PATH="$directory${PATH:+:$PATH}" ;;
-    esac
-
-    export PATH
+  local directory="${1:?Directory path is required}"
+
+  case ":${PATH:-}:" in
+  *":$directory:"*) ;;
+  *) PATH="$directory${PATH:+:$PATH}" ;;
+  esac
+
+  export PATH
 }
 
 ##
@@ -466,27 +466,27 @@
 # Configures the environment inherited by repository-specific hooks.
 ##
 configure_environment() {
-    : "${HOME:?HOME must be set for the Dev Container post-start lifecycle}"
-
-    export XDG_CACHE_HOME="${XDG_CACHE_HOME:-$HOME/.cache}"
-    export XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
-    export XDG_DATA_HOME="${XDG_DATA_HOME:-$HOME/.local/share}"
-    export XDG_STATE_HOME="${XDG_STATE_HOME:-$HOME/.local/state}"
-
-    POST_START_STATE_DIRECTORY="$XDG_STATE_HOME/$POST_START_STATE_DIRECTORY_NAME"
-    POST_START_MARKER_FILE="$POST_START_STATE_DIRECTORY/$POST_START_MARKER_NAME"
-
-    export DEVCONTAINER_ID
-    export DEVCONTAINER_RUNTIME_ID="$RUNTIME_CONTAINER_ID"
-    export DEVCONTAINER_LIFECYCLE="post-start"
-    export DEVCONTAINER_REPO_ROOT="$REPO_ROOT"
-    export DEVCONTAINER_SCRIPT_DIRECTORY="$SCRIPT_DIRECTORY"
-
-    prepend_path_once "$HOME/.local/bin"
-    prepend_path_once "$HOME/bin"
-
-    print_debug "Platform: $(uname -s 2> /dev/null || printf "unknown")/$(uname -m 2> /dev/null || printf "unknown")"
-    print_debug "PATH: $PATH"
+  : "${HOME:?HOME must be set for the Dev Container post-start lifecycle}"
+
+  export XDG_CACHE_HOME="${XDG_CACHE_HOME:-$HOME/.cache}"
+  export XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
+  export XDG_DATA_HOME="${XDG_DATA_HOME:-$HOME/.local/share}"
+  export XDG_STATE_HOME="${XDG_STATE_HOME:-$HOME/.local/state}"
+
+  POST_START_STATE_DIRECTORY="$XDG_STATE_HOME/$POST_START_STATE_DIRECTORY_NAME"
+  POST_START_MARKER_FILE="$POST_START_STATE_DIRECTORY/$POST_START_MARKER_NAME"
+
+  export DEVCONTAINER_ID
+  export DEVCONTAINER_RUNTIME_ID="$RUNTIME_CONTAINER_ID"
+  export DEVCONTAINER_LIFECYCLE="post-start"
+  export DEVCONTAINER_REPO_ROOT="$REPO_ROOT"
+  export DEVCONTAINER_SCRIPT_DIRECTORY="$SCRIPT_DIRECTORY"
+
+  prepend_path_once "$HOME/.local/bin"
+  prepend_path_once "$HOME/bin"
+
+  print_debug "Platform: $(uname -s 2>/dev/null || printf "unknown")/$(uname -m 2>/dev/null || printf "unknown")"
+  print_debug "PATH: $PATH"
 }
 
 # -----------------------------------------------------------------------------
@@ -497,17 +497,17 @@
 # Validates basic workspace and home-directory expectations.
 ##
 validate_environment() {
-    [[ -d $REPO_ROOT ]] || die "Repository root does not exist: $REPO_ROOT"
-    [[ -r $REPO_ROOT ]] || die "Repository root is not readable: $REPO_ROOT"
-    [[ -d $HOME ]] || die "Home directory does not exist: $HOME"
-
-    if [[ ! -w $REPO_ROOT ]]; then
-        print_warning "Repository root is not writable: $REPO_ROOT"
-    fi
-
-    if [[ ! -w $HOME ]]; then
-        print_warning "Home directory is not writable: $HOME"
-    fi
+  [[ -d $REPO_ROOT ]] || die "Repository root does not exist: $REPO_ROOT"
+  [[ -r $REPO_ROOT ]] || die "Repository root is not readable: $REPO_ROOT"
+  [[ -d $HOME ]] || die "Home directory does not exist: $HOME"
+
+  if [[ ! -w $REPO_ROOT ]]; then
+    print_warning "Repository root is not writable: $REPO_ROOT"
+  fi
+
+  if [[ ! -w $HOME ]]; then
+    print_warning "Home directory is not writable: $HOME"
+  fi
 }
 
 ##
@@ -518,25 +518,25 @@
 #   $2 - Optional directory mode. Defaults to 0755.
 ##
 ensure_directory() {
-    local directory="${1:?Directory path is required}"
-    local mode="${2:-0755}"
-
-    if [[ -d $directory ]]; then
-        print_debug "Directory already exists: $directory"
-        return 0
-    fi
-
-    if ((DRY_RUN == 1)); then
-        print_status "Would create directory: $directory"
-        return 0
-    fi
-
-    print_status "Creating directory: $directory"
-    mkdir -p "$directory"
-
-    if ! chmod "$mode" "$directory" 2> /dev/null; then
-        print_warning "Could not set mode $mode on directory: $directory"
-    fi
+  local directory="${1:?Directory path is required}"
+  local mode="${2:-0755}"
+
+  if [[ -d $directory ]]; then
+    print_debug "Directory already exists: $directory"
+    return 0
+  fi
+
+  if ((DRY_RUN == 1)); then
+    print_status "Would create directory: $directory"
+    return 0
+  fi
+
+  print_status "Creating directory: $directory"
+  mkdir -p "$directory"
+
+  if ! chmod "$mode" "$directory" 2>/dev/null; then
+    print_warning "Could not set mode $mode on directory: $directory"
+  fi
 }
 
 ##
@@ -543,14 +543,14 @@
 # Maintains baseline user-local and repository-local directories.
 ##
 create_required_directories() {
-    ensure_directory "$HOME/.local/bin"
-    ensure_directory "$XDG_CACHE_HOME"
-    ensure_directory "$XDG_CONFIG_HOME"
-    ensure_directory "$XDG_DATA_HOME"
-    ensure_directory "$XDG_STATE_HOME"
-    ensure_directory "$POST_START_STATE_DIRECTORY" 0700
-    ensure_directory "$REPO_ROOT/.cache" 0775
-    ensure_directory "$REPO_ROOT/.tmp" 0775
+  ensure_directory "$HOME/.local/bin"
+  ensure_directory "$XDG_CACHE_HOME"
+  ensure_directory "$XDG_CONFIG_HOME"
+  ensure_directory "$XDG_DATA_HOME"
+  ensure_directory "$XDG_STATE_HOME"
+  ensure_directory "$POST_START_STATE_DIRECTORY" 0700
+  ensure_directory "$REPO_ROOT/.cache" 0775
+  ensure_directory "$REPO_ROOT/.tmp" 0775
 }
 
 ##
@@ -557,29 +557,29 @@
 # Reports the effective user and container identifiers.
 ##
 report_runtime_identity() {
-    local user_name
-    local user_id
-    local group_name
-    local group_id
-
-    user_name="$(id -un 2> /dev/null || printf "unknown")"
-    user_id="$(id -u 2> /dev/null || printf "unknown")"
-    group_name="$(id -gn 2> /dev/null || printf "unknown")"
-    group_id="$(id -g 2> /dev/null || printf "unknown")"
-
-    print_status "Container user: $user_name (uid=$user_id, gid=$group_id, group=$group_name)"
-
-    if [[ -n $DEVCONTAINER_ID ]]; then
-        print_status "Dev Container ID: $DEVCONTAINER_ID"
-    else
-        print_status "Dev Container ID: unavailable"
-    fi
-
-    if [[ -n $RUNTIME_CONTAINER_ID ]]; then
-        print_status "Runtime container ID: $RUNTIME_CONTAINER_ID"
-    else
-        print_status "Runtime container ID: unavailable"
-    fi
+  local user_name
+  local user_id
+  local group_name
+  local group_id
+
+  user_name="$(id -un 2>/dev/null || printf "unknown")"
+  user_id="$(id -u 2>/dev/null || printf "unknown")"
+  group_name="$(id -gn 2>/dev/null || printf "unknown")"
+  group_id="$(id -g 2>/dev/null || printf "unknown")"
+
+  print_status "Container user: $user_name (uid=$user_id, gid=$group_id, group=$group_name)"
+
+  if [[ -n $DEVCONTAINER_ID ]]; then
+    print_status "Dev Container ID: $DEVCONTAINER_ID"
+  else
+    print_status "Dev Container ID: unavailable"
+  fi
+
+  if [[ -n $RUNTIME_CONTAINER_ID ]]; then
+    print_status "Runtime container ID: $RUNTIME_CONTAINER_ID"
+  else
+    print_status "Runtime container ID: unavailable"
+  fi
 }
 
 # -----------------------------------------------------------------------------
@@ -593,26 +593,26 @@
 #   $1 - Hook path.
 ##
 run_hook() {
-    local hook="${1:?Hook path is required}"
-    local relative_hook="${hook#"$REPO_ROOT"/}"
-
-    if ((DRY_RUN == 1)); then
-        print_status "Would run hook: $relative_hook"
-        return 0
-    fi
-
-    print_status "Running hook: $relative_hook"
-
-    if bash "$hook"; then
-        return 0
-    fi
-
-    if is_enabled "${DEVCONTAINER_POST_START_CONTINUE_ON_HOOK_FAILURE:-0}"; then
-        print_warning "Hook failed; continuing: $relative_hook"
-        return 0
-    fi
-
-    die "Hook failed: $relative_hook"
+  local hook="${1:?Hook path is required}"
+  local relative_hook="${hook#"$REPO_ROOT"/}"
+
+  if ((DRY_RUN == 1)); then
+    print_status "Would run hook: $relative_hook"
+    return 0
+  fi
+
+  print_status "Running hook: $relative_hook"
+
+  if bash "$hook"; then
+    return 0
+  fi
+
+  if is_enabled "${DEVCONTAINER_POST_START_CONTINUE_ON_HOOK_FAILURE:-0}"; then
+    print_warning "Hook failed; continuing: $relative_hook"
+    return 0
+  fi
+
+  die "Hook failed: $relative_hook"
 }
 
 ##
@@ -619,41 +619,41 @@
 # Discovers and executes post-start hooks in deterministic filename order.
 ##
 run_extension_hooks() {
-    local hooks_directory="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/post-start.d"
-    local local_hook="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/post-start.local.sh"
-    local hook
-    local nullglob_was_enabled=0
-    local previous_lc_all="${LC_ALL-}"
-    local -a hooks=()
-
-    if [[ -f $local_hook ]]; then
-        run_hook "$local_hook"
-    fi
-
-    [[ -d $hooks_directory ]] || return 0
-
-    if shopt -q nullglob; then
-        nullglob_was_enabled=1
-    fi
-
-    LC_ALL=C
-    shopt -s nullglob
-    hooks=("$hooks_directory"/*.sh)
-
-    if ((nullglob_was_enabled == 0)); then
-        shopt -u nullglob
-    fi
-
-    if [[ -n $previous_lc_all ]]; then
-        LC_ALL="$previous_lc_all"
-    else
-        unset LC_ALL
-    fi
-
-    for hook in "${hooks[@]}"; do
-        [[ -f $hook ]] || continue
-        run_hook "$hook"
-    done
+  local hooks_directory="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/post-start.d"
+  local local_hook="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/post-start.local.sh"
+  local hook
+  local nullglob_was_enabled=0
+  local previous_lc_all="${LC_ALL-}"
+  local -a hooks=()
+
+  if [[ -f $local_hook ]]; then
+    run_hook "$local_hook"
+  fi
+
+  [[ -d $hooks_directory ]] || return 0
+
+  if shopt -q nullglob; then
+    nullglob_was_enabled=1
+  fi
+
+  LC_ALL=C
+  shopt -s nullglob
+  hooks=("$hooks_directory"/*.sh)
+
+  if ((nullglob_was_enabled == 0)); then
+    shopt -u nullglob
+  fi
+
+  if [[ -n $previous_lc_all ]]; then
+    LC_ALL="$previous_lc_all"
+  else
+    unset LC_ALL
+  fi
+
+  for hook in "${hooks[@]}"; do
+    [[ -f $hook ]] || continue
+    run_hook "$hook"
+  done
 }
 
 ##
@@ -660,20 +660,20 @@
 # Loads and increments the previous successful post-start run count.
 ##
 calculate_run_count() {
-    local previous_run_count=""
-
-    [[ -f $POST_START_MARKER_FILE ]] || return 0
-
-    previous_run_count="$(
-        awk -F= '$1 == "run_count" { print $2; exit }' \
-            "$POST_START_MARKER_FILE" 2> /dev/null || true
-    )"
-
-    if [[ $previous_run_count =~ ^[0-9]+$ ]]; then
-        POST_START_RUN_COUNT=$((previous_run_count + 1))
-    else
-        print_warning "Ignoring an invalid previous post-start run count."
-    fi
+  local previous_run_count=""
+
+  [[ -f $POST_START_MARKER_FILE ]] || return 0
+
+  previous_run_count="$(
+    awk -F= '$1 == "run_count" { print $2; exit }' \
+      "$POST_START_MARKER_FILE" 2>/dev/null || true
+  )"
+
+  if [[ $previous_run_count =~ ^[0-9]+$ ]]; then
+    POST_START_RUN_COUNT=$((previous_run_count + 1))
+  else
+    print_warning "Ignoring an invalid previous post-start run count."
+  fi
 }
 
 ##
@@ -680,30 +680,30 @@
 # Atomically records metadata for the latest successful post-start run.
 ##
 write_post_start_marker() {
-    local completed_at
-
-    if ((DRY_RUN == 1)); then
-        print_status "Would record post-start run $POST_START_RUN_COUNT: $POST_START_MARKER_FILE"
-        return 0
-    fi
-
-    completed_at="$(date -u "+%Y-%m-%dT%H:%M:%SZ")"
-    TEMPORARY_FILE="$(mktemp "${POST_START_MARKER_FILE}.tmp.XXXXXX")"
-
-    {
-        printf "schema_version=%s\n" "$POST_START_SCHEMA_VERSION"
-        printf "completed_at=%s\n" "$completed_at"
-        printf "run_count=%s\n" "$POST_START_RUN_COUNT"
-        printf "devcontainer_id=%s\n" "$DEVCONTAINER_ID"
-        printf "runtime_container_id=%s\n" "$RUNTIME_CONTAINER_ID"
-        printf "repository_root=%s\n" "$REPO_ROOT"
-    } > "$TEMPORARY_FILE"
-
-    chmod 0600 "$TEMPORARY_FILE"
-    mv "$TEMPORARY_FILE" "$POST_START_MARKER_FILE"
-    TEMPORARY_FILE=""
-
-    print_debug "Recorded post-start run $POST_START_RUN_COUNT: $POST_START_MARKER_FILE"
+  local completed_at
+
+  if ((DRY_RUN == 1)); then
+    print_status "Would record post-start run $POST_START_RUN_COUNT: $POST_START_MARKER_FILE"
+    return 0
+  fi
+
+  completed_at="$(date -u "+%Y-%m-%dT%H:%M:%SZ")"
+  TEMPORARY_FILE="$(mktemp "${POST_START_MARKER_FILE}.tmp.XXXXXX")"
+
+  {
+    printf "schema_version=%s\n" "$POST_START_SCHEMA_VERSION"
+    printf "completed_at=%s\n" "$completed_at"
+    printf "run_count=%s\n" "$POST_START_RUN_COUNT"
+    printf "devcontainer_id=%s\n" "$DEVCONTAINER_ID"
+    printf "runtime_container_id=%s\n" "$RUNTIME_CONTAINER_ID"
+    printf "repository_root=%s\n" "$REPO_ROOT"
+  } >"$TEMPORARY_FILE"
+
+  chmod 0600 "$TEMPORARY_FILE"
+  mv "$TEMPORARY_FILE" "$POST_START_MARKER_FILE"
+  TEMPORARY_FILE=""
+
+  print_debug "Recorded post-start run $POST_START_RUN_COUNT: $POST_START_MARKER_FILE"
 }
 
 ##
@@ -710,16 +710,16 @@
 # Prints the final startup status message.
 ##
 print_startup_complete() {
-    if ((DRY_RUN == 1)); then
-        print_status "Post-start dry run complete."
-        return 0
-    fi
-
-    if [[ -n $DEVCONTAINER_ID ]]; then
-        print_status "Development $DEVCONTAINER_ID container started successfully!"
-    else
-        print_status "Development container started successfully!"
-    fi
+  if ((DRY_RUN == 1)); then
+    print_status "Post-start dry run complete."
+    return 0
+  fi
+
+  if [[ -n $DEVCONTAINER_ID ]]; then
+    print_status "Development $DEVCONTAINER_ID container started successfully!"
+  else
+    print_status "Development container started successfully!"
+  fi
 }
 
 # -----------------------------------------------------------------------------
@@ -733,33 +733,33 @@
 #   $@ - Script arguments.
 ##
 main() {
-    parse_arguments "$@"
-    normalize_devcontainer_id
-    install_traps
-
-    if is_enabled "${DEVCONTAINER_POST_START_VERBOSE:-0}"; then
-        VERBOSE=1
-    fi
-
-    if is_enabled "${DEVCONTAINER_POST_START_TRACE:-0}"; then
-        set -o xtrace
-    fi
-
-    resolve_paths
-    resolve_runtime_container_id
-    cd -- "$REPO_ROOT"
-    configure_environment
-
-    print_status "Running development container post-start setup."
-    print_status "Repository root: $REPO_ROOT"
-
-    validate_environment
-    report_runtime_identity
-    create_required_directories
-    calculate_run_count
-    run_extension_hooks
-    write_post_start_marker
-    print_startup_complete
+  parse_arguments "$@"
+  normalize_devcontainer_id
+  install_traps
+
+  if is_enabled "${DEVCONTAINER_POST_START_VERBOSE:-0}"; then
+    VERBOSE=1
+  fi
+
+  if is_enabled "${DEVCONTAINER_POST_START_TRACE:-0}"; then
+    set -o xtrace
+  fi
+
+  resolve_paths
+  resolve_runtime_container_id
+  cd -- "$REPO_ROOT"
+  configure_environment
+
+  print_status "Running development container post-start setup."
+  print_status "Repository root: $REPO_ROOT"
+
+  validate_environment
+  report_runtime_identity
+  create_required_directories
+  calculate_run_count
+  run_extension_hooks
+  write_post_start_marker
+  print_startup_complete
 }
 
 main "$@"
diff .devcontainer/scripts/update-content.sh.orig .devcontainer/scripts/update-content.sh
--- .devcontainer/scripts/update-content.sh.orig
+++ .devcontainer/scripts/update-content.sh
@@ -104,7 +104,7 @@
 #   Writes the formatted message to stdout.
 ##
 print_status() {
-    printf "%s %s\n" "$STATUS_PREFIX" "$*"
+  printf "%s %s\n" "$STATUS_PREFIX" "$*"
 }
 
 ##
@@ -117,9 +117,9 @@
 #   Writes the formatted message to stderr when enabled.
 ##
 print_debug() {
-    if ((VERBOSE == 1)); then
-        printf "%s %s\n" "$DEBUG_PREFIX" "$*" >&2
-    fi
+  if ((VERBOSE == 1)); then
+    printf "%s %s\n" "$DEBUG_PREFIX" "$*" >&2
+  fi
 }
 
 ##
@@ -132,7 +132,7 @@
 #   Writes the formatted message to stderr.
 ##
 print_warning() {
-    printf "%s %s\n" "$WARNING_PREFIX" "$*" >&2
+  printf "%s %s\n" "$WARNING_PREFIX" "$*" >&2
 }
 
 ##
@@ -145,9 +145,9 @@
 #   Does not return.
 ##
 die() {
-    ERROR_REPORTED=1
-    printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
-    exit 1
+  ERROR_REPORTED=1
+  printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
+  exit 1
 }
 
 ##
@@ -160,10 +160,10 @@
 #   Does not return.
 ##
 usage_error() {
-    ERROR_REPORTED=1
-    printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
-    printf "%s Try --help for usage information.\n" "$ERROR_PREFIX" >&2
-    exit 2
+  ERROR_REPORTED=1
+  printf "%s %s\n" "$ERROR_PREFIX" "$*" >&2
+  printf "%s Try --help for usage information.\n" "$ERROR_PREFIX" >&2
+  exit 2
 }
 
 ##
@@ -176,14 +176,14 @@
 #   0 when enabled; otherwise 1.
 ##
 is_enabled() {
-    case "${1:-0}" in
-        1 | true | TRUE | yes | YES | on | ON)
-            return 0
-            ;;
-        *)
-            return 1
-            ;;
-    esac
+  case "${1:-0}" in
+  1 | true | TRUE | yes | YES | on | ON)
+    return 0
+    ;;
+  *)
+    return 1
+    ;;
+  esac
 }
 
 ##
@@ -193,11 +193,11 @@
 #   TEMPORARY_FILE
 ##
 cleanup() {
-    if [[ -n $TEMPORARY_FILE && -f $TEMPORARY_FILE ]]; then
-        rm -f "$TEMPORARY_FILE"
-    fi
-
-    TEMPORARY_FILE=""
+  if [[ -n $TEMPORARY_FILE && -f $TEMPORARY_FILE ]]; then
+    rm -f "$TEMPORARY_FILE"
+  fi
+
+  TEMPORARY_FILE=""
 }
 
 ##
@@ -212,20 +212,20 @@
 #   The original command exit code.
 ##
 on_error() {
-    local exit_code="${1:?Exit code is required}"
-    local line_number="${2:?Line number is required}"
-    local command="${3:-unknown command}"
-
-    trap - ERR
-    ERROR_REPORTED=1
-
-    printf "%s Command failed with exit code %d at line %d: %s\n" \
-        "$ERROR_PREFIX" \
-        "$exit_code" \
-        "$line_number" \
-        "$command" >&2
-
-    return "$exit_code"
+  local exit_code="${1:?Exit code is required}"
+  local line_number="${2:?Line number is required}"
+  local command="${3:-unknown command}"
+
+  trap - ERR
+  ERROR_REPORTED=1
+
+  printf "%s Command failed with exit code %d at line %d: %s\n" \
+    "$ERROR_PREFIX" \
+    "$exit_code" \
+    "$line_number" \
+    "$command" >&2
+
+  return "$exit_code"
 }
 
 ##
@@ -238,16 +238,16 @@
 #   Does not return.
 ##
 on_exit() {
-    local exit_code="${1:?Exit code is required}"
-
-    trap - EXIT
-    cleanup
-
-    if ((exit_code != 0 && ERROR_REPORTED == 0)); then
-        print_warning "Update-content lifecycle stopped with exit code $exit_code."
-    fi
-
-    exit "$exit_code"
+  local exit_code="${1:?Exit code is required}"
+
+  trap - EXIT
+  cleanup
+
+  if ((exit_code != 0 && ERROR_REPORTED == 0)); then
+    print_warning "Update-content lifecycle stopped with exit code $exit_code."
+  fi
+
+  exit "$exit_code"
 }
 
 ##
@@ -261,12 +261,12 @@
 #   Does not return.
 ##
 on_signal() {
-    local signal_name="${1:?Signal name is required}"
-    local exit_code="${2:?Exit code is required}"
-
-    ERROR_REPORTED=1
-    print_warning "Received $signal_name; stopping the update-content lifecycle."
-    exit "$exit_code"
+  local signal_name="${1:?Signal name is required}"
+  local exit_code="${2:?Exit code is required}"
+
+  ERROR_REPORTED=1
+  print_warning "Received $signal_name; stopping the update-content lifecycle."
+  exit "$exit_code"
 }
 
 ##
@@ -273,10 +273,10 @@
 # Installs error, exit, and signal handlers.
 ##
 install_traps() {
-    trap 'on_error "$?" "$LINENO" "$BASH_COMMAND"' ERR
-    trap 'on_exit "$?"' EXIT
-    trap 'on_signal "SIGINT" 130' INT
-    trap 'on_signal "SIGTERM" 143' TERM
+  trap 'on_error "$?" "$LINENO" "$BASH_COMMAND"' ERR
+  trap 'on_exit "$?"' EXIT
+  trap 'on_signal "SIGINT" 130' INT
+  trap 'on_signal "SIGTERM" 143' TERM
 }
 
 # -----------------------------------------------------------------------------
@@ -287,16 +287,16 @@
 # Prints command usage to stdout.
 ##
 print_usage() {
-    printf "Usage: %s [OPTIONS]\n" "${0##*/}"
-    printf "\n"
-    printf "Reconcile the container after workspace content is updated.\n"
-    printf "\n"
-    printf "Options:\n"
-    printf "  --devcontainer-id ID  Record and display the Dev Container identifier.\n"
-    printf "  --container-id ID     Override runtime container identity detection.\n"
-    printf "  --dry-run             Report actions without modifying files or running hooks.\n"
-    printf "  --verbose             Print additional diagnostic information.\n"
-    printf "  --help                Show this help text.\n"
+  printf "Usage: %s [OPTIONS]\n" "${0##*/}"
+  printf "\n"
+  printf "Reconcile the container after workspace content is updated.\n"
+  printf "\n"
+  printf "Options:\n"
+  printf "  --devcontainer-id ID  Record and display the Dev Container identifier.\n"
+  printf "  --container-id ID     Override runtime container identity detection.\n"
+  printf "  --dry-run             Report actions without modifying files or running hooks.\n"
+  printf "  --verbose             Print additional diagnostic information.\n"
+  printf "  --help                Show this help text.\n"
 }
 
 ##
@@ -306,52 +306,52 @@
 #   $@ - Script arguments.
 ##
 parse_arguments() {
-    while (($# > 0)); do
-        case "$1" in
-            --devcontainer-id)
-                (($# >= 2)) || usage_error "Option --devcontainer-id requires a value."
-                DEVCONTAINER_ID="$2"
-                shift
-                ;;
-            --devcontainer-id=*)
-                DEVCONTAINER_ID="${1#*=}"
-                ;;
-            --container-id)
-                (($# >= 2)) || usage_error "Option --container-id requires a value."
-                RUNTIME_CONTAINER_ID="$2"
-                shift
-                ;;
-            --container-id=*)
-                RUNTIME_CONTAINER_ID="${1#*=}"
-                ;;
-            --dry-run)
-                DRY_RUN=1
-                ;;
-            --verbose)
-                VERBOSE=1
-                ;;
-            --help)
-                print_usage
-                exit 0
-                ;;
-            --)
-                shift
-                break
-                ;;
-            -*)
-                usage_error "Unknown option: $1"
-                ;;
-            *)
-                usage_error "Unexpected positional argument: $1"
-                ;;
-        esac
-
-        shift
-    done
-
-    if (($# > 0)); then
-        usage_error "Unexpected positional argument: $1"
-    fi
+  while (($# > 0)); do
+    case "$1" in
+    --devcontainer-id)
+      (($# >= 2)) || usage_error "Option --devcontainer-id requires a value."
+      DEVCONTAINER_ID="$2"
+      shift
+      ;;
+    --devcontainer-id=*)
+      DEVCONTAINER_ID="${1#*=}"
+      ;;
+    --container-id)
+      (($# >= 2)) || usage_error "Option --container-id requires a value."
+      RUNTIME_CONTAINER_ID="$2"
+      shift
+      ;;
+    --container-id=*)
+      RUNTIME_CONTAINER_ID="${1#*=}"
+      ;;
+    --dry-run)
+      DRY_RUN=1
+      ;;
+    --verbose)
+      VERBOSE=1
+      ;;
+    --help)
+      print_usage
+      exit 0
+      ;;
+    --)
+      shift
+      break
+      ;;
+    -*)
+      usage_error "Unknown option: $1"
+      ;;
+    *)
+      usage_error "Unexpected positional argument: $1"
+      ;;
+    esac
+
+    shift
+  done
+
+  if (($# > 0)); then
+    usage_error "Unexpected positional argument: $1"
+  fi
 }
 
 ##
@@ -362,14 +362,14 @@
 #   $2 - Identifier value.
 ##
 validate_identifier() {
-    local label="${1:?Identifier label is required}"
-    local value="${2:-}"
-
-    case "$value" in
-        *$'\n'* | *$'\r'*)
-            usage_error "$label must not contain line breaks."
-            ;;
-    esac
+  local label="${1:?Identifier label is required}"
+  local value="${2:-}"
+
+  case "$value" in
+  *$'\n'* | *$'\r'*)
+    usage_error "$label must not contain line breaks."
+    ;;
+  esac
 }
 
 ##
@@ -376,12 +376,12 @@
 # Validates and normalizes the optional Dev Container identifier.
 ##
 normalize_devcontainer_id() {
-    if [[ $DEVCONTAINER_ID == "\${devcontainerId}" ]]; then
-        print_warning "The devcontainerId substitution was not resolved by this implementation."
-        DEVCONTAINER_ID=""
-    fi
-
-    validate_identifier "The Dev Container ID" "$DEVCONTAINER_ID"
+  if [[ $DEVCONTAINER_ID == "\${devcontainerId}" ]]; then
+    print_warning "The devcontainerId substitution was not resolved by this implementation."
+    DEVCONTAINER_ID=""
+  fi
+
+  validate_identifier "The Dev Container ID" "$DEVCONTAINER_ID"
 }
 
 # -----------------------------------------------------------------------------
@@ -398,12 +398,12 @@
 #   Writes the resolved path to stdout.
 ##
 resolve_directory() {
-    local directory="${1:?Directory path is required}"
-
-    (
-        cd -- "$directory"
-        pwd -P
-    )
+  local directory="${1:?Directory path is required}"
+
+  (
+    cd -- "$directory"
+    pwd -P
+  )
 }
 
 ##
@@ -410,16 +410,16 @@
 # Resolves the script directory and repository root.
 ##
 resolve_paths() {
-    SCRIPT_DIRECTORY="$(resolve_directory "$(dirname "${BASH_SOURCE[0]}")")"
-
-    if [[ -n ${DEVCONTAINER_REPO_ROOT:-} ]]; then
-        REPO_ROOT="$(resolve_directory "$DEVCONTAINER_REPO_ROOT")"
-    else
-        REPO_ROOT="$(resolve_directory "$SCRIPT_DIRECTORY/../..")"
-    fi
-
-    [[ -d $REPO_ROOT/$DEVCONTAINER_DIRECTORY ]] ||
-        print_warning "Expected Dev Container directory not found: $REPO_ROOT/$DEVCONTAINER_DIRECTORY"
+  SCRIPT_DIRECTORY="$(resolve_directory "$(dirname "${BASH_SOURCE[0]}")")"
+
+  if [[ -n ${DEVCONTAINER_REPO_ROOT:-} ]]; then
+    REPO_ROOT="$(resolve_directory "$DEVCONTAINER_REPO_ROOT")"
+  else
+    REPO_ROOT="$(resolve_directory "$SCRIPT_DIRECTORY/../..")"
+  fi
+
+  [[ -d $REPO_ROOT/$DEVCONTAINER_DIRECTORY ]] ||
+    print_warning "Expected Dev Container directory not found: $REPO_ROOT/$DEVCONTAINER_DIRECTORY"
 }
 
 ##
@@ -426,24 +426,24 @@
 # Resolves the runtime container identifier when one was not supplied.
 ##
 resolve_runtime_container_id() {
-    if [[ -n $RUNTIME_CONTAINER_ID ]]; then
-        validate_identifier "The runtime container ID" "$RUNTIME_CONTAINER_ID"
-        return 0
-    fi
-
-    if [[ -r /etc/hostname ]]; then
-        IFS= read -r RUNTIME_CONTAINER_ID < /etc/hostname || true
-    fi
-
-    if [[ -z $RUNTIME_CONTAINER_ID ]] && command -v hostname > /dev/null 2>&1; then
-        RUNTIME_CONTAINER_ID="$(hostname 2> /dev/null || true)"
-    fi
-
-    if [[ -z $RUNTIME_CONTAINER_ID && -n ${HOSTNAME:-} ]]; then
-        RUNTIME_CONTAINER_ID="$HOSTNAME"
-    fi
-
+  if [[ -n $RUNTIME_CONTAINER_ID ]]; then
     validate_identifier "The runtime container ID" "$RUNTIME_CONTAINER_ID"
+    return 0
+  fi
+
+  if [[ -r /etc/hostname ]]; then
+    IFS= read -r RUNTIME_CONTAINER_ID </etc/hostname || true
+  fi
+
+  if [[ -z $RUNTIME_CONTAINER_ID ]] && command -v hostname >/dev/null 2>&1; then
+    RUNTIME_CONTAINER_ID="$(hostname 2>/dev/null || true)"
+  fi
+
+  if [[ -z $RUNTIME_CONTAINER_ID && -n ${HOSTNAME:-} ]]; then
+    RUNTIME_CONTAINER_ID="$HOSTNAME"
+  fi
+
+  validate_identifier "The runtime container ID" "$RUNTIME_CONTAINER_ID"
 }
 
 ##
@@ -453,14 +453,14 @@
 #   $1 - Directory to add.
 ##
 prepend_path_once() {
-    local directory="${1:?Directory path is required}"
-
-    case ":${PATH:-}:" in
-        *":$directory:"*) ;;
-        *) PATH="$directory${PATH:+:$PATH}" ;;
-    esac
-
-    export PATH
+  local directory="${1:?Directory path is required}"
+
+  case ":${PATH:-}:" in
+  *":$directory:"*) ;;
+  *) PATH="$directory${PATH:+:$PATH}" ;;
+  esac
+
+  export PATH
 }
 
 ##
@@ -467,27 +467,27 @@
 # Configures the environment inherited by repository-specific hooks.
 ##
 configure_environment() {
-    : "${HOME:?HOME must be set for the Dev Container update-content lifecycle}"
-
-    export XDG_CACHE_HOME="${XDG_CACHE_HOME:-$HOME/.cache}"
-    export XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
-    export XDG_DATA_HOME="${XDG_DATA_HOME:-$HOME/.local/share}"
-    export XDG_STATE_HOME="${XDG_STATE_HOME:-$HOME/.local/state}"
-
-    UPDATE_STATE_DIRECTORY="$XDG_STATE_HOME/$UPDATE_STATE_DIRECTORY_NAME"
-    UPDATE_MARKER_FILE="$UPDATE_STATE_DIRECTORY/$UPDATE_MARKER_NAME"
-
-    export DEVCONTAINER_ID
-    export DEVCONTAINER_RUNTIME_ID="$RUNTIME_CONTAINER_ID"
-    export DEVCONTAINER_LIFECYCLE="update-content"
-    export DEVCONTAINER_REPO_ROOT="$REPO_ROOT"
-    export DEVCONTAINER_SCRIPT_DIRECTORY="$SCRIPT_DIRECTORY"
-
-    prepend_path_once "$HOME/.local/bin"
-    prepend_path_once "$HOME/bin"
-
-    print_debug "Platform: $(uname -s 2> /dev/null || printf "unknown")/$(uname -m 2> /dev/null || printf "unknown")"
-    print_debug "PATH: $PATH"
+  : "${HOME:?HOME must be set for the Dev Container update-content lifecycle}"
+
+  export XDG_CACHE_HOME="${XDG_CACHE_HOME:-$HOME/.cache}"
+  export XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
+  export XDG_DATA_HOME="${XDG_DATA_HOME:-$HOME/.local/share}"
+  export XDG_STATE_HOME="${XDG_STATE_HOME:-$HOME/.local/state}"
+
+  UPDATE_STATE_DIRECTORY="$XDG_STATE_HOME/$UPDATE_STATE_DIRECTORY_NAME"
+  UPDATE_MARKER_FILE="$UPDATE_STATE_DIRECTORY/$UPDATE_MARKER_NAME"
+
+  export DEVCONTAINER_ID
+  export DEVCONTAINER_RUNTIME_ID="$RUNTIME_CONTAINER_ID"
+  export DEVCONTAINER_LIFECYCLE="update-content"
+  export DEVCONTAINER_REPO_ROOT="$REPO_ROOT"
+  export DEVCONTAINER_SCRIPT_DIRECTORY="$SCRIPT_DIRECTORY"
+
+  prepend_path_once "$HOME/.local/bin"
+  prepend_path_once "$HOME/bin"
+
+  print_debug "Platform: $(uname -s 2>/dev/null || printf "unknown")/$(uname -m 2>/dev/null || printf "unknown")"
+  print_debug "PATH: $PATH"
 }
 
 # -----------------------------------------------------------------------------
@@ -498,17 +498,17 @@
 # Validates basic workspace and home-directory expectations.
 ##
 validate_environment() {
-    [[ -d $REPO_ROOT ]] || die "Repository root does not exist: $REPO_ROOT"
-    [[ -r $REPO_ROOT ]] || die "Repository root is not readable: $REPO_ROOT"
-    [[ -d $HOME ]] || die "Home directory does not exist: $HOME"
-
-    if [[ ! -w $REPO_ROOT ]]; then
-        print_warning "Repository root is not writable: $REPO_ROOT"
-    fi
-
-    if [[ ! -w $HOME ]]; then
-        print_warning "Home directory is not writable: $HOME"
-    fi
+  [[ -d $REPO_ROOT ]] || die "Repository root does not exist: $REPO_ROOT"
+  [[ -r $REPO_ROOT ]] || die "Repository root is not readable: $REPO_ROOT"
+  [[ -d $HOME ]] || die "Home directory does not exist: $HOME"
+
+  if [[ ! -w $REPO_ROOT ]]; then
+    print_warning "Repository root is not writable: $REPO_ROOT"
+  fi
+
+  if [[ ! -w $HOME ]]; then
+    print_warning "Home directory is not writable: $HOME"
+  fi
 }
 
 ##
@@ -519,25 +519,25 @@
 #   $2 - Optional directory mode. Defaults to 0755.
 ##
 ensure_directory() {
-    local directory="${1:?Directory path is required}"
-    local mode="${2:-0755}"
-
-    if [[ -d $directory ]]; then
-        print_debug "Directory already exists: $directory"
-        return 0
-    fi
-
-    if ((DRY_RUN == 1)); then
-        print_status "Would create directory: $directory"
-        return 0
-    fi
-
-    print_status "Creating directory: $directory"
-    mkdir -p "$directory"
-
-    if ! chmod "$mode" "$directory" 2> /dev/null; then
-        print_warning "Could not set mode $mode on directory: $directory"
-    fi
+  local directory="${1:?Directory path is required}"
+  local mode="${2:-0755}"
+
+  if [[ -d $directory ]]; then
+    print_debug "Directory already exists: $directory"
+    return 0
+  fi
+
+  if ((DRY_RUN == 1)); then
+    print_status "Would create directory: $directory"
+    return 0
+  fi
+
+  print_status "Creating directory: $directory"
+  mkdir -p "$directory"
+
+  if ! chmod "$mode" "$directory" 2>/dev/null; then
+    print_warning "Could not set mode $mode on directory: $directory"
+  fi
 }
 
 ##
@@ -544,14 +544,14 @@
 # Creates baseline user-local and repository-local directories.
 ##
 create_required_directories() {
-    ensure_directory "$HOME/.local/bin"
-    ensure_directory "$XDG_CACHE_HOME"
-    ensure_directory "$XDG_CONFIG_HOME"
-    ensure_directory "$XDG_DATA_HOME"
-    ensure_directory "$XDG_STATE_HOME"
-    ensure_directory "$UPDATE_STATE_DIRECTORY" 0700
-    ensure_directory "$REPO_ROOT/.cache" 0775
-    ensure_directory "$REPO_ROOT/.tmp" 0775
+  ensure_directory "$HOME/.local/bin"
+  ensure_directory "$XDG_CACHE_HOME"
+  ensure_directory "$XDG_CONFIG_HOME"
+  ensure_directory "$XDG_DATA_HOME"
+  ensure_directory "$XDG_STATE_HOME"
+  ensure_directory "$UPDATE_STATE_DIRECTORY" 0700
+  ensure_directory "$REPO_ROOT/.cache" 0775
+  ensure_directory "$REPO_ROOT/.tmp" 0775
 }
 
 ##
@@ -558,19 +558,19 @@
 # Captures lightweight Git metadata for the completion marker.
 ##
 capture_repository_state() {
-    local status_output
-
-    command -v git > /dev/null 2>&1 || return 0
-    git -C "$REPO_ROOT" rev-parse --is-inside-work-tree > /dev/null 2>&1 || return 0
-
-    GIT_COMMIT="$(git -C "$REPO_ROOT" rev-parse HEAD 2> /dev/null || true)"
-    status_output="$(git -C "$REPO_ROOT" status --porcelain 2> /dev/null || true)"
-
-    if [[ -n $status_output ]]; then
-        GIT_DIRTY="true"
-    else
-        GIT_DIRTY="false"
-    fi
+  local status_output
+
+  command -v git >/dev/null 2>&1 || return 0
+  git -C "$REPO_ROOT" rev-parse --is-inside-work-tree >/dev/null 2>&1 || return 0
+
+  GIT_COMMIT="$(git -C "$REPO_ROOT" rev-parse HEAD 2>/dev/null || true)"
+  status_output="$(git -C "$REPO_ROOT" status --porcelain 2>/dev/null || true)"
+
+  if [[ -n $status_output ]]; then
+    GIT_DIRTY="true"
+  else
+    GIT_DIRTY="false"
+  fi
 }
 
 ##
@@ -577,18 +577,18 @@
 # Calculates the next successful update-content run number.
 ##
 calculate_run_count() {
-    local previous_count=""
-
-    [[ -f $UPDATE_MARKER_FILE ]] || return 0
-
-    previous_count="$(
-        awk -F= '$1 == "run_count" { print $2; exit }' \
-            "$UPDATE_MARKER_FILE" 2> /dev/null || true
-    )"
-
-    if [[ $previous_count =~ ^[0-9]+$ ]]; then
-        UPDATE_RUN_COUNT=$((previous_count + 1))
-    fi
+  local previous_count=""
+
+  [[ -f $UPDATE_MARKER_FILE ]] || return 0
+
+  previous_count="$(
+    awk -F= '$1 == "run_count" { print $2; exit }' \
+      "$UPDATE_MARKER_FILE" 2>/dev/null || true
+  )"
+
+  if [[ $previous_count =~ ^[0-9]+$ ]]; then
+    UPDATE_RUN_COUNT=$((previous_count + 1))
+  fi
 }
 
 ##
@@ -595,35 +595,35 @@
 # Reports the effective user, container, and repository identifiers.
 ##
 report_runtime_context() {
-    local user_name
-    local user_id
-    local group_name
-    local group_id
-
-    user_name="$(id -un 2> /dev/null || printf "unknown")"
-    user_id="$(id -u 2> /dev/null || printf "unknown")"
-    group_name="$(id -gn 2> /dev/null || printf "unknown")"
-    group_id="$(id -g 2> /dev/null || printf "unknown")"
-
-    print_status "Container user: $user_name (uid=$user_id, gid=$group_id, group=$group_name)"
-
-    if [[ -n $DEVCONTAINER_ID ]]; then
-        print_status "Dev Container ID: $DEVCONTAINER_ID"
-    else
-        print_status "Dev Container ID: unavailable"
-    fi
-
-    if [[ -n $RUNTIME_CONTAINER_ID ]]; then
-        print_status "Runtime container ID: $RUNTIME_CONTAINER_ID"
-    else
-        print_status "Runtime container ID: unavailable"
-    fi
-
-    if [[ -n $GIT_COMMIT ]]; then
-        print_status "Git content state: commit=$GIT_COMMIT, dirty=$GIT_DIRTY"
-    fi
-
-    print_status "Update-content run: $UPDATE_RUN_COUNT"
+  local user_name
+  local user_id
+  local group_name
+  local group_id
+
+  user_name="$(id -un 2>/dev/null || printf "unknown")"
+  user_id="$(id -u 2>/dev/null || printf "unknown")"
+  group_name="$(id -gn 2>/dev/null || printf "unknown")"
+  group_id="$(id -g 2>/dev/null || printf "unknown")"
+
+  print_status "Container user: $user_name (uid=$user_id, gid=$group_id, group=$group_name)"
+
+  if [[ -n $DEVCONTAINER_ID ]]; then
+    print_status "Dev Container ID: $DEVCONTAINER_ID"
+  else
+    print_status "Dev Container ID: unavailable"
+  fi
+
+  if [[ -n $RUNTIME_CONTAINER_ID ]]; then
+    print_status "Runtime container ID: $RUNTIME_CONTAINER_ID"
+  else
+    print_status "Runtime container ID: unavailable"
+  fi
+
+  if [[ -n $GIT_COMMIT ]]; then
+    print_status "Git content state: commit=$GIT_COMMIT, dirty=$GIT_DIRTY"
+  fi
+
+  print_status "Update-content run: $UPDATE_RUN_COUNT"
 }
 
 ##
@@ -630,32 +630,32 @@
 # Atomically writes the latest update-content completion marker.
 ##
 write_update_marker() {
-    local completed_at
-
-    if ((DRY_RUN == 1)); then
-        print_status "Would write update-content marker: $UPDATE_MARKER_FILE"
-        return 0
-    fi
-
-    completed_at="$(date -u "+%Y-%m-%dT%H:%M:%SZ")"
-    TEMPORARY_FILE="$(mktemp "${UPDATE_MARKER_FILE}.tmp.XXXXXX")"
-
-    {
-        printf "schema_version=%s\n" "$UPDATE_SCHEMA_VERSION"
-        printf "completed_at=%s\n" "$completed_at"
-        printf "run_count=%s\n" "$UPDATE_RUN_COUNT"
-        printf "devcontainer_id=%s\n" "$DEVCONTAINER_ID"
-        printf "runtime_container_id=%s\n" "$RUNTIME_CONTAINER_ID"
-        printf "git_commit=%s\n" "$GIT_COMMIT"
-        printf "git_dirty=%s\n" "$GIT_DIRTY"
-        printf "repository_root=%s\n" "$REPO_ROOT"
-    } > "$TEMPORARY_FILE"
-
-    chmod 0600 "$TEMPORARY_FILE"
-    mv "$TEMPORARY_FILE" "$UPDATE_MARKER_FILE"
-    TEMPORARY_FILE=""
-
-    print_status "Wrote update-content marker: $UPDATE_MARKER_FILE"
+  local completed_at
+
+  if ((DRY_RUN == 1)); then
+    print_status "Would write update-content marker: $UPDATE_MARKER_FILE"
+    return 0
+  fi
+
+  completed_at="$(date -u "+%Y-%m-%dT%H:%M:%SZ")"
+  TEMPORARY_FILE="$(mktemp "${UPDATE_MARKER_FILE}.tmp.XXXXXX")"
+
+  {
+    printf "schema_version=%s\n" "$UPDATE_SCHEMA_VERSION"
+    printf "completed_at=%s\n" "$completed_at"
+    printf "run_count=%s\n" "$UPDATE_RUN_COUNT"
+    printf "devcontainer_id=%s\n" "$DEVCONTAINER_ID"
+    printf "runtime_container_id=%s\n" "$RUNTIME_CONTAINER_ID"
+    printf "git_commit=%s\n" "$GIT_COMMIT"
+    printf "git_dirty=%s\n" "$GIT_DIRTY"
+    printf "repository_root=%s\n" "$REPO_ROOT"
+  } >"$TEMPORARY_FILE"
+
+  chmod 0600 "$TEMPORARY_FILE"
+  mv "$TEMPORARY_FILE" "$UPDATE_MARKER_FILE"
+  TEMPORARY_FILE=""
+
+  print_status "Wrote update-content marker: $UPDATE_MARKER_FILE"
 }
 
 # -----------------------------------------------------------------------------
@@ -669,26 +669,26 @@
 #   $1 - Hook path.
 ##
 run_hook() {
-    local hook="${1:?Hook path is required}"
-    local relative_hook="${hook#"$REPO_ROOT"/}"
-
-    if ((DRY_RUN == 1)); then
-        print_status "Would run hook: $relative_hook"
-        return 0
-    fi
-
-    print_status "Running hook: $relative_hook"
-
-    if bash "$hook"; then
-        return 0
-    fi
-
-    if is_enabled "${DEVCONTAINER_UPDATE_CONTENT_CONTINUE_ON_HOOK_FAILURE:-0}"; then
-        print_warning "Hook failed; continuing: $relative_hook"
-        return 0
-    fi
-
-    die "Hook failed: $relative_hook"
+  local hook="${1:?Hook path is required}"
+  local relative_hook="${hook#"$REPO_ROOT"/}"
+
+  if ((DRY_RUN == 1)); then
+    print_status "Would run hook: $relative_hook"
+    return 0
+  fi
+
+  print_status "Running hook: $relative_hook"
+
+  if bash "$hook"; then
+    return 0
+  fi
+
+  if is_enabled "${DEVCONTAINER_UPDATE_CONTENT_CONTINUE_ON_HOOK_FAILURE:-0}"; then
+    print_warning "Hook failed; continuing: $relative_hook"
+    return 0
+  fi
+
+  die "Hook failed: $relative_hook"
 }
 
 ##
@@ -695,41 +695,41 @@
 # Discovers and executes update-content hooks in deterministic filename order.
 ##
 run_extension_hooks() {
-    local hooks_directory="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/update-content.d"
-    local local_hook="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/update-content.local.sh"
-    local hook
-    local nullglob_was_enabled=0
-    local previous_lc_all="${LC_ALL-}"
-    local -a hooks=()
-
-    if [[ -f $local_hook ]]; then
-        run_hook "$local_hook"
-    fi
-
-    [[ -d $hooks_directory ]] || return 0
-
-    if shopt -q nullglob; then
-        nullglob_was_enabled=1
-    fi
-
-    LC_ALL=C
-    shopt -s nullglob
-    hooks=("$hooks_directory"/*.sh)
-
-    if ((nullglob_was_enabled == 0)); then
-        shopt -u nullglob
-    fi
-
-    if [[ -n $previous_lc_all ]]; then
-        LC_ALL="$previous_lc_all"
-    else
-        unset LC_ALL
-    fi
-
-    for hook in "${hooks[@]}"; do
-        [[ -f $hook ]] || continue
-        run_hook "$hook"
-    done
+  local hooks_directory="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/update-content.d"
+  local local_hook="$REPO_ROOT/$DEVCONTAINER_DIRECTORY/scripts/update-content.local.sh"
+  local hook
+  local nullglob_was_enabled=0
+  local previous_lc_all="${LC_ALL-}"
+  local -a hooks=()
+
+  if [[ -f $local_hook ]]; then
+    run_hook "$local_hook"
+  fi
+
+  [[ -d $hooks_directory ]] || return 0
+
+  if shopt -q nullglob; then
+    nullglob_was_enabled=1
+  fi
+
+  LC_ALL=C
+  shopt -s nullglob
+  hooks=("$hooks_directory"/*.sh)
+
+  if ((nullglob_was_enabled == 0)); then
+    shopt -u nullglob
+  fi
+
+  if [[ -n $previous_lc_all ]]; then
+    LC_ALL="$previous_lc_all"
+  else
+    unset LC_ALL
+  fi
+
+  for hook in "${hooks[@]}"; do
+    [[ -f $hook ]] || continue
+    run_hook "$hook"
+  done
 }
 
 ##
@@ -742,13 +742,13 @@
 #   Writes the joined labels to stdout.
 ##
 join_labels() {
-    local label
-    local separator=""
-
-    for label in "$@"; do
-        printf "%s%s" "$separator" "$label"
-        separator=", "
-    done
+  local label
+  local separator=""
+
+  for label in "$@"; do
+    printf "%s%s" "$separator" "$label"
+    separator=", "
+  done
 }
 
 ##
@@ -755,28 +755,28 @@
 # Reports recognized project manifests without installing dependencies.
 ##
 report_detected_tooling() {
-    local -a detected=()
-
-    [[ -f $REPO_ROOT/package.json ]] && detected+=("Node.js")
-    [[ -f $REPO_ROOT/pyproject.toml || -f $REPO_ROOT/requirements.txt || -f $REPO_ROOT/setup.py ]] &&
-        detected+=("Python")
-    [[ -f $REPO_ROOT/Cargo.toml ]] && detected+=("Rust")
-    [[ -f $REPO_ROOT/go.mod ]] && detected+=("Go")
-    [[ -f $REPO_ROOT/pom.xml || -f $REPO_ROOT/build.gradle || -f $REPO_ROOT/build.gradle.kts ]] &&
-        detected+=("Java/JVM")
-    [[ -f $REPO_ROOT/Gemfile ]] && detected+=("Ruby")
-    [[ -f $REPO_ROOT/composer.json ]] && detected+=("PHP")
-    [[ -f $REPO_ROOT/compose.yaml || -f $REPO_ROOT/compose.yml || -f $REPO_ROOT/docker-compose.yaml || -f $REPO_ROOT/docker-compose.yml ]] &&
-        detected+=("Docker Compose")
-    [[ -f $REPO_ROOT/Taskfile.yml || -f $REPO_ROOT/Taskfile.yaml ]] && detected+=("Task")
-    [[ -f $REPO_ROOT/Makefile ]] && detected+=("Make")
-
-    if ((${#detected[@]} == 0)); then
-        print_debug "No common project manifests detected."
-        return 0
-    fi
-
-    print_status "Detected project tooling: $(join_labels "${detected[@]}")"
+  local -a detected=()
+
+  [[ -f $REPO_ROOT/package.json ]] && detected+=("Node.js")
+  [[ -f $REPO_ROOT/pyproject.toml || -f $REPO_ROOT/requirements.txt || -f $REPO_ROOT/setup.py ]] &&
+    detected+=("Python")
+  [[ -f $REPO_ROOT/Cargo.toml ]] && detected+=("Rust")
+  [[ -f $REPO_ROOT/go.mod ]] && detected+=("Go")
+  [[ -f $REPO_ROOT/pom.xml || -f $REPO_ROOT/build.gradle || -f $REPO_ROOT/build.gradle.kts ]] &&
+    detected+=("Java/JVM")
+  [[ -f $REPO_ROOT/Gemfile ]] && detected+=("Ruby")
+  [[ -f $REPO_ROOT/composer.json ]] && detected+=("PHP")
+  [[ -f $REPO_ROOT/compose.yaml || -f $REPO_ROOT/compose.yml || -f $REPO_ROOT/docker-compose.yaml || -f $REPO_ROOT/docker-compose.yml ]] &&
+    detected+=("Docker Compose")
+  [[ -f $REPO_ROOT/Taskfile.yml || -f $REPO_ROOT/Taskfile.yaml ]] && detected+=("Task")
+  [[ -f $REPO_ROOT/Makefile ]] && detected+=("Make")
+
+  if ((${#detected[@]} == 0)); then
+    print_debug "No common project manifests detected."
+    return 0
+  fi
+
+  print_status "Detected project tooling: $(join_labels "${detected[@]}")"
 }
 
 # -----------------------------------------------------------------------------
@@ -790,36 +790,36 @@
 #   $@ - Script arguments.
 ##
 main() {
-    parse_arguments "$@"
-    normalize_devcontainer_id
-    install_traps
-
-    if is_enabled "${DEVCONTAINER_UPDATE_CONTENT_VERBOSE:-0}"; then
-        VERBOSE=1
-    fi
-
-    if is_enabled "${DEVCONTAINER_UPDATE_CONTENT_TRACE:-0}"; then
-        set -o xtrace
-    fi
-
-    resolve_paths
-    resolve_runtime_container_id
-    cd -- "$REPO_ROOT"
-    configure_environment
-
-    print_status "Reconciling updated development container content."
-    print_status "Repository root: $REPO_ROOT"
-
-    validate_environment
-    create_required_directories
-    capture_repository_state
-    calculate_run_count
-    report_runtime_context
-    report_detected_tooling
-    run_extension_hooks
-    write_update_marker
-
-    print_status "Development container content reconciliation complete."
+  parse_arguments "$@"
+  normalize_devcontainer_id
+  install_traps
+
+  if is_enabled "${DEVCONTAINER_UPDATE_CONTENT_VERBOSE:-0}"; then
+    VERBOSE=1
+  fi
+
+  if is_enabled "${DEVCONTAINER_UPDATE_CONTENT_TRACE:-0}"; then
+    set -o xtrace
+  fi
+
+  resolve_paths
+  resolve_runtime_container_id
+  cd -- "$REPO_ROOT"
+  configure_environment
+
+  print_status "Reconciling updated development container content."
+  print_status "Repository root: $REPO_ROOT"
+
+  validate_environment
+  create_required_directories
+  capture_repository_state
+  calculate_run_count
+  report_runtime_context
+  report_detected_tooling
+  run_extension_hooks
+  write_update_marker
+
+  print_status "Development container content reconciliation complete."
 }
 
 main "$@"

